== APP / LAYOUT ==
import { Toaster } from "@/components/ui/sonner";
import { TooltipProvider } from "@/components/ui/tooltip";
import NotFound from "@/pages/NotFound";
import { Route, Switch } from "wouter";
import ErrorBoundary from "./components/ErrorBoundary";
import { ThemeProvider } from "./contexts/ThemeContext";
import Home from "./pages/Home";
import { PwaStatusBanner, PwaStatusProvider } from "./pwa/PwaStatus";
import { PwaMobileNavigation } from "./pwa/PwaMobileNavigation";

function Router() {
  return <Switch><Route path="/" component={Home} /><Route path="/404" component={NotFound} /><Route component={NotFound} /></Switch>;
}

export default function App() {
  return <ErrorBoundary><ThemeProvider defaultTheme="dark"><PwaStatusProvider><TooltipProvider><PwaStatusBanner /><Toaster theme="dark" richColors position="top-right" /><Router /><PwaMobileNavigation /></TooltipProvider></PwaStatusProvider></ThemeProvider></ErrorBoundary>;
}
import { useAuth } from "@/_core/hooks/useAuth";
import { Avatar, AvatarFallback } from "@/components/ui/avatar";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu";
import {
  Sidebar,
  SidebarContent,
  SidebarFooter,
  SidebarHeader,
  SidebarInset,
  SidebarMenu,
  SidebarMenuButton,
  SidebarMenuItem,
  SidebarProvider,
  SidebarTrigger,
  useSidebar,
} from "@/components/ui/sidebar";
import { startLogin } from "@/const";
import { useIsMobile } from "@/hooks/useMobile";
import { LayoutDashboard, LogOut, PanelLeft, Users } from "lucide-react";
import { CSSProperties, useEffect, useRef, useState } from "react";
import { useLocation } from "wouter";
import { DashboardLayoutSkeleton } from './DashboardLayoutSkeleton';
import { Button } from "./ui/button";

const menuItems = [
  { icon: LayoutDashboard, label: "Page 1", path: "/" },
  { icon: Users, label: "Page 2", path: "/some-path" },
];

const SIDEBAR_WIDTH_KEY = "sidebar-width";
const DEFAULT_WIDTH = 280;
const MIN_WIDTH = 200;
const MAX_WIDTH = 480;

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const [sidebarWidth, setSidebarWidth] = useState(() => {
    const saved = localStorage.getItem(SIDEBAR_WIDTH_KEY);
    return saved ? parseInt(saved, 10) : DEFAULT_WIDTH;
  });
  const { loading, user } = useAuth();

  useEffect(() => {
    localStorage.setItem(SIDEBAR_WIDTH_KEY, sidebarWidth.toString());
  }, [sidebarWidth]);

  if (loading) {
    return <DashboardLayoutSkeleton />
  }

  if (!user) {
    return (
      <div className="flex items-center justify-center min-h-screen">
        <div className="flex flex-col items-center gap-8 p-8 max-w-md w-full">
          <div className="flex flex-col items-center gap-6">
            <h1 className="text-2xl font-semibold tracking-tight text-center">
              Sign in to continue
            </h1>
            <p className="text-sm text-muted-foreground text-center max-w-sm">
              Access to this dashboard requires authentication. Continue to launch the login flow.
            </p>
          </div>
          <Button
            onClick={() => startLogin()}
            size="lg"
            className="w-full shadow-lg hover:shadow-xl transition-all"
          >
            Sign in
          </Button>
        </div>
      </div>
    );
  }

  return (
    <SidebarProvider
      style={
        {
          "--sidebar-width": `${sidebarWidth}px`,
        } as CSSProperties
      }
    >
      <DashboardLayoutContent setSidebarWidth={setSidebarWidth}>
        {children}
      </DashboardLayoutContent>
    </SidebarProvider>
  );
}

type DashboardLayoutContentProps = {
  children: React.ReactNode;
  setSidebarWidth: (width: number) => void;
};

function DashboardLayoutContent({
  children,
  setSidebarWidth,
}: DashboardLayoutContentProps) {
  const { user, logout } = useAuth();
  const [location, setLocation] = useLocation();
  const { state, toggleSidebar } = useSidebar();
  const isCollapsed = state === "collapsed";
  const [isResizing, setIsResizing] = useState(false);
  const sidebarRef = useRef<HTMLDivElement>(null);
  const activeMenuItem = menuItems.find(item => item.path === location);
  const isMobile = useIsMobile();

  useEffect(() => {
    if (isCollapsed) {
      setIsResizing(false);
    }
  }, [isCollapsed]);

  useEffect(() => {
    const handleMouseMove = (e: MouseEvent) => {
      if (!isResizing) return;

      const sidebarLeft = sidebarRef.current?.getBoundingClientRect().left ?? 0;
      const newWidth = e.clientX - sidebarLeft;
      if (newWidth >= MIN_WIDTH && newWidth <= MAX_WIDTH) {
        setSidebarWidth(newWidth);
      }
    };

    const handleMouseUp = () => {
      setIsResizing(false);
    };

    if (isResizing) {
      document.addEventListener("mousemove", handleMouseMove);
      document.addEventListener("mouseup", handleMouseUp);
      document.body.style.cursor = "col-resize";
      document.body.style.userSelect = "none";
    }

    return () => {
      document.removeEventListener("mousemove", handleMouseMove);
      document.removeEventListener("mouseup", handleMouseUp);
      document.body.style.cursor = "";
      document.body.style.userSelect = "";
    };
  }, [isResizing, setSidebarWidth]);

  return (
    <>
      <div className="relative" ref={sidebarRef}>
        <Sidebar
          collapsible="icon"
          className="border-r-0"
          disableTransition={isResizing}
        >
          <SidebarHeader className="h-16 justify-center">
            <div className="flex items-center gap-3 px-2 transition-all w-full">
              <button
                onClick={toggleSidebar}
                className="h-8 w-8 flex items-center justify-center hover:bg-accent rounded-lg transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-ring shrink-0"
                aria-label="Toggle navigation"
              >
                <PanelLeft className="h-4 w-4 text-muted-foreground" />
              </button>
              {!isCollapsed ? (
                <div className="flex items-center gap-2 min-w-0">
                  <span className="font-semibold tracking-tight truncate">
                    Navigation
                  </span>
                </div>
              ) : null}
            </div>
          </SidebarHeader>

          <SidebarContent className="gap-0">
            <SidebarMenu className="px-2 py-1">
              {menuItems.map(item => {
                const isActive = location === item.path;
                return (
                  <SidebarMenuItem key={item.path}>
                    <SidebarMenuButton
                      isActive={isActive}
                      onClick={() => setLocation(item.path)}
                      tooltip={item.label}
                      className={`h-10 transition-all font-normal`}
                    >
                      <item.icon
                        className={`h-4 w-4 ${isActive ? "text-primary" : ""}`}
                      />
                      <span>{item.label}</span>
                    </SidebarMenuButton>
                  </SidebarMenuItem>
                );
              })}
            </SidebarMenu>
          </SidebarContent>

          <SidebarFooter className="p-3">
            <DropdownMenu>
              <DropdownMenuTrigger asChild>
                <button className="flex items-center gap-3 rounded-lg px-1 py-1 hover:bg-accent/50 transition-colors w-full text-left group-data-[collapsible=icon]:justify-center focus:outline-none focus-visible:ring-2 focus-visible:ring-ring">
                  <Avatar className="h-9 w-9 border shrink-0">
                    <AvatarFallback className="text-xs font-medium">
                      {user?.name?.charAt(0).toUpperCase()}
                    </AvatarFallback>
                  </Avatar>
                  <div className="flex-1 min-w-0 group-data-[collapsible=icon]:hidden">
                    <p className="text-sm font-medium truncate leading-none">
                      {user?.name || "-"}
                    </p>
                    <p className="text-xs text-muted-foreground truncate mt-1.5">
                      {user?.email || "-"}
                    </p>
                  </div>
                </button>
              </DropdownMenuTrigger>
              <DropdownMenuContent align="end" className="w-48">
                <DropdownMenuItem
                  onClick={logout}
                  className="cursor-pointer text-destructive focus:text-destructive"
                >
                  <LogOut className="mr-2 h-4 w-4" />
                  <span>Sign out</span>
                </DropdownMenuItem>
              </DropdownMenuContent>
            </DropdownMenu>
          </SidebarFooter>
        </Sidebar>
        <div
          className={`absolute top-0 right-0 w-1 h-full cursor-col-resize hover:bg-primary/20 transition-colors ${isCollapsed ? "hidden" : ""}`}
          onMouseDown={() => {
            if (isCollapsed) return;
            setIsResizing(true);
          }}
          style={{ zIndex: 50 }}
        />
      </div>

      <SidebarInset>
        {isMobile && (
          <div className="flex border-b h-14 items-center justify-between bg-background/95 px-2 backdrop-blur supports-[backdrop-filter]:backdrop-blur sticky top-0 z-40">
            <div className="flex items-center gap-2">
              <SidebarTrigger className="h-9 w-9 rounded-lg bg-background" />
              <div className="flex items-center gap-3">
                <div className="flex flex-col gap-1">
                  <span className="tracking-tight text-foreground">
                    {activeMenuItem?.label ?? "Menu"}
                  </span>
                </div>
              </div>
            </div>
          </div>
        )}
        <main className="flex-1 p-4">{children}</main>
      </SidebarInset>
    </>
  );
}
== NAVIGATION / HOME ==
import { Activity, BellRing, FlaskConical, Gauge, Menu, ScanSearch, Target, TrendingUp, WalletCards } from "lucide-react";
import { useState } from "react";

type PwaNavTarget = "dashboard" | "scanner" | "discovery" | "setup-monitor" | "scalping" | "swing" | "paper" | "alerts" | "research";

function navigate(target: PwaNavTarget) {
  window.dispatchEvent(new CustomEvent<PwaNavTarget>("crypto-hub:navigate", { detail: target }));
}

export function PwaMobileNavigation() {
  const [moreOpen, setMoreOpen] = useState(false);
  const primaryItems = [
    { label: "Home", target: "dashboard" as const, icon: Gauge },
    { label: "Scanner", target: "scanner" as const, icon: ScanSearch },
    { label: "Scalp", target: "scalping" as const, icon: Target },
    { label: "Swing", target: "swing" as const, icon: TrendingUp },
    { label: "Paper", target: "paper" as const, icon: WalletCards },
  ];
  const moreItems = [
    { label: "Discovery", target: "discovery" as const, icon: ScanSearch },
    { label: "Setup Monitor", target: "setup-monitor" as const, icon: Activity },
    { label: "Alerts", target: "alerts" as const, icon: BellRing },
    { label: "Research", target: "research" as const, icon: FlaskConical },
  ];
  const choose = (target: PwaNavTarget) => { setMoreOpen(false); navigate(target); };
  return <nav aria-label="Mobile workspace navigation" className="pwa-mobile-nav fixed inset-x-0 bottom-0 z-[80] flex border-t border-white/[.08] bg-[#080d17]/95 px-1 pt-1 backdrop-blur-xl lg:hidden">{moreOpen ? <div id="pwa-mobile-more" className="pwa-mobile-more absolute inset-x-2 bottom-full mb-2 rounded-xl border border-white/[.1] bg-[#0b1321]/98 p-2 shadow-2xl"><p className="px-2 py-1 text-[9px] font-semibold uppercase tracking-[.16em] text-slate-500">More workspaces</p>{moreItems.map(item => { const Icon = item.icon; return <button key={item.target} onClick={() => choose(item.target)} className="flex w-full items-center gap-3 rounded-lg px-3 py-3 text-left text-xs text-slate-200 hover:bg-white/[.06]"><Icon className="h-4 w-4 text-cyan-200" />{item.label}</button>; })}</div> : null}{primaryItems.map(item => { const Icon = item.icon; return <button key={item.target} onClick={() => choose(item.target)} className="flex min-w-0 flex-1 flex-col items-center gap-1 rounded-lg px-1 py-2 text-[10px] font-medium text-slate-400 hover:bg-white/[.05] hover:text-cyan-100"><Icon className="h-4 w-4" /><span className="truncate">{item.label}</span></button>; })}<button aria-expanded={moreOpen} aria-controls="pwa-mobile-more" onClick={() => setMoreOpen(value => !value)} className="flex min-w-0 flex-1 flex-col items-center gap-1 rounded-lg px-1 py-2 text-[10px] font-medium text-slate-400 hover:bg-white/[.05] hover:text-cyan-100"><Menu className="h-4 w-4" /><span className="truncate">More</span></button></nav>;
}
import { Button } from "@/components/ui/button";
import { SettingsPanel } from "@/components/crypto/SettingsPanel";
import { PaperTradingWorkspace } from "@/components/crypto/PaperTradingWorkspace";
import { BacktestingPanel } from "@/components/crypto/BacktestingPanel";
import { AlertsPanel } from "@/components/crypto/AlertsPanel";
import { ResearchSummaryPanel } from "@/components/crypto/ResearchSummaryPanel";
import { OpportunityResearchLab } from "@/components/crypto/OpportunityResearchLab";
import { HistoricalDataFoundation } from "@/components/crypto/HistoricalDataFoundation";
import { ExecutionCostLab } from "@/components/crypto/ExecutionCostLab";
import { BackupRecoveryPanel } from "@/components/crypto/BackupRecoveryPanel";
import { AssetIntelligencePanel } from "@/components/crypto/AssetIntelligencePanel";
import { TradeSetupWorkspace } from "@/components/crypto/TradeSetupWorkspace";
import { OpportunityDiscoveryWorkspace } from "@/components/crypto/OpportunityDiscoveryWorkspace";
import { SetupMonitorWorkspace } from "@/components/crypto/SetupMonitorWorkspace";
import { Badge } from "@/components/ui/badge";
import { Progress } from "@/components/ui/progress";
import { Skeleton } from "@/components/ui/skeleton";
import { Switch } from "@/components/ui/switch";
import { trpc } from "@/lib/trpc";
import type { ScannerResponse, ScannerRow } from "@shared/crypto";
import { cn } from "@/lib/utils";
import { OnlineStatusLabel, usePwaStatus } from "@/pwa/PwaStatus";
import {
  Activity, AlertTriangle, ArchiveRestore, ArrowDownRight, ArrowUpRight, BarChart3, BellRing, BookOpen, Calculator, ChevronDown,
  ChevronRight, Clock3, Database, FlaskConical, Gauge, Layers3, LineChart, Loader2, PanelLeft,
  RefreshCw, ScanSearch, Settings2, ShieldCheck, SlidersHorizontal, Sparkles, Target, TrendingUp,
  WalletCards, X,
} from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { toast } from "sonner";

type NavItem = { label: string; icon: typeof Gauge; phase: "live" | "planned" };

const navigation: NavItem[] = [
  { label: "Dashboard", icon: Gauge, phase: "live" },
  { label: "Market Scanner", icon: ScanSearch, phase: "live" },
  { label: "Opportunity Discovery", icon: Sparkles, phase: "live" },
  { label: "Setup Monitor", icon: Activity, phase: "live" },
  { label: "Scalping", icon: Target, phase: "live" },
  { label: "Swing", icon: TrendingUp, phase: "live" },
  { label: "Research Summary", icon: FlaskConical, phase: "live" },
  { label: "Research Lab", icon: FlaskConical, phase: "live" },
  { label: "Historical Data", icon: Database, phase: "live" },
  { label: "Execution Cost Lab", icon: Calculator, phase: "live" },
  { label: "Backup & Recovery", icon: ArchiveRestore, phase: "live" },
  { label: "Sectors", icon: Layers3, phase: "planned" },
  { label: "Backtesting", icon: BarChart3, phase: "live" },
  { label: "Paper Trading", icon: WalletCards, phase: "live" },
  { label: "Alerts", icon: BellRing, phase: "live" },
  { label: "Settings", icon: Settings2, phase: "live" },
];

const number = new Intl.NumberFormat("en-US", { maximumFractionDigits: 0 });
const decimal = new Intl.NumberFormat("en-US", { maximumFractionDigits: 2 });

function formatPrice(value: number | null) {
  if (value === null) return "Unavailable";
  return new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: value >= 100 ? 2 : value >= 1 ? 3 : 5 }).format(value);
}

function formatCompact(value: number | null) {
  if (value === null) return "Unavailable";
  return new Intl.NumberFormat("en-US", { notation: "compact", style: "currency", currency: "USD", maximumFractionDigits: 2 }).format(value);
}

function formatPct(value: number | null) {
  if (value === null) return "Unavailable";
  return `${value >= 0 ? "+" : ""}${decimal.format(value)}%`;
}

function timeAgo(timestamp: number | null | undefined) {
  if (!timestamp) return "Unavailable";
  const seconds = Math.max(0, Math.round((Date.now() - timestamp) / 1_000));
  if (seconds < 60) return `${seconds}s ago`;
  const minutes = Math.round(seconds / 60);
  if (minutes < 60) return `${minutes}m ago`;
  return `${Math.round(minutes / 60)}h ago`;
}

function scoreClass(value: number | null | undefined) {
  if (value === null || value === undefined) return "text-slate-500";
  if (value >= 72) return "text-emerald-300";
  if (value >= 55) return "text-cyan-300";
  if (value >= 40) return "text-amber-300";
  return "text-rose-300";
}

function changeClass(value: number | null | undefined) {
  if (value === null || value === undefined) return "text-slate-500";
  return value >= 0 ? "text-emerald-300" : "text-rose-300";
}

function StatusDot({ status }: { status: "live" | "stale" | "unavailable" }) {
  const { connectionState } = usePwaStatus();
  const effectiveStatus = connectionState === "ONLINE" ? status : "stale";
  return <span className={cn("inline-flex h-1.5 w-1.5 rounded-full", effectiveStatus === "live" ? "bg-emerald-400 shadow-[0_0_10px_rgba(52,211,153,.8)]" : effectiveStatus === "stale" ? "bg-amber-400" : "bg-rose-400")} />;
}

function EmptyRow() {
  return <div className="grid min-h-48 place-items-center px-6 text-center text-sm text-slate-400"><div><Database className="mx-auto mb-3 h-5 w-5 text-slate-500" /><p className="font-medium text-slate-300">No live score is available</p><p className="mt-1 max-w-sm text-xs leading-5">The system will show “Unavailable” rather than construct an opportunity when the required source data is missing.</p></div></div>;
}

function OpportunityRow({ row, rank, onSelect, selected }: { row: ScannerRow; rank: number; onSelect: () => void; selected: boolean }) {
  const score = row.score;
  return <button onClick={onSelect} className={cn("grid w-full grid-cols-[36px_minmax(150px,1.4fr)_minmax(88px,.8fr)_minmax(86px,.7fr)_minmax(116px,.9fr)_minmax(106px,.8fr)_32px] items-center gap-3 border-b border-white/[.055] px-5 py-3.5 text-left transition-colors hover:bg-cyan-300/[.045] focus:outline-none focus-visible:bg-cyan-300/[.08]", selected && "bg-cyan-300/[.07] shadow-[inset_2px_0_0_#22d3ee]", !score && "opacity-70")}>
    <span className="font-mono text-xs text-slate-500">{String(rank).padStart(2, "0")}</span>
    <span className="min-w-0"><span className="flex items-center gap-2"><span className="grid h-7 w-7 place-items-center rounded-md bg-slate-800 text-[10px] font-bold text-cyan-200">{row.asset.symbol.slice(0, 2)}</span><span className="min-w-0"><span className="block truncate font-semibold text-slate-100">{row.asset.symbol}<span className="ml-1.5 hidden text-xs font-normal text-slate-500 lg:inline">{row.asset.name}</span></span><span className="block truncate text-[10px] uppercase tracking-[.16em] text-slate-500">{row.asset.sector}</span></span></span></span>
    <span><span className="font-mono text-sm text-slate-100">{formatPrice(row.asset.price)}</span><span className={cn("mt-1 block font-mono text-[11px]", changeClass(row.asset.change24h))}>{formatPct(row.asset.change24h)}</span></span>
    <span><span className={cn("font-mono text-2xl font-semibold tracking-tight", scoreClass(score?.score))}>{score?.score ?? "—"}</span><span className="ml-1 text-[10px] uppercase tracking-wider text-slate-500">/100</span></span>
    <span><span className={cn("inline-flex items-center rounded-full border px-2 py-1 text-[10px] font-semibold uppercase tracking-[.08em]", score?.direction === "bullish" ? "border-emerald-300/20 bg-emerald-300/[.07] text-emerald-200" : score?.direction === "bearish" ? "border-rose-300/20 bg-rose-300/[.07] text-rose-200" : "border-slate-500/25 bg-slate-500/[.07] text-slate-300")}>{score?.setupType ?? "Insufficient data"}</span></span>
    <span><span className={cn("font-mono text-sm", scoreClass(score?.confidence))}>{score?.confidence ?? "—"}</span><span className="ml-1 text-[10px] uppercase tracking-wider text-slate-500">conf.</span></span>
    <ChevronRight className="h-4 w-4 text-slate-600" />
  </button>;
}

function DetailPanel({ row, onClose, onPaperTrade }: { row: ScannerRow | null; onClose: () => void; onPaperTrade: (row: ScannerRow) => void }) {
  const { online, liveDataAvailable } = usePwaStatus();
  if (!row) return <aside className="hidden min-h-[620px] w-[390px] shrink-0 border-l border-white/[.07] bg-slate-950/45 2xl:block"><EmptyRow /></aside>;
  const score = row.score;
  return <aside className="hidden min-h-[620px] w-[390px] shrink-0 border-l border-white/[.07] bg-[#090f1b]/80 2xl:block">
    <div className="flex items-center justify-between border-b border-white/[.07] px-5 py-4"><div><div className="flex items-center gap-2"><span className="text-sm font-bold text-slate-100">{row.asset.symbol} Research Card</span><Badge variant="outline" className="border-cyan-300/20 bg-cyan-300/[.06] text-[9px] uppercase tracking-[.14em] text-cyan-200">{online ? "Live basis" : "Last known data"}</Badge></div><p className="mt-1 text-[11px] text-slate-500">{row.asset.name} · {row.asset.sector}</p></div><button onClick={onClose} className="rounded-md p-1.5 text-slate-500 hover:bg-white/5 hover:text-slate-200" aria-label="Close detail"><X className="h-4 w-4" /></button></div>
    {!score ? <EmptyRow /> : <div className="space-y-5 p-5">
      <div className="grid grid-cols-3 gap-2"><MetricSmall label="Opportunity" value={score.score} tone="cyan" /><MetricSmall label="Confidence" value={score.confidence} tone="emerald" /><MetricSmall label="Risk safety" value={score.riskScore} tone={score.riskScore >= 65 ? "emerald" : "amber"} /></div>
      <section className="rounded-xl border border-white/[.07] bg-white/[.025] p-3.5"><span className="text-[10px] font-semibold uppercase tracking-[.17em] text-cyan-200">Why ranked highly</span><p className="mt-2 text-sm leading-6 text-slate-300">{score.explanation}</p><button onClick={() => onPaperTrade(row)} disabled={!online} className="mt-3 flex items-center gap-2 rounded-md border border-emerald-300/20 bg-emerald-300/[.06] px-3 py-2 text-[11px] font-medium text-emerald-100 hover:bg-emerald-300/[.1] disabled:cursor-not-allowed disabled:opacity-45"><WalletCards className="h-3.5 w-3.5" />{online ? "Paper trade this live signal" : "Paper Trading requires connection"}</button></section>
      <section><div className="mb-2 flex items-center justify-between"><h3 className="text-[10px] font-semibold uppercase tracking-[.17em] text-slate-500">Score components</h3><span className="text-[10px] text-slate-600">Inspectable inputs</span></div><div className="space-y-2">{score.reasons.slice(0, 6).map(reason => <div key={reason.key} className="rounded-lg border border-white/[.06] bg-white/[.02] px-3 py-2.5"><div className="flex items-center justify-between gap-3"><span className="text-xs font-medium text-slate-200">{reason.label}</span><span className={cn("font-mono text-xs", reason.direction === "positive" ? "text-emerald-300" : reason.direction === "negative" ? "text-rose-300" : "text-slate-400")}>{decimal.format(reason.score)}/{reason.maxScore}</span></div><p className="mt-1 text-[11px] leading-4 text-slate-500">{reason.detail}</p></div>)}</div></section>
      <section><h3 className="mb-2 text-[10px] font-semibold uppercase tracking-[.17em] text-slate-500">Multi-timeframe contribution</h3><div className="grid grid-cols-2 gap-2">{score.technicalByTimeframe.map(analysis => <div key={analysis.timeframe} className="rounded-lg border border-white/[.06] bg-white/[.02] px-3 py-2.5"><div className="flex items-center justify-between"><span className="font-mono text-xs text-slate-300">{analysis.timeframe.toUpperCase()}</span><span className={cn("text-[10px] font-semibold uppercase", analysis.bias === "bullish" ? "text-emerald-300" : analysis.bias === "bearish" ? "text-rose-300" : "text-slate-400")}>{analysis.bias}</span></div><div className="mt-2 flex items-end justify-between"><span className="font-mono text-lg text-slate-100">{analysis.score}<span className="text-[10px] text-slate-500">/10</span></span><span className="text-[10px] text-slate-500">RSI {analysis.rsi ?? "—"}</span></div></div>)}</div></section>
      {score.missingConditions.length > 0 && <section className="rounded-xl border border-amber-300/10 bg-amber-300/[.035] p-3.5"><span className="text-[10px] font-semibold uppercase tracking-[.17em] text-amber-200">What is missing</span><ul className="mt-2 space-y-1.5">{score.missingConditions.map(item => <li key={item} className="flex gap-2 text-xs leading-5 text-amber-100/75"><AlertTriangle className="mt-0.5 h-3.5 w-3.5 shrink-0 text-amber-300/75" />{item}</li>)}</ul></section>}
    </div>}
  </aside>;
}

function MetricSmall({ label, value, tone }: { label: string; value: number | null; tone: "cyan" | "emerald" | "amber" }) {
  const tones = { cyan: "text-cyan-200", emerald: "text-emerald-200", amber: "text-amber-200" };
  return <div className="rounded-lg border border-white/[.06] bg-white/[.02] px-3 py-2.5"><span className="block text-[9px] font-semibold uppercase tracking-[.14em] text-slate-500">{label}</span><span className={cn("mt-1 block font-mono text-xl font-semibold", tones[tone])}>{value ?? "—"}<span className="text-[10px] text-slate-500">/100</span></span></div>;
}

function RegimeBanner({ scan }: { scan: ScannerResponse | undefined }) {
  const regime = scan?.marketRegime;
  const tone = regime?.classification === "RISK ON" ? "emerald" : regime?.classification === "RISK OFF" ? "rose" : "amber";
  const styles = { emerald: "border-emerald-300/20 bg-emerald-300/[.06] text-emerald-100", amber: "border-amber-300/20 bg-amber-300/[.06] text-amber-100", rose: "border-rose-300/20 bg-rose-300/[.06] text-rose-100" };
  const providerHealth = useMemo(() => {
    const grouped = new Map<string, { provider: string; status: "live" | "stale" | "unavailable"; lastSuccess: number | null; lastError: string | null; errorClass: string | null; affected: Set<string> }>();
    for (const status of scan?.rows.flatMap(row => row.dataStatus).filter(status => status.capability === "OHLCV") ?? []) {
      const provider = status.provider ?? status.source;
      const item = grouped.get(provider) ?? { provider, status: "live" as const, lastSuccess: null, lastError: null, errorClass: null, affected: new Set<string>() };
      item.affected.add(status.timeframe ?? "OHLCV");
      if (status.status === "live") item.lastSuccess = Math.max(item.lastSuccess ?? 0, status.fetchedAt);
      if (status.status === "unavailable") { item.status = "unavailable"; item.lastError = status.message ?? "Provider unavailable."; item.errorClass = status.errorClass ?? "PROVIDER_REQUEST_FAILED"; }
      grouped.set(provider, item);
    }
    return Array.from(grouped.values());
  }, [scan]);
  const { data: monitorSummary } = trpc.crypto.providerMonitorSummary.useQuery(undefined, { staleTime: 60_000, refetchOnWindowFocus: false });
  const monitorChecks = monitorSummary?.checks ?? [];
  const primaryMonitor = monitorChecks.find(check => check.provider === "Binance Futures" && !check.fallbackUsed) ?? null;
  const fallbackUsedCheck = monitorChecks.find(check => check.provider === "Kraken Spot" && check.fallbackUsed && check.status === "live") ?? null;
  const lastFailure = monitorChecks.find(check => check.status === "unavailable" && (check.details as Record<string, unknown>)?.expectedUnavailable !== true) ?? null;
  return <div className={cn("relative overflow-hidden rounded-xl border px-4 py-3", styles[tone])}>
    <div className="absolute inset-y-0 left-0 w-1 bg-current opacity-70" />
    <div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-lg bg-black/15"><Activity className="h-4 w-4" /></div><div><div className="flex items-center gap-2"><span className="text-[10px] font-semibold uppercase tracking-[.18em] opacity-65">Market regime</span><span className="h-1 w-1 rounded-full bg-current opacity-80" /><span className="font-mono text-sm font-bold">{regime?.classification ?? "UNAVAILABLE"}</span></div><p className="mt-0.5 text-xs opacity-75">{regime ? `Regime score ${regime.score}/100 · BTC dominance ${regime.btcDominance === null ? "Unavailable" : `${decimal.format(regime.btcDominance)}%`} · Breadth ${regime.breadth === null ? "Unavailable" : `${decimal.format(regime.breadth)}%`}` : "The source data needed to classify the market environment is unavailable."}</p></div></div>
    {providerHealth.length ? <div className="mt-3 grid gap-2 border-t border-current/10 pt-3 sm:grid-cols-2"><span className="col-span-full text-[9px] font-semibold uppercase tracking-[.14em] opacity-65">Live OHLCV provider health</span>{providerHealth.map(item => <div key={item.provider} className="rounded-md bg-black/10 px-2.5 py-2 text-[10px]"><div className="flex items-center justify-between gap-2"><span className="font-medium">SOURCE: {item.provider}</span><span className={item.status === "live" ? "text-emerald-200" : "text-rose-200"}>DATA QUALITY: {item.status === "live" ? "VALID" : "UNAVAILABLE"}</span></div><p className="mt-1 opacity-75">OHLCV · {Array.from(item.affected).join(", ")} · {item.lastSuccess ? `last success ${timeAgo(item.lastSuccess)}` : `last error ${item.errorClass ?? "UNAVAILABLE"}`}</p>{item.lastError ? <p className="mt-1 truncate text-rose-100/80">{item.lastError}</p> : null}</div>)}</div> : null}
    <div className="mt-3 rounded-md border border-current/10 bg-black/10 px-2.5 py-2 text-[10px]"><div className="flex items-center justify-between"><span className="font-medium">Scheduled Provider Monitor</span><span>{monitorSummary?.monitor?.lastStatus ?? "PENDING"}</span></div><p className="mt-1 opacity-75">Primary Provider: {primaryMonitor?.provider ?? "Pending"} · Fallback Provider: Kraken Spot · Fallback Usage: {fallbackUsedCheck ? "USED IN CONTROLLED 451 CHECK" : "AVAILABLE / NOT USED"}</p><p className="mt-1 opacity-75">Last Successful Request: {primaryMonitor?.status === "live" ? timeAgo(primaryMonitor.createdAt.getTime()) : "Unavailable"} · Last Failure: {lastFailure?.classification ?? "None recorded"} · Last Monitoring Run: {monitorSummary?.execution?.completedAt ? timeAgo(monitorSummary.execution.completedAt.getTime()) : "Pending"}</p></div>
  </div>;
}

export default function Home() {
  const { online, connectionState, liveDataAvailable, updateReady, markReadSnapshot, markLiveDataUnavailable } = usePwaStatus();
  const [refreshNonce, setRefreshNonce] = useState(0);
  const [activeNav, setActiveNav] = useState("Dashboard");
  const [selectedId, setSelectedId] = useState<string | null>(null);
  const [settingsOpen, setSettingsOpen] = useState(false);
  const [paperOpen, setPaperOpen] = useState(() => typeof window !== "undefined" && new URLSearchParams(window.location.search).get("workspace") === "paper-trading");
  const [paperAsset, setPaperAsset] = useState<ScannerRow | null>(null);
  const [paperSetupMode, setPaperSetupMode] = useState<"SCALP" | "SWING" | "LOW_TIMEFRAME_SCALPING" | undefined>();
  const [setupMode, setSetupMode] = useState<"SCALP" | "SWING" | null>(null);
  const [discoveryOpen, setDiscoveryOpen] = useState(() => typeof window !== "undefined" && new URLSearchParams(window.location.search).get("workspace") === "opportunity-discovery");
  const [setupMonitorOpen, setSetupMonitorOpen] = useState(() => typeof window !== "undefined" && new URLSearchParams(window.location.search).get("workspace") === "setup-monitor");
  const [backtestingOpen, setBacktestingOpen] = useState(false);
  const [alertsOpen, setAlertsOpen] = useState(false);
  const [researchSummaryOpen, setResearchSummaryOpen] = useState(false);
  const [researchLabOpen, setResearchLabOpen] = useState(false);
  const [historicalDataOpen, setHistoricalDataOpen] = useState(false);
  const [executionCostLabOpen, setExecutionCostLabOpen] = useState(false);
  const [backupRecoveryOpen, setBackupRecoveryOpen] = useState(false);
  const [assetIntelligenceOpen, setAssetIntelligenceOpen] = useState(false);
  const [minScore, setMinScore] = useState(0);
  const [sector, setSector] = useState("All sectors");
  const { data: scan, isLoading, isFetching, error } = trpc.crypto.scanner.useQuery({ forceRefresh: refreshNonce > 0 }, { refetchOnWindowFocus: false, staleTime: 45_000, retry: 1 });
  const rows = scan?.rows ?? [];
  const sectors = useMemo(() => ["All sectors", ...Array.from(new Set(rows.map(row => row.asset.sector))).sort()], [rows]);
  const filteredRows = useMemo(() => rows.filter(row => (row.score?.score ?? -1) >= minScore && (sector === "All sectors" || row.asset.sector === sector)), [rows, minScore, sector]);
  const selected = rows.find(row => row.asset.id === selectedId) ?? filteredRows[0] ?? null;
  const topRows = rows.filter(row => row.score).slice(0, 3);
  const sectorRotation = useMemo(() => Object.entries(rows.filter(row => row.score).reduce<Record<string, number[]>>((map, row) => { (map[row.asset.sector] ??= []).push(row.score!.score); return map; }, {})).map(([name, values]) => ({ name, score: values.reduce((sum, value) => sum + value, 0) / values.length })).sort((a, b) => b.score - a.score).slice(0, 4), [rows]);
  useEffect(() => { if (online) markReadSnapshot(scan?.generatedAt, Boolean(scan?.dataStatus.some(status => status.status === "live"))); }, [online, scan?.generatedAt, scan?.dataStatus, markReadSnapshot]);
  useEffect(() => { if (online && error) markLiveDataUnavailable(); }, [online, error, markLiveDataUnavailable]);
  useEffect(() => { if (selectedId) setAssetIntelligenceOpen(true); }, [selectedId]);
  const scrollToScanner = () => { requestAnimationFrame(() => document.getElementById("market-scanner")?.scrollIntoView({ behavior: "smooth", block: "start" })); };
  const openPaperWorkspace = (asset: ScannerRow | null, mode?: "SCALP" | "SWING" | "LOW_TIMEFRAME_SCALPING") => { setPaperAsset(asset); setPaperSetupMode(mode); setSetupMode(null); setDiscoveryOpen(false); setActiveNav("Paper Trading"); setPaperOpen(true); if (typeof window !== "undefined") window.history.replaceState(null, "", `${window.location.pathname}?workspace=paper-trading`); };
  const closePaperWorkspace = () => { setPaperOpen(false); setPaperSetupMode(undefined); setActiveNav("Dashboard"); if (typeof window !== "undefined") window.history.replaceState(null, "", window.location.pathname); };
  const openSetupWorkspace = (mode: "SCALP" | "SWING") => { setPaperOpen(false); setDiscoveryOpen(false); setSetupMode(mode); setActiveNav(mode === "SCALP" ? "Scalping" : "Swing"); };
  const openDiscoveryWorkspace = () => { setPaperOpen(false); setSetupMode(null); setSetupMonitorOpen(false); setDiscoveryOpen(true); setActiveNav("Opportunity Discovery"); if (typeof window !== "undefined") window.history.replaceState(null, "", `${window.location.pathname}?workspace=opportunity-discovery`); };
  const openSetupMonitorWorkspace = () => { setPaperOpen(false); setSetupMode(null); setDiscoveryOpen(false); setSetupMonitorOpen(true); setActiveNav("Setup Monitor"); if (typeof window !== "undefined") window.history.replaceState(null, "", `${window.location.pathname}?workspace=setup-monitor`); };
  const handleNav = (item: NavItem) => { if (item.label === "Settings") { setSettingsOpen(true); return; } if (item.label === "Setup Monitor") { openSetupMonitorWorkspace(); return; } if (item.label === "Paper Trading") { openPaperWorkspace(selected); return; } if (item.label === "Scalping") { openSetupWorkspace("SCALP"); return; } if (item.label === "Swing") { openSetupWorkspace("SWING"); return; } if (item.label === "Opportunity Discovery") { openDiscoveryWorkspace(); return; } if (item.label === "Market Scanner") { setActiveNav(item.label); scrollToScanner(); return; } if (item.label === "Backtesting") { setBacktestingOpen(true); return; } if (item.label === "Alerts") { setAlertsOpen(true); return; } if (item.label === "Research Summary") { setResearchSummaryOpen(true); return; } if (item.label === "Research Lab") { setResearchLabOpen(true); return; } if (item.label === "Historical Data") { setHistoricalDataOpen(true); return; } if (item.label === "Execution Cost Lab") { setExecutionCostLabOpen(true); return; } if (item.label === "Backup & Recovery") { setBackupRecoveryOpen(true); return; } if (item.phase === "planned") { toast.message(`${item.label} is planned after the verified Phase 1 scanner milestone.`, { description: "No placeholder data has been added for this feature." }); return; } setActiveNav(item.label); };
  useEffect(() => {
    const onMobileNavigate = (event: Event) => {
      const target = (event as CustomEvent<"dashboard" | "scanner" | "discovery" | "setup-monitor" | "scalping" | "swing" | "paper" | "alerts" | "research">).detail;
      if (target === "paper") openPaperWorkspace(selected);
      else if (target === "setup-monitor") openSetupMonitorWorkspace();
      else if (target === "scalping") openSetupWorkspace("SCALP");
      else if (target === "swing") openSetupWorkspace("SWING");
      else if (target === "discovery") openDiscoveryWorkspace();
      else {
        closePaperWorkspace();
        if (target === "alerts") { setActiveNav("Alerts"); setAlertsOpen(true); }
        else if (target === "research") { setActiveNav("Research Lab"); setResearchLabOpen(true); }
        else { setActiveNav(target === "scanner" ? "Market Scanner" : "Dashboard"); if (target === "scanner") scrollToScanner(); }
      }
    };
    window.addEventListener("crypto-hub:navigate", onMobileNavigate);
    return () => window.removeEventListener("crypto-hub:navigate", onMobileNavigate);
  }, [selected]);
  const refresh = () => { if (!online) { toast.message("Offline read-only mode", { description: "Live data is unavailable until Crypto Hub reconnects." }); return; } setRefreshNonce(value => value + 1); toast.message("Refreshing public market sources", { description: "The scanner will display unavailable states if a source does not respond." }); };
  const pwaBannerVisible = connectionState !== "ONLINE" || updateReady;
  return <div className={cn("min-h-screen bg-[#060a12] text-slate-100 selection:bg-cyan-300/30", pwaBannerVisible && "pt-16")}>
    <AssetIntelligencePanel row={selected} open={assetIntelligenceOpen} onOpenChange={setAssetIntelligenceOpen} onPaperTrade={(row, mode) => { setAssetIntelligenceOpen(false); openPaperWorkspace(row, mode); }} />
    <div className="terminal-grid fixed inset-0 pointer-events-none opacity-40" />
    <div className="relative mx-auto flex min-h-screen max-w-[1800px]">
      <aside className="hidden w-[234px] shrink-0 border-r border-white/[.07] bg-[#070c16]/80 px-3 py-5 lg:flex lg:flex-col"><div className="flex items-center gap-3 px-2 pb-7"><div className="relative grid h-8 w-8 place-items-center rounded-lg bg-cyan-300 text-slate-950 shadow-[0_0_24px_rgba(34,211,238,.32)]"><TrendingUp className="h-4 w-4 stroke-[2.7]" /><span className={cn("absolute -right-1 -top-1 h-2.5 w-2.5 rounded-full border-2 border-[#070c16]", connectionState === "ONLINE" ? "bg-emerald-400" : "bg-amber-400")} /></div><div><h1 className="text-sm font-bold tracking-tight">Crypto Opportunity</h1><p className="mt-0.5 text-[9px] font-semibold uppercase tracking-[.21em] text-cyan-300">Research terminal</p></div></div><nav className="space-y-1">{navigation.map(item => { const Icon = item.icon; const active = activeNav === item.label; return <button key={item.label} onClick={() => handleNav(item)} className={cn("flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-left text-xs transition-colors", active ? "bg-cyan-300/[.08] text-cyan-100 shadow-[inset_2px_0_0_#22d3ee]" : "text-slate-400 hover:bg-white/[.035] hover:text-slate-200")}><Icon className={cn("h-4 w-4", active ? "text-cyan-300" : "text-slate-500")} /><span className="flex-1">{item.label}</span>{item.phase === "planned" && <span className="text-[8px] uppercase tracking-wide text-slate-600">Soon</span>}</button> })}</nav><div className="mt-auto rounded-xl border border-white/[.07] bg-white/[.025] p-3"><div className="flex items-center justify-between"><span className="text-[9px] font-semibold uppercase tracking-[.15em] text-slate-500">System status</span><span className={cn("flex items-center gap-1.5 text-[10px]", liveDataAvailable ? "text-emerald-300" : "text-amber-200")}><StatusDot status={liveDataAvailable ? "live" : "stale"} />{connectionState}</span></div><p className="mt-2 text-[11px] leading-4 text-slate-500">Live inputs are timestamped. Scores are explainable research signals, not trade instructions.</p></div></aside>
      <div className="min-w-0 flex-1"><main className="min-w-0"><header className="flex h-[70px] items-center justify-between border-b border-white/[.07] bg-[#080d17]/65 px-4 backdrop-blur-xl sm:px-6"><div className="flex items-center gap-3"><button className="rounded-md p-2 text-slate-400 hover:bg-white/[.05] lg:hidden"><PanelLeft className="h-4 w-4" /></button><div><p className="text-[10px] font-semibold uppercase tracking-[.18em] text-slate-500">{activeNav} <span className="text-slate-700">/</span> Live scanner</p><div className="mt-1 flex items-center gap-2"><h2 className="text-base font-semibold tracking-tight text-slate-100">Opportunity intelligence</h2><span className="hidden items-center gap-1 text-[10px] text-slate-500 sm:flex"><Clock3 className="h-3 w-3" />{scan ? `Updated ${timeAgo(scan.generatedAt)}` : "Initializing sources"}</span><OnlineStatusLabel /></div></div></div><div className="flex items-center gap-2"><Button variant="outline" size="sm" onClick={refresh} disabled={isFetching || !online} className="h-8 border-white/[.09] bg-white/[.025] px-3 text-xs text-slate-300 hover:bg-white/[.07] hover:text-white"><RefreshCw className={cn("mr-1.5 h-3.5 w-3.5", isFetching && "animate-spin")} />Refresh</Button><button onClick={() => toast.message("Search will be introduced with the extended scanner universe.")} className="hidden rounded-md border border-white/[.09] bg-white/[.025] p-2 text-slate-400 hover:bg-white/[.07] sm:block"><ScanSearch className="h-3.5 w-3.5" /></button></div></header>
        <div className="p-4 sm:p-6"><div className="mb-5 grid gap-4 xl:grid-cols-[1.45fr_.9fr]"><RegimeBanner scan={scan} /><div className="flex items-center justify-between rounded-xl border border-white/[.07] bg-white/[.025] px-4 py-3"><div><p className="text-[10px] font-semibold uppercase tracking-[.16em] text-slate-500">Data quality</p><p className="mt-1 text-xs text-slate-300">{scan?.dataStatus.filter(status => status.status === "live").length ?? 0} live provider feeds · {scan?.dataStatus.filter(status => status.status !== "live").length ?? 0} unavailable</p></div><ShieldCheck className="h-5 w-5 text-cyan-300" /></div></div>
          {error ? <div className="mb-5 rounded-xl border border-rose-300/20 bg-rose-300/[.05] p-4 text-sm text-rose-100"><div className="flex gap-2"><AlertTriangle className="h-4 w-4 shrink-0" /><div><p className="font-semibold">Live scanner unavailable</p><p className="mt-1 text-xs text-rose-100/70">{error.message}. No market or score values have been generated.</p></div></div></div> : null}
          <section className="mb-5 flex flex-wrap items-center justify-between gap-4 rounded-xl border border-cyan-300/15 bg-cyan-300/[.03] p-4"><div><p className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Opportunity Discovery</p><p className="mt-1 max-w-2xl text-xs leading-5 text-slate-400">Inspect validated Swing evidence as Qualified, Potential, Watch, No Trade, or Data Unavailable without changing Opportunity Score or creating an automatic trade.</p></div><Button size="sm" variant="outline" onClick={openDiscoveryWorkspace} className="border-cyan-300/20 bg-cyan-300/[.05] text-cyan-100"><Sparkles className="mr-1.5 h-3.5 w-3.5" />Open Discovery</Button></section>
          <section className="mb-5"><div className="mb-3 flex items-end justify-between"><div><span className="text-[10px] font-semibold uppercase tracking-[.18em] text-cyan-300">Live overview</span><h3 className="mt-1 text-lg font-semibold tracking-tight">Highest current research signals</h3></div><span className="hidden text-[11px] text-slate-500 md:block">Ranked from active configuration, timestamped market inputs</span></div><div className="grid gap-3 md:grid-cols-3">{isLoading ? [0, 1, 2].map(key => <Skeleton key={key} className="h-[146px] bg-white/[.04]" />) : topRows.length ? topRows.map((row, index) => <button key={row.asset.id} onClick={() => setSelectedId(row.asset.id)} className={cn("group rounded-xl border p-4 text-left transition-all hover:-translate-y-0.5", index === 0 ? "border-cyan-300/25 bg-cyan-300/[.055] shadow-[0_10px_40px_rgba(8,145,178,.09)]" : "border-white/[.07] bg-white/[.025] hover:border-white/[.14]")}><div className="flex items-start justify-between"><span className="flex items-center gap-2"><span className="grid h-8 w-8 place-items-center rounded-lg bg-slate-800 font-mono text-xs font-bold text-cyan-200">{row.asset.symbol.slice(0, 2)}</span><span><span className="block text-sm font-semibold">{row.asset.symbol}</span><span className="block text-[10px] uppercase tracking-[.15em] text-slate-500">Rank {String(index + 1).padStart(2, "0")}</span></span></span><span className={cn("font-mono text-3xl font-semibold", scoreClass(row.score?.score))}>{row.score?.score ?? "—"}</span></div><div className="mt-4 flex items-end justify-between"><div><span className="font-mono text-sm text-slate-200">{formatPrice(row.asset.price)}</span><span className={cn("mt-1 block text-[11px]", changeClass(row.asset.change24h))}>{formatPct(row.asset.change24h)} <span className="text-slate-600">24H</span></span></div><span className="rounded-md border border-white/[.07] bg-black/15 px-2 py-1 text-[10px] text-slate-400">{row.score?.setupType}</span></div></button>) : <div className="col-span-3 rounded-xl border border-white/[.07] bg-white/[.025]"><EmptyRow /></div>}</div></section>
          <div className="grid gap-5 2xl:grid-cols-[minmax(0,1fr)_290px]"><section id="market-scanner" className="scroll-mt-24 overflow-hidden rounded-xl border border-white/[.07] bg-[#090f1b]/70"><div className="flex flex-col gap-3 border-b border-white/[.07] p-4 xl:flex-row xl:items-center xl:justify-between"><div><span className="text-[10px] font-semibold uppercase tracking-[.17em] text-cyan-300">Scanner</span><h3 className="mt-0.5 text-base font-semibold">Ranked opportunity universe</h3></div><div className="flex flex-wrap items-center gap-2"><label className="flex h-8 items-center gap-2 rounded-md border border-white/[.08] bg-white/[.025] px-2.5 text-[11px] text-slate-400">Min score <select value={minScore} onChange={event => setMinScore(Number(event.target.value))} className="bg-transparent text-slate-200 outline-none"><option value={0}>All</option><option value={50}>50+</option><option value={65}>65+</option><option value={75}>75+</option></select><ChevronDown className="h-3 w-3" /></label><label className="flex h-8 items-center gap-2 rounded-md border border-white/[.08] bg-white/[.025] px-2.5 text-[11px] text-slate-400">Sector <select value={sector} onChange={event => setSector(event.target.value)} className="max-w-24 bg-transparent text-slate-200 outline-none"><option>All sectors</option>{sectors.slice(1).map(item => <option key={item}>{item}</option>)}</select><ChevronDown className="h-3 w-3" /></label><Button variant="ghost" size="sm" onClick={() => toast.message("All active scanner inputs are currently visible in the detail card.")} className="h-8 px-2.5 text-xs text-cyan-200 hover:bg-cyan-300/[.08] hover:text-cyan-100"><SlidersHorizontal className="mr-1.5 h-3.5 w-3.5" />Filters</Button></div></div><div className="scanner-head grid grid-cols-[36px_minmax(150px,1.4fr)_minmax(88px,.8fr)_minmax(86px,.7fr)_minmax(116px,.9fr)_minmax(106px,.8fr)_32px] gap-3 border-b border-white/[.055] px-5 py-2 text-[9px] font-semibold uppercase tracking-[.14em] text-slate-600"><span>#</span><span>Asset / sector</span><span>Price / 24h</span><span>Score</span><span>Setup</span><span>Confidence</span><span /></div>{isLoading ? <div className="space-y-0 p-5">{[0, 1, 2, 3, 4].map(key => <Skeleton key={key} className="mb-3 h-12 bg-white/[.035]" />)}</div> : filteredRows.length ? filteredRows.map((row, index) => <OpportunityRow key={row.asset.id} row={row} rank={index + 1} selected={selected?.asset.id === row.asset.id} onSelect={() => setSelectedId(row.asset.id)} />) : <EmptyRow />}<div className="flex items-center justify-between bg-white/[.015] px-5 py-3 text-[10px] text-slate-500"><span>{filteredRows.length} of {rows.length} tracked assets shown</span><span>CoinGecko + Binance public data</span></div></section>
            <section className="space-y-5"><div className="rounded-xl border border-white/[.07] bg-[#090f1b]/70 p-4"><div className="flex items-center justify-between"><div><span className="text-[10px] font-semibold uppercase tracking-[.17em] text-cyan-300">Rotation</span><h3 className="mt-0.5 text-sm font-semibold">Sector relative strength</h3></div><Layers3 className="h-4 w-4 text-slate-500" /></div><div className="mt-4 space-y-3">{sectorRotation.length ? sectorRotation.map((item, index) => <div key={item.name}><div className="mb-1.5 flex justify-between text-xs"><span className="text-slate-300">{item.name}</span><span className={scoreClass(item.score)}>{decimal.format(item.score)}</span></div><Progress value={item.score} className="h-1.5 bg-slate-800 [&>div]:bg-cyan-300" /></div>) : <p className="text-xs text-slate-500">Unavailable until live asset scores are calculated.</p>}</div></div><div className="rounded-xl border border-white/[.07] bg-[#090f1b]/70 p-4"><div className="flex items-center justify-between"><div><span className="text-[10px] font-semibold uppercase tracking-[.17em] text-cyan-300">Configuration</span><h3 className="mt-0.5 text-sm font-semibold">Scoring controls</h3></div><Settings2 className="h-4 w-4 text-slate-500" /></div><div className="mt-4 space-y-3 text-xs"><ControlRow label="Technical weight" value="40%" /><ControlRow label="Market momentum" value="20%" /><ControlRow label="Liquidity / risk" value="15%" /><ControlRow label="Enabled timeframes" value="4" /></div><button onClick={() => toast.message("Editable scoring settings are part of the next authenticated milestone.", { description: "The current default configuration is documented and persisted with every scan." })} className="mt-4 flex w-full items-center justify-center gap-2 rounded-md border border-cyan-300/20 bg-cyan-300/[.06] py-2 text-[11px] font-medium text-cyan-100 transition-colors hover:bg-cyan-300/[.1]"><Settings2 className="h-3.5 w-3.5" />View configuration</button></div><div className="rounded-xl border border-white/[.07] bg-white/[.018] p-4"><div className="flex gap-3"><BookOpen className="mt-0.5 h-4 w-4 shrink-0 text-slate-500" /><p className="text-[11px] leading-5 text-slate-500">The scanner reports inputs, data freshness, scoring components, and missing conditions. It does not claim a score predicts price movement.</p></div></div></section></div>
        </div>
      </main></div><DetailPanel row={selected} onClose={() => setSelectedId(null)} onPaperTrade={openPaperWorkspace} /><SettingsPanel open={settingsOpen} onOpenChange={setSettingsOpen} />{setupMonitorOpen ? <section className="fixed inset-0 z-[69] overflow-y-auto bg-[#060a12] text-slate-100"><div className="terminal-grid fixed inset-0 pointer-events-none opacity-35" /><div className="relative mx-auto min-h-screen max-w-[1800px] px-4 py-5 sm:px-6 lg:px-10"><SetupMonitorWorkspace /></div></section> : null}{discoveryOpen ? <section className="fixed inset-0 z-[69] overflow-y-auto bg-[#060a12] text-slate-100"><div className="terminal-grid fixed inset-0 pointer-events-none opacity-35" /><div className="relative mx-auto min-h-screen max-w-[1800px] px-4 py-5 sm:px-6 lg:px-10"><OpportunityDiscoveryWorkspace onBack={() => { setDiscoveryOpen(false); setActiveNav("Dashboard"); if (typeof window !== "undefined") window.history.replaceState(null, "", window.location.pathname); }} onInspectAsset={assetId => { setSelectedId(assetId); setAssetIntelligenceOpen(true); }} onPaperTrade={assetId => openPaperWorkspace(rows.find(row => row.asset.id === assetId) ?? null, "SWING")} /></div></section> : null}{setupMode ? <section className="fixed inset-0 z-[69] overflow-y-auto bg-[#060a12] text-slate-100"><div className="terminal-grid fixed inset-0 pointer-events-none opacity-35" /><div className="relative mx-auto min-h-screen max-w-[1800px] px-4 py-5 sm:px-6 lg:px-10"><TradeSetupWorkspace mode={setupMode} onBack={() => { setSetupMode(null); setActiveNav("Dashboard"); }} onInspectAsset={assetId => { setSelectedId(assetId); setAssetIntelligenceOpen(true); }} onPaperTrade={(assetId, mode) => openPaperWorkspace(rows.find(row => row.asset.id === assetId) ?? null, mode)} /></div></section> : null}{paperOpen ? <section className="fixed inset-0 z-[70] overflow-y-auto bg-[#060a12] text-slate-100"><div className="terminal-grid fixed inset-0 pointer-events-none opacity-35" /><div className="relative mx-auto min-h-screen max-w-[1800px] px-4 py-5 sm:px-6 lg:px-10"><PaperTradingWorkspace asset={paperAsset} setupMode={paperSetupMode} onBack={closePaperWorkspace} /></div></section> : null}<BacktestingPanel open={backtestingOpen} onOpenChange={setBacktestingOpen} selectedAsset={selected} /><AlertsPanel open={alertsOpen} onOpenChange={setAlertsOpen} onInspectAsset={assetId => { setSelectedId(assetId); setAlertsOpen(false); setAssetIntelligenceOpen(true); }} /><ResearchSummaryPanel open={researchSummaryOpen} onOpenChange={setResearchSummaryOpen} /><OpportunityResearchLab open={researchLabOpen} onOpenChange={setResearchLabOpen} /><HistoricalDataFoundation open={historicalDataOpen} onOpenChange={setHistoricalDataOpen} /><ExecutionCostLab open={executionCostLabOpen} onOpenChange={setExecutionCostLabOpen} /><BackupRecoveryPanel open={backupRecoveryOpen} onOpenChange={setBackupRecoveryOpen} />
    </div>
  </div>;
}

function ControlRow({ label, value }: { label: string; value: string }) { return <div className="flex items-center justify-between border-b border-white/[.055] pb-2.5 last:border-0 last:pb-0"><span className="text-slate-500">{label}</span><span className="font-mono text-slate-200">{value}</span></div>; }

function MobileNavigation({ activeNav, onDashboard, onPaper, onAlerts, onResearch }: { activeNav: string; onDashboard: () => void; onPaper: () => void; onAlerts: () => void; onResearch: () => void }) {
  const items = [
    { label: "Home", active: activeNav === "Dashboard", icon: Gauge, onClick: onDashboard },
    { label: "Scanner", active: activeNav === "Market Scanner", icon: ScanSearch, onClick: onDashboard },
    { label: "Paper", active: activeNav === "Paper Trading", icon: WalletCards, onClick: onPaper },
    { label: "Alerts", active: activeNav === "Alerts", icon: BellRing, onClick: onAlerts },
    { label: "Research", active: activeNav === "Research Lab", icon: FlaskConical, onClick: onResearch },
  ];
  return <nav aria-label="Mobile workspace navigation" className="pwa-mobile-nav fixed inset-x-0 bottom-0 z-[60] flex border-t border-white/[.08] bg-[#080d17]/95 px-1 pt-1 backdrop-blur-xl lg:hidden">{items.map(item => { const Icon = item.icon; return <button key={item.label} onClick={item.onClick} aria-current={item.active ? "page" : undefined} className={cn("flex min-w-0 flex-1 flex-col items-center gap-1 rounded-lg px-1 py-2 text-[10px] font-medium", item.active ? "text-cyan-200" : "text-slate-500")}><Icon className="h-4 w-4" /><span className="truncate">{item.label}</span></button>; })}</nav>;
}
== CRYPTO COMPONENT INVENTORY ==
AlertExecutionHistory.tsx
AlertsPanel.tsx
AssetIntelligencePanel.tsx
BacktestingPanel.tsx
BackupRecoveryPanel.tsx
ExecutionCostLab.tsx
HistoricalDataFoundation.tsx
LowTimeframeScalpingWorkspace.tsx
OpportunityDiscoveryWorkspace.tsx
OpportunityResearchLab.tsx
PaperTradingPanel.tsx
PaperTradingWorkspace.tsx
ResearchRunWorkspace.tsx
ResearchSummaryPanel.tsx
SettingsPanel.tsx
SetupMonitorWorkspace.tsx
TechnicalChart.tsx
TradeSetupWorkspace.tsx
paperTradingWorkspaceFilters.ts
== RESPONSIVE / DIALOG / TABLE SIGNALS ==
client/src/components/crypto/SettingsPanel.tsx:3:import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
client/src/components/crypto/SettingsPanel.tsx:31:  const { user, loading } = useAuth();
client/src/components/crypto/SettingsPanel.tsx:38:    onError: error => toast.error("Settings were not saved", { description: error.message }),
client/src/components/crypto/SettingsPanel.tsx:47:  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-w-4xl border-white/[.1] bg-[#0b1220] p-0 text-slate-100 sm:rounded-xl"><DialogHeader className="border-b border-white/[.07] px-6 py-5"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-lg bg-cyan-300/[.12] text-cyan-200"><SlidersHorizontal className="h-4 w-4" /></span><div><DialogTitle className="text-base">Research configuration</DialogTitle><DialogDescription className="mt-1 text-xs text-slate-500">Controls are stored per user and recorded with future score snapshots.</DialogDescription></div></div></DialogHeader>{loading ? <div className="grid h-72 place-items-center"><Loader2 className="h-5 w-5 animate-spin text-cyan-300" /></div> : !user ? <div className="grid min-h-72 place-items-center px-6 text-center"><div><h3 className="text-sm font-semibold">Sign in to manage the model</h3><p className="mx-auto mt-2 max-w-sm text-xs leading-5 text-slate-500">The public scanner uses the documented default configuration. Sign in to save a personal research model and apply it to your scans.</p><Button onClick={() => startLogin()} className="mt-5 bg-cyan-300 text-slate-950 hover:bg-cyan-200">Sign in to configure</Button></div></div> : settings.isLoading || !draft ? <div className="grid h-72 place-items-center"><Loader2 className="h-5 w-5 animate-spin text-cyan-300" /></div> : <><ScrollArea className="h-[min(70vh,650px)]"><div className="grid gap-6 p-6 lg:grid-cols-2"><section><SectionTitle title="Score weights" description="Weights are normalized across active inputs; sentiment remains neutral until a verified source is added." /> <div className="mt-3 space-y-2">{weightFields.map(field => <NumberField key={field.key} label={field.label} value={draft.weights[field.key]} suffix="%" onChange={value => update(config => { config.weights[field.key] = value; })} />)}</div><SectionTitle title="Thresholds" description="Thresholds guide qualifying-signal and alert logic; they do not guarantee an outcome." className="mt-6" /><div className="mt-3 grid gap-2 sm:grid-cols-3"><NumberField label="Opportunity" value={draft.thresholds.opportunity} onChange={value => update(config => { config.thresholds.opportunity = value; })} /><NumberField label="Confidence" value={draft.thresholds.confidence} onChange={value => update(config => { config.thresholds.confidence = value; })} /><NumberField label="Technical" value={draft.thresholds.technical} onChange={value => update(config => { config.thresholds.technical = value; })} /></div><SectionTitle title="Risk and capital" description="Used by the risk engine and the later paper-trading lab." className="mt-6" /><div className="mt-3 grid gap-2"><NumberField label="Maximum ATR" value={draft.risk.maxAtrPercent} suffix="%" onChange={value => update(config => { config.risk.maxAtrPercent = value; })} /><NumberField label="Minimum market cap" value={draft.risk.minimumMarketCap} suffix="USD" onChange={value => update(config => { config.risk.minimumMarketCap = value; })} /><NumberField label="Volume / market cap floor" value={draft.risk.minimumVolumeToMarketCap} onChange={value => update(config => { config.risk.minimumVolumeToMarketCap = value; })} /><NumberField label="Paper capital" value={draft.paperCapital} suffix="USD" onChange={value => update(config => { config.paperCapital = value; })} /></div></section><section><SectionTitle title="Indicator parameters" description="Technical calculations use only candles available at the signal timestamp." /><div className="mt-3 grid gap-2 sm:grid-cols-2">{indicatorFields.map(field => <NumberField key={field.key} label={field.label} value={draft.indicator[field.key]} onChange={value => update(config => { config.indicator[field.key] = value; })} />)}</div><SectionTitle title="Active timeframes" description="Each timeframe has one bounded contribution to prevent double counting." className="mt-6" /><div className="mt-3 space-y-2">{Object.entries(draft.timeframes).map(([timeframe, value]) => <div key={timeframe} className="flex items-center justify-between rounded-lg border border-white/[.07] bg-white/[.02] px-3 py-2.5"><div><span className="font-mono text-xs text-slate-200">{timeframe.toUpperCase()}</span><span className="ml-2 text-[10px] text-slate-500">Weight {Math.round(value.weight * 100)}%</span></div><Switch checked={value.enabled} onCheckedChange={enabled => update(config => { config.timeframes[timeframe as keyof typeof config.timeframes].enabled = enabled; })} /></div>)}</div><SectionTitle title="Sector hypotheses" description="These multipliers are research hypotheses to validate, not established facts." className="mt-6" /><div className="mt-3 space-y-2">{Object.entries(draft.sectorModels).map(([sector, model]) => <div key={sector} className="rounded-lg border border-white/[.07] bg-white/[.02] p-3"><div className="flex items-center justify-between"><span className="text-xs font-medium text-slate-200">{sector}</span><span className="text-[10px] text-slate-500">Tech ×{model.technicalMultiplier} · Risk ×{model.riskMultiplier}</span></div><div className="mt-2 grid grid-cols-2 gap-2"><NumberField label="Technical" value={model.technicalMultiplier} onChange={value => update(config => { config.sectorModels[sector].technicalMultiplier = value; })} /><NumberField label="Risk" value={model.riskMultiplier} onChange={value => update(config => { config.sectorModels[sector].riskMultiplier = value; })} /></div><p className="mt-2 text-[10px] leading-4 text-slate-500">{model.description}</p></div>)}</div></section></div></ScrollArea><div className="flex items-center justify-between border-t border-white/[.07] bg-black/10 px-6 py-4"><Button variant="ghost" onClick={reset} disabled={save.isPending} className="text-xs text-slate-400 hover:text-slate-100"><RotateCcw className="mr-2 h-3.5 w-3.5" />Discard changes</Button><Button onClick={submit} disabled={save.isPending} className="bg-cyan-300 text-xs text-slate-950 hover:bg-cyan-200">{save.isPending ? <Loader2 className="mr-2 h-3.5 w-3.5 animate-spin" /> : <Check className="mr-2 h-3.5 w-3.5" />}Save configuration</Button></div></>}</DialogContent></Dialog>;
client/src/components/crypto/PaperTradingPanel.tsx:3:import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
client/src/components/crypto/PaperTradingPanel.tsx:17:  const { user, loading } = useAuth();
client/src/components/crypto/PaperTradingPanel.tsx:28:    onError: error => toast.error("Paper trade was not recorded", { description: error.message }),
client/src/components/crypto/PaperTradingPanel.tsx:35:    onError: error => toast.error("Paper trade was not closed", { description: error.message }),
client/src/components/crypto/PaperTradingPanel.tsx:38:  return <><Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-w-4xl border-white/[.1] bg-[#0b1220] p-0 text-slate-100 sm:rounded-xl"><DialogHeader className="border-b border-white/[.07] px-6 py-5"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-lg bg-emerald-300/[.12] text-emerald-200"><WalletCards className="h-4 w-4" /></span><div><DialogTitle className="text-base">Paper trading lab</DialogTitle><DialogDescription className="mt-1 text-xs text-slate-500">Simulated positions retain the exact immutable score, signal reasons, indicator state, regime, provider, and timestamp present at entry.</DialogDescription></div></div></DialogHeader>{loading ? <Loading /> : !user ? <SignIn /> : <ScrollArea className="h-[min(72vh,650px)]"><div className="space-y-6 p-6"><LiveSignal asset={asset} side={side} setSide={setSide} riskPercent={riskPercent} setRiskPercent={setRiskPercent} onCreate={create} pending={openTrade.isPending} /><PortfolioView portfolio={portfolio.data} loading={portfolio.isLoading} onClose={tradeId => closeTrade.mutate({ tradeId })} closing={closeTrade.isPending} onSnapshot={value => setSnapshot(value as Snapshot)} /></div></ScrollArea>}</DialogContent></Dialog><SnapshotDialog snapshot={snapshot} onOpenChange={open => { if (!open) setSnapshot(null); }} /></>;
client/src/components/crypto/PaperTradingPanel.tsx:43:  return <section className="rounded-xl border border-emerald-300/15 bg-emerald-300/[.04] p-4"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><span className="text-[10px] font-semibold uppercase tracking-[.17em] text-emerald-200">Live signal selected</span><h3 className="mt-1 text-lg font-semibold">{asset.asset.symbol} · {asset.score.setupType}</h3><p className="mt-1 text-xs leading-5 text-slate-400">Opportunity {asset.score.score}/100 · Confidence {asset.score.confidence}/100 · Current price {asset.asset.price === null ? "Unavailable" : money.format(asset.asset.price)}</p></div><span className="flex items-center gap-1.5 rounded-full border border-emerald-300/20 px-2.5 py-1 text-[10px] text-emerald-200"><ShieldCheck className="h-3 w-3" />Immutable snapshot on entry</span></div><div className="mt-4 grid gap-2 sm:grid-cols-[1fr_1fr_125px]"><div className="flex rounded-lg border border-white/[.08] bg-black/10 p-1"><button onClick={() => setSide("long")} className={`flex-1 rounded-md py-2 text-xs font-medium ${side === "long" ? "bg-emerald-300/15 text-emerald-200" : "text-slate-500"}`}><ArrowUpRight className="mr-1 inline h-3.5 w-3.5" />Long</button><button onClick={() => setSide("short")} className={`flex-1 rounded-md py-2 text-xs font-medium ${side === "short" ? "bg-rose-300/15 text-rose-200" : "text-slate-500"}`}><ArrowDownRight className="mr-1 inline h-3.5 w-3.5" />Short</button></div><label className="flex items-center justify-between rounded-lg border border-white/[.08] bg-black/10 px-3 text-xs text-slate-400">Risk <input type="number" min="0.1" max="5" step="0.1" value={riskPercent} onChange={event => setRiskPercent(Number(event.target.value))} className="w-12 bg-transparent text-right font-mono text-emerald-100 outline-none" /><span>%</span></label><Button onClick={onCreate} disabled={pending || asset.asset.price === null} className="bg-emerald-300 text-xs text-slate-950 hover:bg-emerald-200">{pending ? <Loader2 className="mr-2 h-3.5 w-3.5 animate-spin" /> : <BadgeDollarSign className="mr-2 h-3.5 w-3.5" />}Record trade</Button></div></section>;
client/src/components/crypto/PaperTradingPanel.tsx:46:function PortfolioView({ portfolio, loading, onClose, closing, onSnapshot }: { portfolio: { metrics: { currentEquity: number; realizedPnl: number; unrealizedPnl: number; winRate: number | null; maxDrawdown: number; profitFactor: number | null; sharpeRatio: number | null; tradeCount: number }; trades: Array<{ id: number; side: string; assetId: string; status: string; entryPrice: number; realizedPnl: number | null; immutableEntrySnapshot: unknown }> } | undefined; loading: boolean; onClose: (tradeId: number) => void; closing: boolean; onSnapshot: (snapshot: unknown) => void }) {
client/src/components/crypto/PaperTradingPanel.tsx:47:  return <section><div className="mb-3"><span className="text-[10px] font-semibold uppercase tracking-[.17em] text-emerald-200">Portfolio</span><h3 className="mt-1 text-sm font-semibold">Current simulated performance</h3></div>{loading ? <Loading /> : portfolio ? <><div className="grid gap-2 sm:grid-cols-4"><Metric label="Equity" value={money.format(portfolio.metrics.currentEquity)} /><Metric label="Realized P/L" value={money.format(portfolio.metrics.realizedPnl)} /><Metric label="Unrealized P/L" value={money.format(portfolio.metrics.unrealizedPnl)} /><Metric label="Win rate" value={portfolio.metrics.winRate === null ? "—" : `${portfolio.metrics.winRate}%`} /></div><div className="mt-3 grid gap-2 sm:grid-cols-4"><Metric label="Max drawdown" value={`${portfolio.metrics.maxDrawdown}%`} /><Metric label="Profit factor" value={portfolio.metrics.profitFactor === null ? "—" : String(portfolio.metrics.profitFactor)} /><Metric label="Sharpe" value={portfolio.metrics.sharpeRatio === null ? "—" : String(portfolio.metrics.sharpeRatio)} /><Metric label="Trades" value={String(portfolio.metrics.tradeCount)} /></div><div className="mt-5 overflow-hidden rounded-xl border border-white/[.07]"><div className="grid grid-cols-[minmax(120px,1fr)_72px_80px_120px] gap-2 border-b border-white/[.07] bg-white/[.025] px-4 py-2 text-[9px] font-semibold uppercase tracking-[.14em] text-slate-500"><span>Position</span><span>Status</span><span>Entry</span><span /></div>{portfolio.trades.length ? portfolio.trades.map(trade => <div key={trade.id} className="grid grid-cols-[minmax(120px,1fr)_72px_80px_120px] items-center gap-2 border-b border-white/[.055] px-4 py-3 text-xs last:border-0"><span><span className="block font-medium text-slate-200">{trade.side.toUpperCase()} · {trade.assetId}</span><span className="mt-0.5 block text-[10px] text-slate-500">Immutable entry snapshot retained</span></span><span className={trade.status === "open" ? "text-cyan-200" : "text-slate-400"}>{trade.status}</span><span className="font-mono text-slate-300">{trade.entryPrice}</span><span className="flex items-center justify-end gap-1"><Button variant="ghost" size="sm" onClick={() => onSnapshot(trade.immutableEntrySnapshot)} className="h-7 px-2 text-[10px] text-cyan-200 hover:bg-cyan-300/[.08] hover:text-cyan-100">Why?<ChevronRight className="ml-1 h-3 w-3" /></Button>{trade.status === "open" ? <Button variant="ghost" size="sm" onClick={() => onClose(trade.id)} disabled={closing} className="h-7 px-2 text-[10px] text-rose-200 hover:bg-rose-300/[.08] hover:text-rose-100">Close</Button> : <span className="font-mono text-[10px] text-slate-400">{trade.realizedPnl === null ? "—" : money.format(trade.realizedPnl)}</span>}</span></div>) : <div className="p-6 text-center text-xs text-slate-500">No paper positions have been recorded.</div>}</div></> : null}</section>;
client/src/components/crypto/PaperTradingPanel.tsx:50:function SnapshotDialog({ snapshot, onOpenChange }: { snapshot: Snapshot | null; onOpenChange: (open: boolean) => void }) { const reasons = snapshot?.observation?.exactScoringComponents ?? snapshot?.opportunity?.reasons ?? []; return <Dialog open={Boolean(snapshot)} onOpenChange={onOpenChange}><DialogContent className="max-w-3xl border-white/[.1] bg-[#0b1220] text-slate-100"><DialogHeader><DialogTitle>Why this paper trade was opened</DialogTitle><DialogDescription>Immutable entry evidence retained at the time the simulated position was recorded; it is never reconstructed from current data.</DialogDescription></DialogHeader>{snapshot ? <div className="space-y-4"><div className="grid grid-cols-2 gap-2 sm:grid-cols-4"><Metric label="Score" value={snapshot.opportunity?.score === undefined ? "UNAVAILABLE" : `${snapshot.opportunity.score}/100`} /><Metric label="Confidence" value={snapshot.opportunity?.confidence === undefined ? "UNAVAILABLE" : `${snapshot.opportunity.confidence}/100`} /><Metric label="Setup" value={snapshot.opportunity?.setupType ?? "UNAVAILABLE"} /><Metric label="Regime" value={snapshot.marketRegime?.classification ?? "UNAVAILABLE"} /></div><div className="grid gap-2 sm:grid-cols-3"><Metric label="Entry" value={snapshot.observation?.entryPrice === undefined ? "UNAVAILABLE" : money.format(snapshot.observation.entryPrice)} /><Metric label="Stop" value={snapshot.observation?.stopLoss === undefined ? "UNAVAILABLE" : money.format(snapshot.observation.stopLoss)} /><Metric label="Target" value={snapshot.observation?.target === undefined ? "UNAVAILABLE" : money.format(snapshot.observation.target)} /></div><section className="rounded-xl border border-white/[.07] bg-white/[.02] p-4"><span className="text-[10px] font-semibold uppercase tracking-[.15em] text-emerald-200">Stored score evidence</span><div className="mt-3 space-y-2">{reasons.length ? reasons.map(reason => <div key={reason.label} className="rounded-lg border border-white/[.06] bg-black/10 px-3 py-2.5"><div className="flex justify-between gap-3"><span className="text-xs font-medium text-slate-200">{reason.label}</span><span className="font-mono text-xs text-emerald-200">{reason.score}/{reason.maxScore}</span></div><p className="mt-1 text-[11px] leading-4 text-slate-500">{reason.detail}</p></div>) : <p className="text-xs text-slate-500">UNAVAILABLE — no score evidence was stored on this legacy trade.</p>}</div></section>{snapshot.observation?.missingConditions?.length ? <section className="rounded-xl border border-amber-300/15 bg-amber-300/[.035] p-4"><span className="text-[10px] font-semibold uppercase tracking-[.15em] text-amber-200">Stored missing conditions</span><ul className="mt-2 space-y-1 text-xs text-amber-100/75">{snapshot.observation.missingConditions.map(item => <li key={item}>{item}</li>)}</ul></section> : null}</div> : null}</DialogContent></Dialog>; }
client/src/components/crypto/BacktestingPanel.tsx:3:import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
client/src/components/crypto/BacktestingPanel.tsx:29:  const { user, loading } = useAuth();
client/src/components/crypto/BacktestingPanel.tsx:48:  const run = trpc.crypto.runBacktest.useMutation({ onSuccess: data => { setResult(data); toast.success("Backtest completed", { description: `${data.metrics.signalCount} historical signals evaluated from ${data.candlesReturned} live-source candles.` }); }, onError: error => toast.error("Backtest could not run", { description: error.message }) });
client/src/components/crypto/BacktestingPanel.tsx:51:  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-w-4xl border-white/[.1] bg-[#0b1220] p-0 text-slate-100 sm:rounded-xl"><DialogHeader className="border-b border-white/[.07] px-6 py-5"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-lg bg-violet-300/[.12] text-violet-200"><BarChart3 className="h-4 w-4" /></span><div><DialogTitle className="text-base">Historical backtesting lab</DialogTitle><DialogDescription className="mt-1 text-xs text-slate-500">Signals use the selected historical timeframe only; stops, targets, and holding exits are evaluated in later bars.</DialogDescription></div></div></DialogHeader>{loading ? <div className="grid h-72 place-items-center"><Loader2 className="h-5 w-5 animate-spin text-violet-300" /></div> : !user ? <div className="grid min-h-72 place-items-center px-6 text-center"><div><h3 className="text-sm font-semibold">Sign in to record a backtest</h3><p className="mx-auto mt-2 max-w-sm text-xs leading-5 text-slate-500">Backtest configurations and results are saved under your account for a reproducible research trail.</p><Button onClick={() => startLogin()} className="mt-5 bg-violet-300 text-slate-950 hover:bg-violet-200">Sign in to backtest</Button></div></div> : <ScrollArea className="h-[min(72vh,680px)]"><div className="grid gap-6 p-6 lg:grid-cols-[.9fr_1.1fr]"><section><span className="text-[10px] font-semibold uppercase tracking-[.18em] text-violet-200">Experiment configuration</span><div className="mt-3 space-y-2"><SelectField label="Asset" value={assetId} onChange={setAssetId}>{DEFAULT_ASSET_UNIVERSE.map(asset => <option key={asset.id} value={asset.id}>{asset.symbol}</option>)}</SelectField><SelectField label="Timeframe" value={timeframe} onChange={value => setTimeframe(value as Timeframe)}><option value="15m">15M</option><option value="1h">1H</option><option value="4h">4H</option><option value="1d">1D</option></SelectField><NumberField label="Minimum opportunity" value={minimumScore} min={0} max={100} onChange={setMinimumScore} /><NumberField label="Minimum confidence" value={minimumConfidence} min={0} max={100} onChange={setMinimumConfidence} /><NumberField label="Holding bars" value={holdingBars} min={1} max={100} onChange={setHoldingBars} /><NumberField label="Account risk (%)" value={riskPercent} min={0.1} max={5} step="0.1" onChange={setRiskPercent} /><NumberField label="Maximum concurrent" value={maximumConcurrent} min={1} max={20} onChange={setMaximumConcurrent} /></div><div className="mt-5 rounded-xl border border-violet-300/15 bg-violet-300/[.035] p-3"><span className="text-[10px] font-semibold uppercase tracking-[.15em] text-violet-200">Entry and exit rules</span><div className="mt-3 space-y-2"><SelectField label="Entry" value={entryRule} onChange={value => setEntryRule(value as typeof entryRule)}><option value="bullish">Bullish technical score</option><option value="bullish-volume">Bullish + volume confirmation</option></SelectField><SelectField label="Stop loss" value={stopRule} onChange={value => setStopRule(value as typeof stopRule)}><option value="atr">ATR multiple</option><option value="percent">Fixed percentage</option></SelectField>{stopRule === "atr" ? <NumberField label="ATR stop multiplier" value={stopAtrMultiplier} min={0.25} max={10} step="0.25" onChange={setStopAtrMultiplier} /> : <NumberField label="Fixed stop (%)" value={stopPercent} min={0.1} max={50} step="0.1" onChange={setStopPercent} />}<SelectField label="Take profit" value={takeProfitRule} onChange={value => setTakeProfitRule(value as typeof takeProfitRule)}><option value="risk-reward">Risk/reward target</option><option value="holding-close">Close at holding end</option></SelectField>{takeProfitRule === "risk-reward" ? <NumberField label="Target risk/reward" value={targetRiskReward} min={0.25} max={20} step="0.25" onChange={setTargetRiskReward} /> : null}</div></div><div className="mt-5 rounded-xl border border-white/[.07] bg-white/[.02] p-3"><div className="flex items-center gap-2 text-[10px] font-semibold uppercase tracking-[.15em] text-slate-500"><CalendarDays className="h-3.5 w-3.5" />Data window</div><div className="mt-3 grid grid-cols-2 gap-2"><label className="text-[10px] text-slate-500">Start<input type="date" value={startDate} onChange={event => setStartDate(event.target.value)} className="mt-1 w-full rounded-md border border-white/[.07] bg-black/10 px-2 py-1.5 text-xs text-slate-200 outline-none" /></label><label className="text-[10px] text-slate-500">End<input type="date" value={endDate} onChange={event => setEndDate(event.target.value)} className="mt-1 w-full rounded-md border border-white/[.07] bg-black/10 px-2 py-1.5 text-xs text-slate-200 outline-none" /></label></div><div className="mt-2"><NumberField label="Fetched candles" value={candleLimit} min={250} max={1000} onChange={setCandleLimit} /></div></div><Button onClick={submit} disabled={run.isPending} className="mt-5 w-full bg-violet-300 text-xs text-slate-950 hover:bg-violet-200">{run.isPending ? <Loader2 className="mr-2 h-3.5 w-3.5 animate-spin" /> : <BarChart3 className="mr-2 h-3.5 w-3.5" />}Run chronological backtest</Button></section><section>{result ? <><span className="text-[10px] font-semibold uppercase tracking-[.18em] text-violet-200">Run #{result.runId} · {result.asset.symbol}</span><h3 className="mt-1 text-lg font-semibold">Outcome analytics</h3><div className="mt-4 grid grid-cols-2 gap-2 sm:grid-cols-3"><Metric label="Signals" value={String(result.metrics.signalCount)} /><Metric label="Win rate" value={result.metrics.winRate === null ? "—" : `${result.metrics.winRate}%`} /><Metric label="Average account return" value={result.metrics.averageReturn === null ? "—" : `${result.metrics.averageReturn}%`} /><Metric label="Median return" value={result.metrics.medianReturn === null ? "—" : `${result.metrics.medianReturn}%`} /><Metric label="Max drawdown" value={`${result.metrics.maximumDrawdown}%`} /><Metric label="Profit factor" value={result.metrics.profitFactor === null ? "—" : String(result.metrics.profitFactor)} /><Metric label="Expectancy" value={result.metrics.expectancy === null ? "—" : `${result.metrics.expectancy}%`} /><Metric label="Sharpe" value={result.metrics.sharpeRatio === null ? "—" : String(result.metrics.sharpeRatio)} /><Metric label="Ending equity" value={`$${result.metrics.endingEquity.toLocaleString()}`} /></div><div className="mt-5 rounded-xl border border-violet-300/15 bg-violet-300/[.04] p-4"><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-violet-200">Score-combination research</span><p className="mt-1 text-[11px] leading-5 text-slate-400">Performance is grouped by the actual positive signal combination at each decision point. Small samples are evidence, not conclusions.</p><div className="mt-3 space-y-2">{result.scoreResearch.slice(0, 4).map(row => <div key={row.combination} className="grid grid-cols-[minmax(0,1fr)_44px_58px_58px] gap-2 border-t border-white/[.06] pt-2 text-[10px]"><span className="truncate text-slate-300" title={row.combination}>{row.combination}</span><span className="font-mono text-slate-500">n={row.sampleSize}</span><span className="font-mono text-emerald-200">{row.winRate === null ? "—" : `${row.winRate}%`}</span><span className="font-mono text-cyan-200">{row.averageReturn === null ? "—" : `${row.averageReturn}%`}</span></div>)}</div></div><div className="mt-5 rounded-xl border border-emerald-300/15 bg-emerald-300/[.04] p-4"><div className="flex gap-2"><ShieldAlert className="mt-0.5 h-4 w-4 shrink-0 text-emerald-200" /><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-emerald-200">Anti-look-ahead guard · {result.dataCutoff.timeframe.toUpperCase()}</span><p className="mt-1 text-xs leading-5 text-emerald-100/70">{result.dataCutoff.model} The model required {result.dataCutoff.requiredHistory} prior candles and held each signal for at most {result.dataCutoff.holdingBars} later bars.</p></div></div></div></> : <div className="grid h-full min-h-80 place-items-center rounded-xl border border-dashed border-white/[.1] p-6 text-center"><div><BarChart3 className="mx-auto h-5 w-5 text-slate-600" /><p className="mt-3 text-sm text-slate-300">Configure and run an experiment</p><p className="mt-1 max-w-sm text-xs leading-5 text-slate-500">Results identify risk-normalized returns, drawdown, profit factor, score-combination evidence, and historical model constraints only when the sample permits it.</p></div></div>}</section></div></ScrollArea>}</DialogContent></Dialog>;
client/src/components/crypto/AlertsPanel.tsx:3:import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
client/src/components/crypto/AlertsPanel.tsx:22:  const { user, loading } = useAuth();
client/src/components/crypto/AlertsPanel.tsx:38:  const create = trpc.crypto.createAlert.useMutation({ onSuccess: async data => { await invalidateExecutionHistory(); toast.success("Alert rule saved", { description: data.scheduleState === "scheduled" ? "The published schedule is active." : "Publish the project to activate its recurring schedule; on-demand evaluation is available now." }); }, onError: error => toast.error("Alert was not saved", { description: error.message }) });
client/src/components/crypto/AlertsPanel.tsx:39:  const evaluate = trpc.crypto.evaluateAlert.useMutation({ onSuccess: async data => { await invalidateExecutionHistory(); const skipped = "skipped" in data ? data.skipped : "no match"; toast.message(data.triggered ? "Alert threshold met" : "Executed successfully — no qualifying opportunities", { description: data.triggered ? "The immutable matching snapshot was saved." : `Recorded outcome: ${skipped}.` }); }, onError: error => toast.error("Execution failed", { description: error.message }) });
client/src/components/crypto/AlertsPanel.tsx:40:  const setEnabled = trpc.crypto.setAlertEnabled.useMutation({ onSuccess: invalidateExecutionHistory, onError: error => toast.error("Schedule state was not changed", { description: error.message }) });
client/src/components/crypto/AlertsPanel.tsx:46:  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-w-6xl border-white/[.1] bg-[#0b1220] p-0 text-slate-100 sm:rounded-xl"><DialogHeader className="border-b border-white/[.07] px-6 py-5"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-lg bg-amber-300/[.12] text-amber-200"><BellRing className="h-4 w-4" /></span><div><DialogTitle className="text-base">Opportunity alerts</DialogTitle><DialogDescription className="mt-1 text-xs text-slate-500">Execution history preserves immutable point-in-time research evidence. It never creates paper or real trades.</DialogDescription></div></div></DialogHeader>{loading ? <div className="grid h-72 place-items-center"><Loader2 className="h-5 w-5 animate-spin text-amber-300" /></div> : !user ? <div className="grid min-h-72 place-items-center px-6 text-center"><div><h3 className="text-sm font-semibold">Sign in to configure alerts</h3><p className="mx-auto mt-2 max-w-sm text-xs leading-5 text-slate-500">Alert thresholds and their execution history are private research records.</p><Button onClick={() => startLogin()} className="mt-5 bg-amber-300 text-slate-950 hover:bg-amber-200">Sign in to configure</Button></div></div> : <ScrollArea className="h-[min(78vh,760px)]"><div className="grid gap-6 p-6 lg:grid-cols-[360px_minmax(0,1fr)]"><section><span className="text-[10px] font-semibold uppercase tracking-[.18em] text-amber-200">New threshold rule</span><div className="mt-3 space-y-2"><label className="block rounded-lg border border-white/[.07] bg-white/[.02] px-3 py-2.5"><span className="text-[10px] text-slate-500">Rule name</span><input value={name} onChange={event => setName(event.target.value)} className="mt-1 w-full bg-transparent text-sm text-slate-100 outline-none" /></label><CompactNumber label="Minimum opportunity" value={opportunity} min={0} max={100} onChange={setOpportunity} /><CompactNumber label="Minimum confidence" value={confidence} min={0} max={100} onChange={setConfidence} /><CompactNumber label="Minimum technical" value={technical} min={0} max={40} onChange={setTechnical} /><CompactNumber label="Cooldown minutes" value={cooldown} min={5} max={10080} onChange={setCooldown} /><label className="block rounded-lg border border-white/[.07] bg-white/[.02] px-3 py-2.5"><span className="text-[10px] text-slate-500">UTC schedule (six fields)</span><input value={cron} onChange={event => setCron(event.target.value)} className="mt-1 w-full bg-transparent font-mono text-xs text-cyan-100 outline-none" /><span className="mt-1 block text-[10px] text-slate-600">Use `0 0 * * * *` for every hour.</span></label><label className="flex items-center justify-between rounded-lg border border-white/[.07] bg-white/[.02] px-3 py-2.5"><span className="text-xs text-slate-300">Market regime must not be Risk Off</span><Switch checked={requireNotRiskOff} onCheckedChange={setRequireNotRiskOff} /></label><label className="flex items-center justify-between rounded-lg border border-white/[.07] bg-white/[.02] px-3 py-2.5"><span className="text-xs text-slate-300">Require bullish setup</span><Switch checked={requireBullishSetup} onCheckedChange={setRequireBullishSetup} /></label><label className="flex items-center justify-between rounded-lg border border-white/[.07] bg-white/[.02] px-3 py-2.5"><span className="text-xs text-slate-300">Send owner notification if matched</span><Switch checked={notificationEnabled} onCheckedChange={setNotificationEnabled} /></label><label className="block rounded-lg border border-white/[.07] bg-white/[.02] px-3 py-2.5"><span className="text-[10px] text-slate-500">Required bullish timeframe contribution</span><select value={requiredTimeframe} onChange={event => setRequiredTimeframe(event.target.value as typeof requiredTimeframe)} className="mt-1 w-full bg-transparent text-xs text-cyan-100 outline-none"><option value="">Any enabled timeframe</option><option value="15m">15m</option><option value="1h">1H</option><option value="4h">4H</option><option value="1d">1D</option></select></label></div><div className="mt-5 rounded-xl border border-white/[.07] bg-white/[.02] p-3"><div className="flex items-center justify-between"><span className="text-[10px] font-semibold uppercase tracking-[.15em] text-slate-500">Asset scope</span><button onClick={() => setAssetIds([])} className="text-[10px] text-amber-200">All tracked assets</button></div><div className="mt-3 grid grid-cols-3 gap-1.5">{DEFAULT_ASSET_UNIVERSE.map(asset => <button key={asset.id} onClick={() => toggleAsset(asset.id)} className={`rounded-md border px-2 py-1.5 text-[10px] ${assetIds.includes(asset.id) ? "border-amber-300/30 bg-amber-300/[.1] text-amber-100" : "border-white/[.07] text-slate-500"}`}>{asset.symbol}</button>)}</div></div><Button onClick={submit} disabled={create.isPending} className="mt-5 w-full bg-amber-300 text-xs text-slate-950 hover:bg-amber-200">{create.isPending ? <Loader2 className="mr-2 h-3.5 w-3.5 animate-spin" /> : <Plus className="mr-2 h-3.5 w-3.5" />}Save alert rule</Button><p className="mt-3 text-[10px] leading-4 text-slate-600">This form does not modify existing alerts. Evaluations only record observations; they never open paper or real trades.</p></section><section><span className="text-[10px] font-semibold uppercase tracking-[.18em] text-amber-200">Configured alerts and history</span><div className="mt-3 space-y-3">{alerts.isLoading ? <div className="grid h-44 place-items-center"><Loader2 className="h-5 w-5 animate-spin text-amber-300" /></div> : alerts.data?.length ? alerts.data.map(alert => { const snapshot = alert.lastSignalSnapshot as { signalSnapshots?: Array<{ asset: { symbol: string }; setup: { type: string } }> } | null; return <div key={alert.id} className="rounded-xl border border-white/[.07] bg-white/[.02] p-4"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2"><span className="text-sm font-semibold">{alert.name}</span>{alert.isEnabled ? <span className="rounded-full bg-emerald-300/[.1] px-2 py-0.5 text-[9px] text-emerald-200">SCHEDULED</span> : <span className="rounded-full bg-slate-400/[.08] px-2 py-0.5 text-[9px] text-slate-400">MANUAL / PENDING</span>}</div><p className="mt-1 text-[11px] text-slate-500">{JSON.stringify(alert.conditions)}</p></div><Switch checked={alert.isEnabled} onCheckedChange={enabled => setEnabled.mutate({ alertId: alert.id, enabled })} disabled={!alert.scheduleCronTaskUid || setEnabled.isPending} /></div><div className="mt-3 flex flex-wrap gap-2"><Button variant="outline" size="sm" onClick={() => evaluate.mutate({ alertId: alert.id })} disabled={evaluate.isPending} className="h-7 border-amber-300/20 bg-amber-300/[.05] px-2.5 text-[10px] text-amber-100 hover:bg-amber-300/[.1]"><Play className="mr-1.5 h-3 w-3" />Evaluate now</Button>{alert.lastTriggeredAt ? <span className="flex items-center gap-1 text-[10px] text-slate-500"><Clock3 className="h-3 w-3" />Triggered {new Date(alert.lastTriggeredAt).toLocaleString()}</span> : null}</div>{snapshot?.signalSnapshots?.length ? <div className="mt-3 rounded-lg border border-emerald-300/10 bg-emerald-300/[.035] p-3 text-xs text-emerald-100/80"><CheckCircle2 className="mr-1.5 inline h-3.5 w-3.5" />Last matching context: {snapshot.signalSnapshots.map(match => `${match.asset.symbol} (${match.setup.type})`).join(", ")}. The immutable execution history retains the complete snapshot.<div className="mt-2 flex flex-wrap gap-2">{snapshot.signalSnapshots.map(match => { const asset = DEFAULT_ASSET_UNIVERSE.find(item => item.symbol === match.asset.symbol); return asset ? <Button key={asset.id} variant="outline" size="sm" onClick={() => onInspectAsset?.(asset.id)} className="h-7 border-emerald-300/20 bg-emerald-300/[.04] px-2 text-[10px] text-emerald-100">Inspect {asset.symbol}</Button> : null; })}</div></div> : null}<AlertExecutionHistory alertId={alert.id} alertName={alert.name} /></div> }) : <div className="grid min-h-64 place-items-center rounded-xl border border-dashed border-white/[.1] px-6 text-center"><div><ShieldAlert className="mx-auto h-5 w-5 text-slate-600" /><p className="mt-3 text-sm text-slate-300">No alert rules yet</p><p className="mt-1 text-xs leading-5 text-slate-500">Create a transparent score threshold rather than a fixed-price alert. Every execution retains its actual conditions.</p></div></div>}</div></section></div></ScrollArea>}</DialogContent></Dialog>;
client/src/components/crypto/ResearchSummaryPanel.tsx:2:import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
client/src/components/crypto/ResearchSummaryPanel.tsx:22:  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-w-5xl border-white/[.1] bg-[#0b1220] p-0 text-slate-100 sm:rounded-xl"><DialogHeader className="border-b border-white/[.07] px-6 py-5"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-lg bg-cyan-300/[.12] text-cyan-200"><BarChart3 className="h-4 w-4" /></span><div><DialogTitle className="text-base">Research Summary</DialogTitle><DialogDescription className="mt-1 text-xs text-slate-500">A timestamped historical evidence view. It does not represent a prediction or guarantee.</DialogDescription></div></div></DialogHeader>{isLoading ? <div className="grid h-72 place-items-center"><Loader2 className="h-5 w-5 animate-spin text-cyan-300" /></div> : !report ? <div className="grid min-h-72 place-items-center px-6 text-center"><div><Database className="mx-auto h-5 w-5 text-slate-600" /><p className="mt-3 text-sm text-slate-300">No validation report is available</p><p className="mt-1 max-w-sm text-xs leading-5 text-slate-500">The system will show no evidence rather than display a fabricated result.</p></div></div> : <ScrollArea className="h-[min(74vh,720px)]"><div className="space-y-6 p-6"><div className="grid gap-3 sm:grid-cols-3"><Fact label="Historical signals" value={String(report.aggregate.signalCount)} /><Fact label="Analysis timeframe" value={report.input.timeframe.toUpperCase()} /><Fact label="As of" value={new Date(latest!.asOf).toLocaleString()} /></div><section className="rounded-xl border border-amber-300/15 bg-amber-300/[.035] p-4"><div className="flex gap-2"><ShieldAlert className="mt-0.5 h-4 w-4 shrink-0 text-amber-200" /><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-amber-200">Evidence discipline</span><p className="mt-1 text-xs leading-5 text-amber-100/75">{report.basis} All results use a next-bar entry and post-entry outcome windows. Rows marked Weak Evidence or Insufficient Data are not a basis for model selection.</p></div></div></section><section><span className="text-[10px] font-semibold uppercase tracking-[.18em] text-cyan-200">Observed score combinations</span><div className="mt-3 grid gap-3 lg:grid-cols-3">{research!.combinations.map(item => <div key={item.id} className="rounded-xl border border-white/[.07] bg-white/[.02] p-4"><div className="flex items-start justify-between gap-2"><div><span className="text-[10px] font-semibold text-slate-500">{item.id}</span><h3 className="mt-1 text-sm font-semibold text-slate-100">{item.label}</h3></div><span className={`rounded-full border px-2 py-1 text-[9px] font-semibold ${statusTone(item.status)}`}>{item.status}</span></div><p className="mt-3 text-[11px] leading-5 text-slate-500">{item.reason}</p><div className="mt-3 grid grid-cols-2 gap-2 text-xs"><Fact label="Observations" value={String(item.metrics.signalCount)} /><Fact label="Average account return" value={display(item.metrics.averageReturn, "%")} /><Fact label="Win rate" value={display(item.metrics.winRate, "%")} /><Fact label="Profit factor" value={display(item.metrics.profitFactor)} /></div><div className="mt-3 border-t border-white/[.06] pt-3 text-[10px] text-slate-500">24H: {display(item.horizons.find(horizon => horizon.label === "24H")?.averageReturn, "%")} · 3D: {display(item.horizons.find(horizon => horizon.label === "3D")?.averageReturn, "%")} · 7D: {display(item.horizons.find(horizon => horizon.label === "7D")?.averageReturn, "%")} · 30D: {display(item.horizons.find(horizon => horizon.label === "30D")?.averageReturn, "%")}</div></div>)}</div></section><section className="grid gap-4 lg:grid-cols-2"><SummaryCard title="Threshold evidence" items={[bestOpportunity ? `Highest observed qualifying opportunity threshold: ${bestOpportunity.threshold}+ (${bestOpportunity.metrics.signalCount} observations; ${display(bestOpportunity.metrics.averageReturn, "%")})` : "No opportunity threshold has the minimum observation count.", bestConfidence ? `Highest observed qualifying confidence threshold: ${bestConfidence.threshold}+ (${bestConfidence.metrics.signalCount} observations; ${display(bestConfidence.metrics.averageReturn, "%")})` : "No confidence threshold has the minimum observation count.", `Joint 80+/70+: ${research!.thresholds.joint.metrics.signalCount} observations; ${display(research!.thresholds.joint.metrics.averageReturn, "%")} average account return; ${research!.thresholds.joint.status}.`]} /><SummaryCard title="Observed extremes" items={[bestCombination ? `Highest observed average: ${bestCombination.label} (${display(bestCombination.metrics.averageReturn, "%")}; ${bestCombination.status}).` : "No measured combination.", worstCombination ? `Lowest observed average: ${worstCombination.label} (${display(worstCombination.metrics.averageReturn, "%")}; ${worstCombination.status}).` : "No measured combination.", "Observed comparisons are not promoted to predictive rules without adequate independent data." ]} /></section><section><span className="text-[10px] font-semibold uppercase tracking-[.18em] text-cyan-200">Sector and regime evidence</span><div className="mt-3 grid gap-3 lg:grid-cols-2"><div className="rounded-xl border border-white/[.07] bg-white/[.02] p-4"><p className="text-sm font-semibold">Sector model comparison</p><p className="mt-2 text-xs leading-5 text-slate-500">{research!.sectorComparison.reason}</p><div className="mt-3 grid grid-cols-2 gap-2">{report.sectorEvidence.map(item => <div key={item.sector} className="rounded-lg border border-white/[.06] px-3 py-2"><span className="block text-xs text-slate-200">{item.sector}</span><span className="mt-1 block font-mono text-[10px] text-slate-500">{item.signalCount} signals · {item.research.status}</span></div>)}</div></div><div className="rounded-xl border border-white/[.07] bg-white/[.02] p-4"><p className="text-sm font-semibold">Market regime comparison</p><p className="mt-2 text-xs leading-5 text-slate-500">{research!.regimeComparison.reason}</p></div></div></section><section className="rounded-xl border border-white/[.07] bg-white/[.02] p-4"><p className="text-sm font-semibold">Data limitations</p><ul className="mt-3 space-y-2">{research!.dataLimitations.map(item => <li key={item} className="text-xs leading-5 text-slate-500">{item}</li>)}</ul></section><div className="flex justify-end"><Button variant="outline" onClick={() => onOpenChange(false)} className="border-white/[.1] bg-white/[.03] text-slate-200">Close summary</Button></div></div></ScrollArea>}</DialogContent></Dialog>;
client/src/components/crypto/AlertExecutionHistory.tsx:2:import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
client/src/components/crypto/AlertExecutionHistory.tsx:32:  return value ? new Date(value).toLocaleString() : "Unavailable";
client/src/components/crypto/AlertExecutionHistory.tsx:49:  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-w-5xl border-white/[.1] bg-[#0b1220] p-0 text-slate-100 sm:rounded-xl"><DialogHeader className="border-b border-white/[.07] px-6 py-5"><DialogTitle className="flex flex-wrap items-center gap-2 text-base">{detail?.alertName ?? "Alert execution"}{detail ? <span className={`rounded-full border px-2 py-0.5 text-[9px] ${outcomeStyle(outcome)}`}>{outcome}</span> : null}</DialogTitle><DialogDescription className="mt-1 text-xs text-slate-500">POINT-IN-TIME SNAPSHOT — this inspection shows only the data captured during the original execution. It never recomputes historical values from current market data.</DialogDescription></DialogHeader><ScrollArea className="h-[min(74vh,720px)]"><div className="space-y-5 p-6">{execution.isLoading ? <div className="grid h-56 place-items-center"><Loader2 className="h-5 w-5 animate-spin text-amber-300" /></div> : !detail ? <div className="rounded-xl border border-rose-300/15 bg-rose-300/[.04] p-4 text-sm text-rose-100">The execution could not be loaded.</div> : <><section className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><Field label="Execution ID" value={`#${detail.id}`} /><Field label="Timestamp" value={formatTime(detail.completedAt)} /><Field label="Duration" value={detail.durationMs === null ? "Unavailable" : `${detail.durationMs} ms`} /><Field label="HTTP / result" value={`${detail.httpStatus ?? "—"} / ${outcome}`} /><Field label="Assets scanned" value={detail.assetsScanned ?? "Unavailable"} /><Field label="Qualifying opportunities" value={detail.qualifyingOpportunities ?? 0} /><Field label="Score config" value={detail.configurationVersion ?? "Legacy snapshot"} /><Field label="Notification" value={detail.notificationStatus ?? "Unavailable"} /></section><section className="rounded-xl border border-cyan-300/15 bg-cyan-300/[.035] p-4"><div className="flex items-center gap-2"><ShieldCheck className="h-4 w-4 text-cyan-200" /><h3 className="text-sm font-semibold text-cyan-100">Point-in-time market regime</h3></div><p className="mt-1 text-xs text-slate-500">Captured source/freshness and unavailable inputs are retained exactly as evaluated.</p><div className="mt-3 grid gap-3 sm:grid-cols-3"><Field label="Classification" value={String(finalRegime.classification ?? regime.availability ?? "Unavailable")} /><Field label="Regime score" value={String(finalRegime.score ?? "Unavailable")} /><Field label="Snapshot timestamp" value={String(regime.timestamp ?? snapshot.generatedAt ?? "Unavailable")} /></div><details className="mt-3 rounded-lg border border-white/[.07] bg-black/10 p-3"><summary className="cursor-pointer text-xs text-cyan-100">Inspect regime inputs and provenance</summary><pre className="mt-3 overflow-x-auto whitespace-pre-wrap break-words text-[10px] leading-4 text-slate-400">{JSON.stringify(regime, null, 2)}</pre></details></section><section className="rounded-xl border border-violet-300/15 bg-violet-300/[.035] p-4"><div className="flex items-center gap-2"><Database className="h-4 w-4 text-violet-200" /><h3 className="text-sm font-semibold text-violet-100">Immutable signal and sector evidence</h3></div><p className="mt-1 text-xs text-slate-500">Matched signals: {signals.length}. Sector state is retained for all scanned assets rather than re-queried today.</p><div className="mt-3 grid gap-3 sm:grid-cols-2"><Field label="Signal snapshots" value={signals.length ? signals.map(item => String(asRecord(asRecord(item).asset).symbol ?? "Asset")).join(", ") : "No qualifying opportunity"} /><Field label="Sector snapshots" value={`${sectors.length} captured asset sector states`} /></div><details className="mt-3 rounded-lg border border-white/[.07] bg-black/10 p-3"><summary className="cursor-pointer text-xs text-violet-100">Inspect complete signal and sector snapshots</summary><pre className="mt-3 overflow-x-auto whitespace-pre-wrap break-words text-[10px] leading-4 text-slate-400">{JSON.stringify({ signals, sectors }, null, 2)}</pre></details></section><section className="rounded-xl border border-amber-300/15 bg-amber-300/[.035] p-4"><div className="flex items-center gap-2"><Clock3 className="h-4 w-4 text-amber-200" /><h3 className="text-sm font-semibold text-amber-100">Configuration and data provenance</h3></div><p className="mt-1 text-xs text-slate-500">The execution remains tied to its original configuration fingerprint, conditions, and provider-freshness record.</p><details className="mt-3 rounded-lg border border-white/[.07] bg-black/10 p-3"><summary className="cursor-pointer text-xs text-amber-100">Inspect captured configuration, conditions, and data sources</summary><pre className="mt-3 overflow-x-auto whitespace-pre-wrap break-words text-[10px] leading-4 text-slate-400">{JSON.stringify({ conditions: snapshot.conditions, scoringConfiguration: snapshot.scoringConfiguration, scoringConfigurationVersion: snapshot.scoringConfigurationVersion, dataProvenance: provenance }, null, 2)}</pre></details></section>{detail.errorMessage ? <section className="rounded-xl border border-rose-300/15 bg-rose-300/[.04] p-4"><div className="flex items-center gap-2"><TriangleAlert className="h-4 w-4 text-rose-200" /><h3 className="text-sm font-semibold text-rose-100">Recorded error detail</h3></div><p className="mt-2 text-xs leading-5 text-rose-100/80">{detail.errorMessage}</p></section> : null}</>}</div></ScrollArea></DialogContent></Dialog>;
client/src/components/crypto/AlertExecutionHistory.tsx:55:  return <section className="mt-4 border-t border-white/[.07] pt-4"><div className="flex items-center justify-between gap-3"><div><p className="text-[10px] font-semibold uppercase tracking-[.16em] text-slate-500">Execution history</p><p className="mt-1 text-[11px] text-slate-500">Scheduled and manual point-in-time records. Zero matches are a successful evaluation state.</p></div><span className="rounded-full border border-white/[.08] px-2 py-1 text-[9px] text-slate-400">{history.data?.length ?? 0} records</span></div>{history.isLoading ? <div className="grid h-24 place-items-center"><Loader2 className="h-4 w-4 animate-spin text-amber-300" /></div> : history.data?.length ? <div className="mt-3 overflow-x-auto rounded-xl border border-white/[.07]"><table className="min-w-[720px] w-full text-left text-[10px]"><thead className="bg-white/[.025] text-slate-500"><tr><th className="px-3 py-2.5 font-medium">Time</th><th className="px-3 py-2.5 font-medium">Alert</th><th className="px-3 py-2.5 font-medium">Status</th><th className="px-3 py-2.5 font-medium">Assets scanned</th><th className="px-3 py-2.5 font-medium">Matches</th><th className="px-3 py-2.5 font-medium">Score config</th><th className="px-3 py-2.5 font-medium">Notification</th><th className="px-3 py-2.5" /></tr></thead><tbody>{history.data.map(execution => { const outcome = executionOutcome(execution); return <tr key={execution.id} className="border-t border-white/[.06] text-slate-300 hover:bg-white/[.025]"><td className="px-3 py-3 text-slate-400">{formatTime(execution.completedAt)}</td><td className="px-3 py-3 font-medium text-slate-200">{alertName}</td><td className="px-3 py-3"><span className={`rounded-full border px-2 py-0.5 text-[9px] ${outcomeStyle(outcome)}`}>{outcome === "NO_MATCH" ? "NO MATCH" : outcome}</span></td><td className="px-3 py-3">{execution.assetsScanned ?? "—"}</td><td className="px-3 py-3">{execution.qualifyingOpportunities ?? 0}</td><td className="max-w-28 truncate px-3 py-3 font-mono text-slate-400">{execution.configurationVersion ?? "Legacy"}</td><td className="px-3 py-3 text-slate-400">{execution.notificationStatus ?? "—"}</td><td className="px-3 py-3"><Button variant="ghost" size="sm" onClick={() => setSelectedExecutionId(execution.id)} className="h-7 px-2 text-[10px] text-cyan-100 hover:bg-cyan-300/[.08]">Inspect <ChevronRight className="ml-1 h-3 w-3" /></Button></td></tr>; })}</tbody></table></div> : <div className="mt-3 rounded-xl border border-dashed border-white/[.1] p-4 text-center text-xs text-slate-500">No execution records are available yet.</div>}<ExecutionInspector alertId={alertId} executionId={selectedExecutionId} open={selectedExecutionId !== null} onOpenChange={open => !open && setSelectedExecutionId(null)} /></section>;
client/src/components/crypto/OpportunityResearchLab.tsx:3:import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
client/src/components/crypto/OpportunityResearchLab.tsx:30:  const { user, loading } = useAuth();
client/src/components/crypto/OpportunityResearchLab.tsx:51:  const { data: detail, isFetching: loadingDetail } = trpc.crypto.researchExperiment.useQuery({ experimentId: activeExperimentId ?? 1 }, { enabled: open && Boolean(activeExperimentId) && Boolean(user) });
client/src/components/crypto/OpportunityResearchLab.tsx:55:    onError: error => toast.error("Research run could not complete", { description: error.message }),
client/src/components/crypto/OpportunityResearchLab.tsx:66:  const download = async (format: "json" | "csv") => { if (!online) { toast.message("Offline read-only mode", { description: "Research exports require a live connection to Crypto Hub." }); return; } if (!activeExperimentId) return; try { const file = await utils.crypto.exportResearchExperiment.fetch({ experimentId: activeExperimentId, format }); const url = URL.createObjectURL(new Blob([file.content], { type: file.mimeType })); const link = document.createElement("a"); link.href = url; link.download = file.filename; link.click(); URL.revokeObjectURL(url); } catch (error) { toast.error("Export unavailable", { description: error instanceof Error ? error.message : "The research export could not be generated." }); } };
client/src/components/crypto/OpportunityResearchLab.tsx:67:  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-w-6xl border-white/[.1] bg-[#0b1220] p-0 text-slate-100 sm:rounded-xl"><DialogHeader className="border-b border-white/[.07] px-6 py-5"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-lg bg-fuchsia-300/[.12] text-fuchsia-200"><FlaskConical className="h-4 w-4" /></span><div><DialogTitle className="text-base">Opportunity Research Lab</DialogTitle><DialogDescription className="mt-1 text-xs text-slate-500">Reproducible historical observation only. It does not change production scoring, alerts, or trade behavior.</DialogDescription></div></div></DialogHeader>{loading ? <Loading /> : !user ? <SignIn /> : <ScrollArea className="h-[min(78vh,760px)]"><div className="grid gap-6 p-6 xl:grid-cols-[345px_minmax(0,1fr)]"><section className="space-y-4"><div><span className="text-[10px] font-semibold uppercase tracking-[.18em] text-fuchsia-200">Controlled experiment</span><h3 className="mt-1 text-sm font-semibold">Exact variable membership</h3></div><label className="block text-[10px] text-slate-500">Research name<input value={name} onChange={event => setName(event.target.value)} maxLength={128} className="field" /></label><label className="block text-[10px] text-slate-500">Experiment<select value={experimentId} onChange={event => setExperimentId(event.target.value as typeof experimentId)} className="field">{experiments.map(experiment => <option key={experiment.id} value={experiment.id}>{experiment.id} — {experiment.label}</option>)}</select></label><div className="rounded-xl border border-fuchsia-300/15 bg-fuchsia-300/[.035] p-3"><p className="text-[10px] font-semibold uppercase tracking-[.15em] text-fuchsia-200">Variables in this experiment</p><div className="mt-2 flex flex-wrap gap-1.5">{selectedExperiment.variables.map(variable => <span key={variable} className="rounded-full border border-white/[.08] bg-black/15 px-2 py-1 text-[10px] text-slate-300">{variable}</span>)}</div></div><div className="grid grid-cols-2 gap-2"><NumberInput label="Opportunity ≥" value={minimumOpportunity} setValue={setMinimumOpportunity} min={0} max={100} /><NumberInput label="Confidence ≥" value={minimumConfidence} setValue={setMinimumConfidence} min={0} max={100} /><NumberInput label="Holding bars" value={holdingBars} setValue={setHoldingBars} min={1} max={100} /><NumberInput label="Risk %" value={riskPercent} setValue={setRiskPercent} min={0.1} max={5} step="0.1" /><NumberInput label="ATR stop" value={stopAtrMultiplier} setValue={setStopAtrMultiplier} min={0.25} max={10} step="0.25" /><NumberInput label="Target R" value={targetRiskReward} setValue={setTargetRiskReward} min={0.25} max={20} step="0.25" /><NumberInput label="Train %" value={trainPercent} setValue={setTrainPercent} min={50} max={90} /></div><label className="block text-[10px] text-slate-500">Timeframe<select value={timeframe} onChange={event => setTimeframe(event.target.value as Timeframe)} className="field"><option value="15m">15M</option><option value="1h">1H</option><option value="4h">4H</option><option value="1d">1D</option></select></label><div className="grid grid-cols-2 gap-2"><label className="text-[10px] text-slate-500">Start date<input type="date" value={startDate} onChange={event => setStartDate(event.target.value)} className="field" /></label><label className="text-[10px] text-slate-500">End date<input type="date" value={endDate} onChange={event => setEndDate(event.target.value)} className="field" /></label></div><div className="grid grid-cols-2 gap-2"><label className="text-[10px] text-slate-500">Sector<select value={sector} onChange={event => setSector(event.target.value)} className="field">{sectors.map(item => <option key={item}>{item}</option>)}</select></label><label className="text-[10px] text-slate-500">Regime<select value={regime} onChange={event => setRegime(event.target.value as typeof regime)} className="field"><option value="ALL">All regimes</option><option value="RISK ON">Risk On</option><option value="SELECTIVE">Selective</option><option value="RISK OFF">Risk Off</option></select></label></div><div><div className="flex items-center justify-between"><span className="text-[10px] text-slate-500">Asset scope</span><button onClick={() => setAssetIds([])} className="text-[10px] text-fuchsia-200 hover:text-fuchsia-100">{assetIds.length ? "Use all assets" : `All ${DEFAULT_ASSET_UNIVERSE.length} assets`}</button></div><div className="mt-2 grid grid-cols-4 gap-1.5">{DEFAULT_ASSET_UNIVERSE.map(asset => <button key={asset.id} onClick={() => toggleAsset(asset.id)} className={cn("rounded-md border px-1.5 py-1.5 text-[10px] font-semibold", assetIds.includes(asset.id) ? "border-fuchsia-300/35 bg-fuchsia-300/[.12] text-fuchsia-100" : "border-white/[.08] bg-white/[.02] text-slate-400")}>{asset.symbol}</button>)}</div></div><p className="text-[10px] leading-4 text-slate-500">Inputs above are fingerprinted and persisted. Signals use completed bars; entry is the next available open. The earlier period is in-sample and the later period is out-of-sample.</p><Button onClick={runExperiment} disabled={run.isPending || name.trim().length < 3 || Boolean(startDate && endDate && startDate > endDate)} className="w-full bg-fuchsia-300 text-xs text-slate-950 hover:bg-fuchsia-200">{run.isPending ? <Loader2 className="mr-2 h-3.5 w-3.5 animate-spin" /> : <BarChart3 className="mr-2 h-3.5 w-3.5" />}Run and persist research</Button><SavedRuns history={history ?? []} activeId={activeExperimentId} onSelect={setSelectedExperimentId} /></section><section>{loadingDetail ? <Loading /> : !detail ? <Empty /> : <ResultView detail={detail} aggregate={aggregate} calibration={calibration} segments={segments} onDownload={download} />}</section></div></ScrollArea>}</DialogContent></Dialog>;
client/src/components/crypto/OpportunityResearchLab.tsx:78:function ResultSection({ title, detail, rows }: { title: string; detail?: string; rows: Result[] }) { return <section><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-fuchsia-200">{title}</span>{detail ? <p className="mt-1 text-[11px] text-slate-500">{detail}</p> : null}</div><div className="mt-3 grid gap-2 md:grid-cols-2">{rows.map(result => <div key={result.id} className="rounded-xl border border-white/[.07] bg-white/[.02] p-3"><div className="flex items-start justify-between gap-2"><div><span className="text-[9px] font-semibold uppercase tracking-[.14em] text-slate-500">{result.dimension.replaceAll("_", " ")}</span><p className="mt-1 text-xs font-semibold text-slate-200">{result.dimensionKey}</p></div><Status status={result.evidenceStatus} /></div><div className="mt-3 grid grid-cols-3 gap-2 font-mono text-[10px]"><span className="text-slate-500">n <strong className="text-slate-200">{result.signalCount}</strong></span><span className="text-slate-500">win <strong className="text-slate-200">{display(result.metrics.winRate, "%")}</strong></span><span className="text-slate-500">avg <strong className="text-slate-200">{display(result.metrics.averageReturn, "%")}</strong></span></div><p className="mt-2 line-clamp-2 text-[10px] leading-4 text-slate-500">{result.reason}</p></div>)}</div></section>; }
client/src/components/crypto/HistoricalDataFoundation.tsx:3:import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
client/src/components/crypto/HistoricalDataFoundation.tsx:18:const local = (value: Date | null | undefined) => value ? new Date(value).toLocaleString() : "Unavailable";
client/src/components/crypto/HistoricalDataFoundation.tsx:22:  const { user, loading } = useAuth();
client/src/components/crypto/HistoricalDataFoundation.tsx:33:  const { data: datasets, isLoading: loadingDatasets } = trpc.crypto.historicalDatasets.useQuery(undefined, { enabled: open && Boolean(user) });
client/src/components/crypto/HistoricalDataFoundation.tsx:35:  const { data: quality, isLoading: loadingQuality } = trpc.crypto.historicalDataQuality.useQuery(activeDatasetId ? { datasetId: activeDatasetId } : undefined, { enabled: open && Boolean(user) && Boolean(activeDatasetId) });
client/src/components/crypto/HistoricalDataFoundation.tsx:36:  const { data: coverageMatrix, isLoading: loadingMatrix } = trpc.crypto.marketCoverageMatrix.useQuery({ datasetId: activeDatasetId ?? 1 }, { enabled: open && Boolean(user) && Boolean(activeDatasetId) });
client/src/components/crypto/HistoricalDataFoundation.tsx:37:  const { data: ingestionHealth, isLoading: loadingHealth } = trpc.crypto.historicalIngestionHealth.useQuery(undefined, { enabled: open && Boolean(user) });
client/src/components/crypto/HistoricalDataFoundation.tsx:42:  const { data: reconstruction, isFetching: reconstructing, error: reconstructionError } = trpc.crypto.reconstructHistoricalState.useQuery(request ?? { datasetId: 1, assetId, timeframe, timestamp: 1, instrumentType }, { enabled: open && Boolean(user) && Boolean(request) && Number.isFinite(request?.timestamp) });
client/src/components/crypto/HistoricalDataFoundation.tsx:47:  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-w-[min(96vw,1500px)] border-white/[.1] bg-[#0b1220] p-0 text-slate-100 sm:rounded-xl"><DialogHeader className="border-b border-white/[.07] px-6 py-5"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-lg bg-cyan-300/[.12] text-cyan-200"><Database className="h-4 w-4" /></span><div><DialogTitle className="text-base">Historical Data Foundation</DialogTitle><DialogDescription className="mt-1 text-xs text-slate-500">Immutable data lineage, Market Coverage Matrix, closed-candle reconstruction, and research-cost assumptions. This workspace never alters the Opportunity Engine, alerts, or trade behavior.</DialogDescription></div></div></DialogHeader>{loading ? <Loading /> : !user ? <SignIn /> : <ScrollArea className="h-[min(80vh,800px)]"><div className="space-y-6 p-6"><section className="grid gap-4 xl:grid-cols-[minmax(0,1.25fr)_minmax(300px,.75fr)]"><div className="rounded-xl border border-white/[.07] bg-white/[.02] p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Immutable dataset basis</span><h3 className="mt-1 text-sm font-semibold">Selected version and coverage</h3></div><select value={activeDatasetId ?? ""} onChange={event => setDatasetId(Number(event.target.value))} className="rounded-md border border-white/[.1] bg-[#0a101c] px-2 py-1.5 text-xs text-slate-100">{(datasets as Dataset[] | undefined)?.map(dataset => <option key={dataset.id} value={dataset.id}>{dataset.version}</option>)}</select></div>{loadingDatasets ? <Loading /> : activeDataset ? <div className="mt-4 grid gap-2 text-xs sm:grid-cols-3"><Cell label="Sealed UTC" value={local(activeDataset.sealedAt)} /><Cell label="Fingerprint" value={activeDataset.contentFingerprint ? `${activeDataset.contentFingerprint.slice(0, 16)}…` : "Unavailable"} /><Cell label="Dataset ID" value={`#${activeDataset.id}`} /></div> : <p className="mt-4 text-xs text-slate-500">No sealed historical dataset is available.</p>}<p className="mt-3 text-[10px] leading-4 text-slate-500">The selected dataset is sealed. Reconstruction reads only records available by that dataset’s cutoff; later backfills and source revisions cannot rewrite its historical basis.</p></div><div className="rounded-xl border border-amber-300/15 bg-amber-300/[.035] p-4"><div className="flex gap-2"><ShieldAlert className="mt-0.5 h-4 w-4 shrink-0 text-amber-200" /><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-amber-200">Coverage interpretation</span><p className="mt-1 text-xs leading-5 text-amber-100/75">Archive-derived series may remain stale until a completed public archive is published. Missing, stale, or unavailable context is retained explicitly, never filled from current values.</p></div></div></div></section><CoverageMatrix data={coverageMatrix as any} loading={loadingMatrix} /><IngestionHealth data={ingestionHealth as any} loading={loadingHealth} /><section className="overflow-hidden rounded-xl border border-white/[.07] bg-white/[.02]"><div className="border-b border-white/[.07] px-4 py-3"><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Scope audit detail</span><p className="mt-1 text-xs text-slate-500">Per-scope ingestion evidence with source counts, gap detection, and a data-quality score that is separate from Opportunity Score.</p></div>{loadingQuality ? <Loading /> : <div className="max-h-72 overflow-auto"><table className="w-full min-w-[1150px] text-left text-xs"><thead className="sticky top-0 bg-[#0d1421] text-[9px] uppercase tracking-[.14em] text-slate-500"><tr><th className="px-4 py-3">Asset</th><th className="px-3 py-3">Frame / instrument</th><th className="px-3 py-3">Status</th><th className="px-3 py-3">Observed / expected</th><th className="px-3 py-3">Coverage / largest gap</th><th className="px-3 py-3">Quality score</th><th className="px-3 py-3">Missing / duplicates</th><th className="px-3 py-3">Latest ingestion</th><th className="px-3 py-3">Source</th></tr></thead><tbody>{rows.map(row => <tr key={row.id} className="border-t border-white/[.055] text-slate-300"><td className="px-4 py-3 font-semibold">{assetOptions.find(asset => asset.id === row.assetId)?.symbol ?? row.assetId}</td><td className="px-3 py-3 font-mono text-slate-400">{row.timeframe.toUpperCase()} · {row.instrumentType}</td><td className="px-3 py-3"><Pill value={row.status} /></td><td className="px-3 py-3 font-mono">{row.actualCandleCount}/{row.expectedCandleCount}</td><td className="px-3 py-3 font-mono text-slate-400">{row.coveragePercent.toFixed(2)}% / {gapLabel(row.longestGapMs)}</td><td className="px-3 py-3"><Pill value={row.qualityRating} label={`${row.qualityScore.toFixed(2)} · ${row.qualityRating}`} /></td><td className="px-3 py-3 font-mono text-slate-400">{row.missingIntervalCount} / {row.duplicateCount}</td><td className="px-3 py-3 text-[10px] text-slate-500">{local(row.lastSuccessfulIngestionAt)}</td><td className="px-3 py-3 text-[10px] text-slate-500">{row.provider}</td></tr>)}{!rows.length ? <tr><td colSpan={9} className="px-4 py-8 text-center text-slate-500">No quality snapshot is present for this dataset.</td></tr> : null}</tbody></table></div>}</section><section className="grid gap-5 xl:grid-cols-[360px_minmax(0,1fr)]"><div className="space-y-4 rounded-xl border border-white/[.07] bg-white/[.02] p-4"><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Closed-candle reconstruction</span><p className="mt-1 text-xs text-slate-500">Inspect only what the selected dataset could provide at a decision timestamp.</p></div><label className="block text-[10px] text-slate-500">Asset<select value={assetId} onChange={event => setAssetId(event.target.value)} className="field">{assetOptions.map(asset => <option key={asset.id} value={asset.id}>{asset.symbol} — {asset.name}</option>)}</select></label><div className="grid grid-cols-2 gap-2"><label className="text-[10px] text-slate-500">Timeframe<select value={timeframe} onChange={event => setTimeframe(event.target.value as Timeframe)} className="field"><option value="15m">15M</option><option value="1h">1H</option><option value="4h">4H</option><option value="1d">1D</option></select></label><label className="text-[10px] text-slate-500">Instrument<select value={instrumentType} onChange={event => setInstrumentType(event.target.value as typeof instrumentType)} className="field"><option value="perpetual">Perpetual</option><option value="spot">Spot</option></select></label></div><label className="block text-[10px] text-slate-500">Decision time (UTC)<input type="datetime-local" value={timestamp} onChange={event => setTimestamp(event.target.value)} className="field" /></label><p className="text-[10px] leading-4 text-slate-500">The signal excludes candles that had not closed at this timestamp. Higher timeframes synchronize to their most recently completed candle.</p></div><ReconstructionCard data={reconstruction} loading={reconstructing} error={reconstructionError?.message} /></section><section className="grid gap-5 xl:grid-cols-[360px_minmax(0,1fr)]"><div className="space-y-3 rounded-xl border border-white/[.07] bg-white/[.02] p-4"><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Research cost model</span><p className="mt-1 text-xs text-slate-500">Preview only. Production trading behavior remains unchanged.</p></div><Field label="Gross return %" value={grossReturn} setValue={setGrossReturn} /><div className="grid grid-cols-2 gap-2"><Field label="Fee % / side" value={fee} setValue={setFee} /><Field label="Slippage % / side" value={slippage} setValue={setSlippage} /></div><label className="block text-[10px] text-slate-500">Funding<select value={fundingMode} onChange={event => setFundingMode(event.target.value as typeof fundingMode)} className="field"><option value="UNAVAILABLE">Unavailable</option><option value="EXCLUDED">Excluded</option><option value="ASSUMED">Assumed</option><option value="ACTUAL">Actual</option></select></label>{instrumentType === "perpetual" && fundingMode !== "UNAVAILABLE" && fundingMode !== "EXCLUDED" ? <Field label="Funding %" value={funding} setValue={setFunding} /> : null}</div><CostCard data={costs} /></section></div></ScrollArea>}</DialogContent></Dialog>;
client/src/components/crypto/HistoricalDataFoundation.tsx:50:function CoverageMatrix({ data, loading }: { data: { snapshot?: { universeKind?: string; historicalSectorStatus?: string; survivorshipWarning?: string; members?: unknown[] } | null; rows?: MatrixRow[] } | null | undefined; loading: boolean }) {
client/src/components/crypto/HistoricalDataFoundation.tsx:53:  if (!loading && data === undefined) return <section className="rounded-xl border border-rose-300/20 bg-rose-300/[.04] p-4 text-xs text-rose-100"><strong>Coverage Matrix unavailable.</strong><p className="mt-1 text-rose-100/70">Select a sealed dataset and retry. If the problem persists, the protected historical coverage request did not return a response; no scoring, alert, or trade state was changed.</p></section>;
client/src/components/crypto/HistoricalDataFoundation.tsx:54:  return <section className="overflow-hidden rounded-xl border border-cyan-300/15 bg-cyan-300/[.025]"><div className="flex flex-wrap items-start justify-between gap-3 border-b border-cyan-300/10 px-4 py-3"><div className="flex gap-2"><span className="mt-0.5 grid h-7 w-7 place-items-center rounded-md bg-cyan-300/[.1] text-cyan-200"><Layers3 className="h-3.5 w-3.5" /></span><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Market Coverage Matrix</span><p className="mt-1 text-xs text-slate-500">Registry-backed availability and quality evidence by selected immutable dataset.</p></div></div><span className="rounded-full border border-cyan-300/20 bg-cyan-300/[.06] px-2 py-1 text-[9px] font-semibold text-cyan-100">{rows.length} registry assets</span></div><div className="grid gap-2 border-b border-cyan-300/10 bg-amber-300/[.035] p-3 lg:grid-cols-2"><Warning title="CURRENT SURVIVOR UNIVERSE" detail={snapshot?.survivorshipWarning ?? "This registry is an intentional source-supported candidate set, not a historical market-membership reconstruction. Delisted and inactive assets may be absent."} /><Warning title="HISTORICAL SECTOR DATA UNAVAILABLE" detail="Registry sector labels organize coverage only. They are not substituted for point-in-time historical sector membership, rank, or sector momentum." /></div>{loading ? <Loading /> : <div className="max-h-[440px] overflow-auto"><table className="w-full min-w-[1540px] text-left text-xs"><thead className="sticky top-0 z-10 bg-[#0d1421] text-[9px] uppercase tracking-[.14em] text-slate-500"><tr><th className="px-4 py-3">Asset / inclusion</th><th className="px-3 py-3">15M</th><th className="px-3 py-3">1H</th><th className="px-3 py-3">4H</th><th className="px-3 py-3">1D</th><th className="px-3 py-3">Market cap</th><th className="px-3 py-3">Regime</th><th className="px-3 py-3">Historical sector</th><th className="px-3 py-3">Aggregate coverage</th><th className="px-3 py-3">Longest gap</th><th className="px-3 py-3">Quality</th><th className="px-3 py-3">Latest observed</th></tr></thead><tbody>{rows.map(row => { const aggregate = aggregateRow(row); return <tr key={row.assetId} className="border-t border-white/[.055] align-top text-slate-300"><td className="px-4 py-3"><div className="flex items-center gap-2"><strong>{row.symbol}</strong><span className="rounded border border-white/[.1] px-1.5 py-0.5 font-mono text-[8px] text-slate-400">{row.priorityTier.replace("_", " ")}</span></div><p className="mt-1 max-w-64 text-[10px] leading-4 text-slate-500">{row.inclusionReason}</p><span className="mt-1 inline-block text-[9px] text-slate-400">Registry taxonomy: {row.registrySector ?? "Unassigned"}</span></td>{(["15m", "1h", "4h", "1d"] as const).map(frame => <td key={frame} className="px-3 py-3"><FrameCell scope={row.timeframes[frame]} /></td>)}<td className="px-3 py-3"><Pill value={row.marketCapStatus} /></td><td className="px-3 py-3"><Pill value={row.regimeStatus} /></td><td className="px-3 py-3"><Pill value={row.sectorStatus} /></td><td className="px-3 py-3 font-mono text-slate-200">{aggregate.coverage.toFixed(2)}%<span className="mt-1 block text-[9px] text-slate-500">{aggregate.observed}/{aggregate.expected}</span></td><td className="px-3 py-3 font-mono">{gapLabel(aggregate.longestGap)}</td><td className="px-3 py-3"><Pill value={row.dataQualityStatus} label={aggregate.score === null ? row.dataQualityStatus : `${aggregate.score.toFixed(2)} · ${row.dataQualityStatus}`} /></td><td className="px-3 py-3 text-[10px] text-slate-500">{local(row.lastObservedAt)}</td></tr>; })}{!rows.length ? <tr><td colSpan={12} className="px-4 py-8 text-center text-slate-500">No registry or coverage snapshot is available for this dataset.</td></tr> : null}</tbody></table></div>}</section>;
client/src/components/crypto/HistoricalDataFoundation.tsx:57:function IngestionHealth({ data, loading }: { data: any; loading: boolean }) {
client/src/components/crypto/HistoricalDataFoundation.tsx:58:  if (loading) return <section className="rounded-xl border border-white/[.07] bg-white/[.02]"><Loading /></section>;
client/src/components/crypto/HistoricalDataFoundation.tsx:63:  return <section className="overflow-hidden rounded-xl border border-cyan-300/15 bg-cyan-300/[.025]"><div className="flex flex-wrap items-start justify-between gap-3 border-b border-cyan-300/10 px-4 py-3"><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Ingestion Health</span><p className="mt-1 text-xs text-slate-500">Immutable callback executions, failed or missing ranges, and informational readiness. This view never runs ingestion or research.</p></div><Pill value={readiness?.status ?? "NOT_READY"} label={(readiness?.status ?? "NOT_READY").replaceAll("_", " ")} /></div><div className="grid gap-3 border-b border-white/[.06] p-4 lg:grid-cols-[minmax(0,1.25fr)_minmax(280px,.75fr)]"><div className="grid gap-2 sm:grid-cols-3"><Cell label="Last successful run" value={local(data?.latest?.success?.completedAt)} /><Cell label="Last partial run" value={local(data?.latest?.partial?.completedAt)} /><Cell label="Last failed run" value={local(data?.latest?.failed?.completedAt)} /></div><div className="rounded-lg border border-amber-300/15 bg-amber-300/[.035] px-3 py-2"><span className="text-[9px] font-semibold uppercase tracking-[.13em] text-amber-200">Research Dataset Readiness</span><p className="mt-1 text-xs text-amber-100/80">{(readiness?.reasons ?? ["Awaiting protected health evidence."]).join(" · ")}</p><p className="mt-1 text-[10px] text-amber-100/60">Next scheduled UTC: {nextRuns || "Unavailable"}</p><p className="mt-1 text-[10px] text-amber-100/60">Informational only; it does not launch Research Lab.</p></div></div><div className="grid gap-4 p-4 xl:grid-cols-[minmax(0,1.2fr)_minmax(320px,.8fr)]"><div className="overflow-auto"><table className="w-full min-w-[860px] text-left text-xs"><thead className="text-[9px] uppercase tracking-[.14em] text-slate-500"><tr><th className="pb-2">Status / execution</th><th className="pb-2">Started / duration</th><th className="pb-2">Assets</th><th className="pb-2">Candles</th><th className="pb-2">Gaps / errors</th></tr></thead><tbody>{executions.map((execution: any) => <tr key={execution.id} className="border-t border-white/[.055] text-slate-300"><td className="py-2"><Pill value={execution.status} /><span className="ml-2 font-mono text-[9px] text-slate-500">#{execution.id}</span></td><td className="py-2 text-[10px] text-slate-400">{local(execution.startedAt)}<span className="block font-mono text-slate-500">{execution.durationMs ?? 0}ms</span></td><td className="py-2 font-mono">{execution.assetsSucceeded}/{execution.assetsAttempted} succeeded<span className="block text-rose-200">{execution.assetsFailed} failed</span></td><td className="py-2 font-mono">+{execution.candlesInserted}<span className="block text-slate-500">{execution.candlesSkipped} skipped · {execution.duplicatesDetected} duplicate</span></td><td className="py-2 font-mono">{execution.gapsDetected} gaps<span className="block max-w-52 truncate text-rose-200">{Array.isArray(execution.providerErrors) && execution.providerErrors.length ? "Provider error recorded" : execution.status === "SUCCESS" && !execution.candlesInserted ? "No new candles available" : "—"}</span></td></tr>)}{!executions.length ? <tr><td colSpan={5} className="py-6 text-center text-slate-500">No callback execution exists yet. The three new schedules are enabled and await their first production cadence.</td></tr> : null}</tbody></table></div><div className="rounded-lg border border-white/[.06] bg-black/10 p-3"><span className="text-[9px] font-semibold uppercase tracking-[.14em] text-cyan-200">Failed / missing range inspection</span><div className="mt-2 max-h-52 space-y-2 overflow-auto">{issues.map((issue: any) => <div key={issue.id} className="rounded border border-white/[.06] bg-white/[.02] p-2 text-[10px]"><div className="flex items-center justify-between gap-2"><strong>{issue.assetId.toUpperCase()} · {issue.timeframe.toUpperCase()}</strong><Pill value={issue.issueKind} /></div><p className="mt-1 text-slate-400">Expected {local(issue.expectedStartAt)} → {local(issue.expectedEndAt)}</p><p className="mt-1 text-rose-200">{issue.errorReason ?? `${issue.missingIntervalCount} missing intervals`}</p><p className="mt-1 text-slate-500">First detected {local(issue.firstDetectedAt)} · last checked {local(issue.lastCheckedAt)} · retry {issue.retryStatus ?? "PENDING"}.</p></div>)}{!issues.length ? <p className="py-4 text-center text-xs text-slate-500">No unresolved failed or missing range is currently recorded.</p> : null}</div></div></div></section>;
client/src/components/crypto/HistoricalDataFoundation.tsx:74:function ReconstructionCard({ data, loading, error }: { data: any; loading: boolean; error?: string }) { if (loading) return <div className="rounded-xl border border-white/[.07] bg-white/[.02]"><Loading /></div>; if (error) return <div className="rounded-xl border border-rose-300/20 bg-rose-300/[.05] p-4 text-xs text-rose-100"><strong>Reconstruction unavailable.</strong><p className="mt-1 text-rose-100/70">{error}</p></div>; if (!data) return <div className="rounded-xl border border-dashed border-white/[.1] p-6 text-xs text-slate-500">Select a sealed dataset and valid decision time.</div>; return <div className="rounded-xl border border-white/[.07] bg-white/[.02] p-4"><div className="flex items-start justify-between gap-3"><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Point-in-time snapshot</span><p className="mt-1 text-xs text-slate-500">Decision candle closed {new Date(data.decisionAt).toLocaleString()} · dataset {data.dataset.version}</p></div><Pill value={data.completeness === "FULL" ? "COMPLETE" : "PARTIAL"} label={data.score.status} /></div><div className="mt-4 grid grid-cols-2 gap-2 sm:grid-cols-4"><Cell label="Opportunity" value={`${data.score.score}/100`} /><Cell label="Confidence" value={`${data.score.confidence}/100`} /><Cell label="Technical" value={`${data.score.technicalScore}/40`} /><Cell label="Latest close" value={String(data.ohlcv.latest.close)} /></div><div className="mt-4 grid gap-2 sm:grid-cols-3"><Context label="Market cap" value={data.marketCap.availability} detail={data.marketCap.value ?? "Unavailable"} /><Context label="Regime" value={data.regime.classification} detail={data.regime.availability} /><Context label="Sector" value={data.sector.availability} detail={data.sector.sector ?? "Not substituted"} /></div>{data.unavailable.length ? <div className="mt-4 rounded-lg border border-amber-300/15 bg-amber-300/[.035] p-3"><span className="text-[9px] font-semibold uppercase tracking-[.14em] text-amber-200">Unavailable historical inputs</span><ul className="mt-2 space-y-1 text-[10px] leading-4 text-amber-100/75">{data.unavailable.map((item: string) => <li key={item}>• {item}</li>)}</ul></div> : null}</div>; }
client/src/components/crypto/HistoricalDataFoundation.tsx:76:function CostCard({ data }: { data: any }) { return <div className="rounded-xl border border-white/[.07] bg-white/[.02] p-4"><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Gross and net are separate</span>{!data ? <p className="mt-3 text-xs text-slate-500">Cost preview unavailable.</p> : <><div className="mt-4 grid grid-cols-2 gap-2 sm:grid-cols-4"><Cell label="Gross" value={`${data.grossReturnPercent}%`} /><Cell label="Fees" value={`${data.totalFeePercent}%`} /><Cell label="Slippage" value={`${data.totalSlippagePercent}%`} /><Cell label="Funding" value={data.fundingPercent === null ? "Unavailable" : `${data.fundingPercent}%`} /></div><div className="mt-4 rounded-lg border border-cyan-300/15 bg-cyan-300/[.04] px-4 py-3"><span className="text-[9px] font-semibold uppercase tracking-[.14em] text-cyan-200">Net return after declared costs</span><strong className="mt-1 block font-mono text-xl text-slate-100">{data.netReturnPercent === null ? "Unavailable" : `${data.netReturnPercent}%`}</strong><p className="mt-1 text-[10px] text-slate-500">Funding: {data.fundingStatus}. Spot never receives a funding charge.</p></div></>}</div>; }
client/src/components/crypto/ExecutionCostLab.tsx:3:import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
client/src/components/crypto/ExecutionCostLab.tsx:17:const fmt = (value: number | null | undefined, suffix = "", digits = 2) => value === null || value === undefined ? "Unavailable" : `${value.toFixed(digits)}${suffix}`;
client/src/components/crypto/ExecutionCostLab.tsx:18:const money = (value: number | null | undefined) => value === null || value === undefined ? "Unavailable" : new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 2 }).format(value);
client/src/components/crypto/ExecutionCostLab.tsx:22:  const { user, loading } = useAuth();
client/src/components/crypto/ExecutionCostLab.tsx:51:  const { data: preview, isFetching: previewing, error: previewError } = trpc.crypto.previewExecutionCostStudy.useQuery(input, { enabled: open && Boolean(user) && validInput, retry: false });
client/src/components/crypto/ExecutionCostLab.tsx:57:    onError: error => toast.error("Execution-cost study could not complete", { description: error.message }),
client/src/components/crypto/ExecutionCostLab.tsx:59:  const download = async (format: "json" | "csv") => { if (!activeStudyId) return; try { const file = await utils.crypto.exportExecutionCostStudy.fetch({ studyId: activeStudyId, format }); const url = URL.createObjectURL(new Blob([file.content], { type: file.mimeType })); const link = document.createElement("a"); link.href = url; link.download = file.filename; link.click(); URL.revokeObjectURL(url); } catch (error) { toast.error("Export unavailable", { description: error instanceof Error ? error.message : "The immutable study export could not be generated." }); } };
client/src/components/crypto/ExecutionCostLab.tsx:61:  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-w-[min(97vw,1540px)] border-white/[.1] bg-[#0b1220] p-0 text-slate-100 sm:rounded-xl"><DialogHeader className="border-b border-white/[.07] px-6 py-5"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-lg bg-violet-300/[.12] text-violet-200"><Calculator className="h-4 w-4" /></span><div><DialogTitle className="text-base">Execution Cost Lab</DialogTitle><DialogDescription className="mt-1 text-xs text-slate-500">Research-only hypothetical execution. It never changes Opportunity scoring, alerts, paper trading, real trading, or launches a Research Lab experiment.</DialogDescription></div></div></DialogHeader>{loading ? <Loading /> : !user ? <SignIn /> : <ScrollArea className="h-[min(82vh,840px)]"><div className="grid gap-6 p-6 xl:grid-cols-[360px_minmax(0,1fr)]"><section className="space-y-4"><div><span className="text-[10px] font-semibold uppercase tracking-[.18em] text-violet-200">Declared cost model</span><h3 className="mt-1 text-sm font-semibold">Hypothetical historical trade</h3></div><label className="block text-[10px] text-slate-500">Study name<input value={name} onChange={event => setName(event.target.value)} className="field" /></label><label className="block text-[10px] text-slate-500">Sealed dataset<select value={activeDatasetId ?? ""} onChange={event => setDatasetId(Number(event.target.value))} className="field">{(datasets as Dataset[] | undefined)?.map(item => <option key={item.id} value={item.id}>{item.version}</option>)}</select></label><div className="grid grid-cols-2 gap-2"><label className="text-[10px] text-slate-500">Asset<select value={assetId} onChange={event => setAssetId(event.target.value)} className="field">{assetOptions.map(asset => <option key={asset.assetId} value={asset.assetId}>{asset.symbol}</option>)}</select></label><label className="text-[10px] text-slate-500">Timeframe<select value={timeframe} onChange={event => setTimeframe(event.target.value as Timeframe)} className="field"><option value="15m">15M</option><option value="1h">1H</option><option value="4h">4H</option><option value="1d">1D</option></select></label><label className="text-[10px] text-slate-500">Instrument<select value={instrumentType} onChange={event => setInstrumentType(event.target.value as typeof instrumentType)} className="field"><option value="perpetual">Perpetual</option><option value="spot">Spot</option></select></label><label className="text-[10px] text-slate-500">Side<select value={side} onChange={event => setSide(event.target.value as typeof side)} className="field"><option value="long">Long</option><option value="short">Short</option></select></label></div><div className="grid grid-cols-2 gap-2"><label className="text-[10px] text-slate-500">Entry (UTC)<input type="datetime-local" value={entryTime} onChange={event => setEntryTime(event.target.value)} className="field" /></label><label className="text-[10px] text-slate-500">Exit (UTC)<input type="datetime-local" value={exitTime} onChange={event => setExitTime(event.target.value)} className="field" /></label></div><NumberField label="Trade size (USD)" value={tradeSizeUsd} setValue={setTradeSizeUsd} min={1} /><section className="rounded-xl border border-white/[.07] bg-white/[.02] p-3"><p className="text-[9px] font-semibold uppercase tracking-[.15em] text-violet-200">Fees — per side</p><div className="mt-2 grid grid-cols-2 gap-2"><SelectField label="Entry" value={entryKind} setValue={value => setEntryKind(value as typeof entryKind)} options={["maker", "taker"]} /><SelectField label="Exit" value={exitKind} setValue={value => setExitKind(value as typeof exitKind)} options={["maker", "taker"]} /><NumberField label="Entry %" value={entryFee} setValue={setEntryFee} step="0.01" min={0} /><NumberField label="Exit %" value={exitFee} setValue={setExitFee} step="0.01" min={0} /></div><p className="mt-2 text-[10px] leading-4 text-slate-500">Default 0.10% is a declared research assumption, not a claim about every exchange’s fee schedule.</p></section><section className="rounded-xl border border-white/[.07] bg-white/[.02] p-3"><p className="text-[9px] font-semibold uppercase tracking-[.15em] text-violet-200">Slippage & liquidity</p><div className="mt-2 grid grid-cols-2 gap-2"><NumberField label="Entry bps" value={entryBps} setValue={setEntryBps} min={0} /><NumberField label="Exit bps" value={exitBps} setValue={setExitBps} min={0} /><NumberField label="Impact coefficient" value={coefficient} setValue={setCoefficient} step="0.1" min={0} /><NumberField label="Impact cap bps" value={capBps} setValue={setCapBps} min={0} /></div><label className="mt-2 flex items-center gap-2 text-[10px] text-slate-400"><input type="checkbox" checked={impactEnabled} onChange={event => setImpactEnabled(event.target.checked)} />Use ESTIMATED_LIQUIDITY_IMPACT from point-in-time OHLCV volume</label></section><section className="rounded-xl border border-white/[.07] bg-white/[.02] p-3"><p className="text-[9px] font-semibold uppercase tracking-[.15em] text-violet-200">Funding</p><label className="mt-2 block text-[10px] text-slate-500">Mode<select disabled={instrumentType === "spot"} value={instrumentType === "spot" ? "EXCLUDED" : fundingMode} onChange={event => setFundingMode(event.target.value as FundingMode)} className="field disabled:opacity-60"><option value="UNAVAILABLE">Funding data unavailable</option><option value="ACTUAL">Actual public funding history</option><option value="ASSUMED">Declared funding assumption</option><option value="EXCLUDED">Explicitly excluded</option></select></label>{instrumentType === "perpetual" && fundingMode === "ASSUMED" ? <NumberField label="Cumulative funding %" value={assumedFunding} setValue={setAssumedFunding} step="0.01" /> : null}<p className="mt-2 text-[10px] leading-4 text-slate-500">Spot never receives funding. Actual funding is fetched and preserved only when you choose to persist a perpetual study.</p></section><Button onClick={() => persist.mutate(input)} disabled={!validInput || persist.isPending || name.trim().length < 3} className="w-full bg-violet-300 text-xs text-slate-950 hover:bg-violet-200">{persist.isPending ? <Loader2 className="mr-2 h-3.5 w-3.5 animate-spin" /> : <Save className="mr-2 h-3.5 w-3.5" />}Persist immutable study</Button><SavedStudies studies={studies ?? []} activeId={activeStudyId} onSelect={setSelectedStudyId} /></section><section className="space-y-5">{previewing ? <Loading /> : previewError ? <ErrorCard error={previewError.message} /> : preview ? <PreviewView preview={preview as any} /> : <Empty />}{study ? <SavedStudy detail={study as any} onDownload={download} /> : null}</section></div></ScrollArea>}</DialogContent></Dialog>;
client/src/components/crypto/ExecutionCostLab.tsx:69:function ErrorCard({ error }: { error: string }) { return <div className="rounded-xl border border-rose-300/20 bg-rose-300/[.05] p-5"><strong className="text-sm text-rose-100">Point-in-time cost preview unavailable</strong><p className="mt-2 text-xs leading-5 text-rose-100/70">{error}</p><p className="mt-2 text-[10px] text-rose-100/55">The lab has not substituted live prices, liquidity, funding, or order-book data.</p></div>; }
client/src/components/crypto/ExecutionCostLab.tsx:71:function PreviewView({ preview }: { preview: any }) { const outcome = preview.outcome; const availability = preview.state?.funding?.status; const sample = preview.sensitivity?.filter((row: any) => row.tradeSizeUsd === 10_000 && row.feePercent === 0.1) ?? []; return <div className="space-y-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><span className="text-[10px] font-semibold uppercase tracking-[.18em] text-violet-200">Transparent cost preview</span><h3 className="mt-1 text-lg font-semibold">Gross result → declared execution costs → net result</h3><p className="mt-1 text-[11px] text-slate-500">Sealed dataset {preview.provenance.dataset.version} · {preview.provenance.dataset.fingerprint?.slice(0, 12) ?? "no fingerprint"}…</p></div><Pill value={outcome.availability} /></div><section className="rounded-xl border border-amber-300/15 bg-amber-300/[.035] p-4"><div className="flex gap-2"><ShieldAlert className="mt-0.5 h-4 w-4 shrink-0 text-amber-200" /><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-amber-200">Research-only and point-in-time</span><p className="mt-1 text-xs leading-5 text-amber-100/75">Historical order-book data is unavailable and is not fabricated. Liquidity impact is labeled as an estimate from the stored OHLCV window. This preview does not create a Research Lab experiment or change any live configuration.</p></div></div></section><div className="grid gap-2 sm:grid-cols-4"><Metric label="Gross return" value={fmt(outcome.gross.returnPercent, "%")} /><Metric label="Gross P/L" value={money(outcome.gross.pnlUsd)} /><Metric label="Total costs" value={money(outcome.totalTradingCostsUsd)} /><Metric label="Net return" value={fmt(outcome.netReturnPercent, "%")} /></div><section className="overflow-hidden rounded-xl border border-white/[.07] bg-white/[.02]"><div className="border-b border-white/[.07] px-4 py-3"><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-violet-200">Cost breakdown</span><p className="mt-1 text-xs text-slate-500">Every charge is shown separately; an unavailable required component leaves net output unavailable.</p></div><div className="overflow-auto"><table className="w-full min-w-[760px] text-left text-xs"><thead className="bg-black/10 text-[9px] uppercase tracking-[.14em] text-slate-500"><tr><th className="px-4 py-3">Component</th><th className="px-3 py-3">Entry</th><th className="px-3 py-3">Exit</th><th className="px-3 py-3">Funding</th><th className="px-3 py-3">Status</th></tr></thead><tbody><Breakdown label="Fee" entry={outcome.entry.fee} exit={outcome.exit.fee} /><Breakdown label="Fixed slippage" entry={outcome.entry.slippage} exit={outcome.exit.slippage} /><Breakdown label="Estimated liquidity impact" entry={outcome.entry.liquidityImpact} exit={outcome.exit.liquidityImpact} /><tr className="border-t border-white/[.06]"><td className="px-4 py-3 font-medium text-slate-200">Funding</td><td className="px-3 py-3 text-slate-600">—</td><td className="px-3 py-3 text-slate-600">—</td><td className="px-3 py-3 font-mono text-slate-200">{money(outcome.funding.amountUsd)}<span className="ml-2 text-[9px] text-slate-500">{outcome.funding.mode}</span></td><td className="px-3 py-3"><Pill value={outcome.funding.status} /></td></tr></tbody></table></div></section><div className="grid gap-5 xl:grid-cols-2"><section className="rounded-xl border border-white/[.07] bg-white/[.02] p-4"><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-violet-200">Liquidity evidence</span><div className="mt-3 grid grid-cols-2 gap-2"><Metric label="Entry tier" value={`${outcome.liquidity.entry.tier} · ${outcome.liquidity.entry.status}`} /><Metric label="Exit tier" value={`${outcome.liquidity.exit.tier} · ${outcome.liquidity.exit.status}`} /><Metric label="Entry quote volume" value={money(outcome.liquidity.entry.quoteVolume)} /><Metric label="Exit quote volume" value={money(outcome.liquidity.exit.quoteVolume)} /></div><p className="mt-3 text-[10px] leading-4 text-slate-500">Funding availability: {availability}. Order book: HISTORICAL ORDER-BOOK DATA UNAVAILABLE.</p></section><section className="rounded-xl border border-white/[.07] bg-white/[.02] p-4"><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-violet-200">Liquidity stress test</span><div className="mt-3 space-y-2">{(preview.stress ?? []).map((item: any) => <div key={item.scenario} className="flex items-center justify-between rounded-lg border border-white/[.06] bg-black/10 px-3 py-2"><span className="text-[10px] text-slate-300">{item.scenario.replaceAll("_", " ")}</span><span className="font-mono text-xs text-slate-100">{fmt(item.outcome.netReturnPercent, "%")}</span></div>)}</div></section></div><section className="rounded-xl border border-white/[.07] bg-white/[.02] p-4"><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-violet-200">Cost sensitivity — fee / slippage / size</span><p className="mt-1 text-xs text-slate-500">All configurations remain visible; the lab never chooses the most favorable one.</p><div className="mt-3 grid gap-2 sm:grid-cols-2 lg:grid-cols-4">{sample.slice(0, 8).map((row: any) => <div key={`${row.tradeSizeUsd}-${row.feePercent}-${row.slippagePercent}`} className="rounded-lg border border-white/[.06] bg-black/10 px-3 py-2"><span className="text-[9px] text-slate-500">{money(row.tradeSizeUsd)} · fee {row.feePercent}% · slip {row.slippagePercent}%</span><strong className="mt-1 block font-mono text-xs text-slate-100">{fmt(row.outcome.netReturnPercent, "%")}</strong></div>)}</div></section>{outcome.limitations.length ? <section className="rounded-xl border border-amber-300/15 bg-amber-300/[.035] p-4"><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-amber-200">Limitations retained</span><ul className="mt-2 space-y-1 text-[10px] leading-4 text-amber-100/75">{outcome.limitations.map((item: string) => <li key={item}>• {item}</li>)}</ul></section> : null}</div>; }
client/src/components/crypto/BackupRecoveryPanel.tsx:2:import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
client/src/components/crypto/BackupRecoveryPanel.tsx:10:const local = (value: Date | string | null | undefined) => value ? new Date(value).toLocaleString() : "Unavailable";
client/src/components/crypto/BackupRecoveryPanel.tsx:14:  const { user, loading } = useAuth();
client/src/components/crypto/BackupRecoveryPanel.tsx:23:    onError: error => toast.error("Recovery archive failed", { description: error.message }),
client/src/components/crypto/BackupRecoveryPanel.tsx:27:    onError: error => toast.error("Archive download unavailable", { description: error.message }),
client/src/components/crypto/BackupRecoveryPanel.tsx:31:    onError: error => toast.error("Verified Recovery ZIP unavailable", { description: error.message }),
client/src/components/crypto/BackupRecoveryPanel.tsx:36:  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-w-[min(96vw,1180px)] border-white/[.1] bg-[#0b1220] p-0 text-slate-100 sm:rounded-xl"><DialogHeader className="border-b border-white/[.07] px-6 py-5"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-lg bg-cyan-300/[.12] text-cyan-200"><ArchiveRestore className="h-4 w-4" /></span><div><DialogTitle className="text-base">Backup & Recovery</DialogTitle><DialogDescription className="mt-1 text-xs text-slate-500">Authenticated, owner-scoped disaster-recovery archives. Backup state is independent from historical ingestion, scoring, research execution, and trading behavior.</DialogDescription></div></div></DialogHeader>{loading ? <Loading /> : !user ? <SignIn /> : <ScrollArea className="h-[min(78vh,760px)]"><div className="space-y-5 p-6"><section className="grid gap-4 xl:grid-cols-[minmax(0,1.25fr)_minmax(300px,.75fr)]"><div className="rounded-xl border border-cyan-300/15 bg-cyan-300/[.025] p-4"><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Portable recovery archive</span><h3 className="mt-1 text-sm font-semibold">Export → verify → restore-validate</h3><p className="mt-2 max-w-2xl text-xs leading-5 text-slate-400">Each archive is a versioned ZIP with normalized JSON entities, captured MySQL DDL, a manifest, per-component SHA-256 checksums, dataset-version identifiers, and an isolated in-memory restore comparison. Private research and cost records are limited to the signed-in owner.</p><Button onClick={() => create.mutate()} disabled={create.isPending} className="mt-4 bg-cyan-300 text-slate-950 hover:bg-cyan-200"><ArchiveRestore className="mr-2 h-4 w-4" />{create.isPending ? "Creating verified archive…" : "Create verified recovery archive"}</Button></div><div className="rounded-xl border border-amber-300/15 bg-amber-300/[.035] p-4"><div className="flex gap-2"><ShieldAlert className="mt-0.5 h-4 w-4 shrink-0 text-amber-200" /><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-amber-200">Retention and limits</span><p className="mt-1 text-xs leading-5 text-amber-100/75">Managed archive retention is 30 days. Download a verified ZIP to storage you control for off-platform resilience. Platform OAuth, secrets, deployment/account state, and existing cron registrations require manual recovery.</p></div></div></div></section>{isPrimaryLoading ? <Loading /> : primary ? <section className="rounded-xl border border-emerald-300/20 bg-emerald-300/[.035] p-4"><div className="flex flex-wrap items-start justify-between gap-4"><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-emerald-200">Verified primary recovery ZIP</span><h3 className="mt-1 font-mono text-sm font-semibold text-slate-100">{primary.exportId}.zip</h3><p className="mt-1 text-xs text-slate-400">Created {local(primary.createdAt)} · {bytes(primary.archiveSizeBytes)} · owner-only signed download</p></div><Button onClick={() => downloadPrimary.mutate()} disabled={downloadPrimary.isPending} className="bg-emerald-300 text-emerald-950 hover:bg-emerald-200"><Download className="mr-2 h-4 w-4" />{downloadPrimary.isPending ? "Preparing protected download…" : "Download Verified Recovery ZIP"}</Button></div><div className="mt-4 grid gap-2 sm:grid-cols-2 lg:grid-cols-4"><Cell title="Verification" detail={primary.status === "verified" ? "Verified" : primary.status} /><Cell title="Checksum" detail={primary.verification?.checksumMismatches?.length === 0 ? "0 errors" : "Mismatch reported"} /><Cell title="Validation" detail={primary.verification?.valid ? `${primary.verification.sourceVsRestoredCounts?.length ?? 0}/37 counts · ${primary.verification.relationshipChecks?.length ?? 0}/54 relationships` : "Unavailable"} /><Cell title="Archive checksum" detail={primary.archiveChecksum} /></div></section> : null}<section className="overflow-hidden rounded-xl border border-white/[.07] bg-white/[.02]"><div className="flex flex-wrap items-start justify-between gap-3 border-b border-white/[.07] px-4 py-3"><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Archive history</span><p className="mt-1 text-xs text-slate-500">Only archives created by this account are visible. Every entry retains manifest, checksum, retention, and restore-validation evidence.</p></div><span className="rounded-full border border-white/[.08] px-2 py-1 text-[9px] font-semibold text-slate-400">{recent.length} retained</span></div>{isLoading ? <Loading /> : <div className="max-h-[430px] overflow-auto"><table className="w-full min-w-[980px] text-left text-xs"><thead className="sticky top-0 bg-[#0d1421] text-[9px] uppercase tracking-[.14em] text-slate-500"><tr><th className="px-4 py-3">Export / status</th><th className="px-3 py-3">Created / retention</th><th className="px-3 py-3">Format / size</th><th className="px-3 py-3">Verification</th><th className="px-3 py-3">Archive checksum</th><th className="px-3 py-3" /></tr></thead><tbody>{recent.map(archive => <tr key={archive.id} className="border-t border-white/[.055] text-slate-300"><td className="px-4 py-3"><strong className="font-mono text-[11px] text-slate-200">{archive.exportId}</strong><span className={archive.status === "verified" ? "ml-2 rounded border border-emerald-300/20 bg-emerald-300/[.06] px-1.5 py-0.5 text-[9px] font-semibold text-emerald-200" : "ml-2 rounded border border-rose-300/20 bg-rose-300/[.06] px-1.5 py-0.5 text-[9px] font-semibold text-rose-200"}>{archive.status}</span></td><td className="px-3 py-3 text-[10px] text-slate-400">{local(archive.createdAt)}<span className="mt-1 block text-slate-500">Until {local(archive.retentionUntil)}</span></td><td className="px-3 py-3 font-mono text-[10px]">{archive.archiveFormat} · {bytes(archive.archiveSizeBytes)}</td><td className="px-3 py-3"><span className="inline-flex items-center gap-1 text-[10px] text-emerald-200"><CheckCircle2 className="h-3.5 w-3.5" />{archive.verification?.valid ? "counts + relationships match" : "not verified"}</span></td><td className="px-3 py-3 font-mono text-[10px] text-slate-400">{archive.archiveChecksum ? `${archive.archiveChecksum.slice(0, 16)}…` : "Unavailable"}</td><td className="px-3 py-3 text-right"><Button variant="outline" size="sm" disabled={archive.status !== "verified" || download.isPending} onClick={() => download.mutate({ archiveId: archive.id })} className="border-white/[.1] bg-white/[.03] text-xs text-slate-200 hover:bg-white/[.08]"><Download className="mr-1.5 h-3.5 w-3.5" />Download</Button></td></tr>)}{!recent.length ? <tr><td colSpan={6} className="px-4 py-10 text-center text-slate-500">No recovery archive exists yet. Creating an archive does not run Research Lab, score assets, or alter ingestion schedules.</td></tr> : null}</tbody></table></div>}</section><section className="grid gap-3 lg:grid-cols-3"><Cell title="Included" detail="Historical lineage, context, universe snapshots, ingestion evidence, owner-scoped research/cost records, schema DDL, manifest, checksums." /><Cell title="Restore validation" detail="Archive is reconstructed in an isolated in-memory validator; source and restored counts, checksums, and foreign-key relationships are compared." /><Cell title="Manual recovery" detail="Provision compatible MySQL, apply captured DDL, import JSON in dependency order, reconnect code/secrets, recreate or reattach platform schedules." /></section></div></ScrollArea>}</DialogContent></Dialog>;
client/src/components/crypto/AssetIntelligencePanel.tsx:2:import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
client/src/components/crypto/AssetIntelligencePanel.tsx:29:  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="h-[min(92vh,940px)] max-w-7xl border-white/[.1] bg-[#08101d] p-0 text-slate-100 sm:rounded-xl"><DialogHeader className="border-b border-white/[.07] px-5 py-4 sm:px-7"><div className="flex flex-wrap items-start justify-between gap-4"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-cyan-300/[.12] font-mono text-sm font-bold text-cyan-100">{row?.asset.symbol.slice(0, 2) ?? "—"}</div><div><DialogTitle className="text-lg">{row?.asset.name ?? "Asset Intelligence"} <span className="font-mono text-cyan-200">{row?.asset.symbol}</span></DialogTitle><DialogDescription className="mt-1 text-xs text-slate-500">Existing production score, validated market evidence, and an additive current Swing setup interpretation. No second score or automatic trade is created.</DialogDescription></div></div><div className="flex flex-wrap gap-2 text-[10px] font-semibold uppercase tracking-[.12em]"><Tag label={`OHLCV ${data?.header.ohlcvState ?? "UNAVAILABLE"}`} tone={data?.header.ohlcvState === "CURRENT" ? "emerald" : data?.header.ohlcvState === "UNAVAILABLE" ? "rose" : "amber"} /><Tag label={data?.header.ohlcvProvider ?? "Provider unavailable"} tone="cyan" /></div></div></DialogHeader><ScrollArea className="h-[calc(min(92vh,940px)-92px)]">{!row ? <Unavailable message="Select an asset from the dashboard or scanner to inspect its existing score evidence." /> : detail.isLoading ? <Loading /> : detail.error ? <Unavailable message={detail.error.message} /> : data ? <div className="space-y-6 p-5 sm:p-7"><section className="grid gap-4 xl:grid-cols-[1.45fr_.85fr]"><div className="rounded-2xl border border-cyan-300/15 bg-cyan-300/[.035] p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><span className="text-[10px] font-semibold uppercase tracking-[.18em] text-cyan-200">Opportunity Score — single source of truth</span><div className="mt-2 flex items-end gap-2"><strong className="font-mono text-5xl tracking-tight text-cyan-100">{score?.score ?? "—"}</strong><span className="mb-1 font-mono text-sm text-slate-500">/100</span></div><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-300">{score?.explanation ?? "UNAVAILABLE — current validated inputs did not produce an Opportunity Score."}</p></div><div className="grid min-w-[210px] grid-cols-2 gap-2"><Mini label="Confidence" value={score?.confidence === undefined ? "UNAVAILABLE" : `${score.confidence}/100`} /><Mini label="Setup" value={score?.setupType ?? "UNAVAILABLE"} /><Mini label="Risk" value={score?.riskLevel?.toUpperCase() ?? "UNAVAILABLE"} /><Mini label="Primary TF" value={(data.header.primaryTimeframe ?? timeframe).toUpperCase()} /></div></div><div className="mt-5 grid gap-2 sm:grid-cols-3"><Mini label="Current price" value={row.asset.price === null ? "UNAVAILABLE" : currency.format(row.asset.price)} /><Mini label="24h change" value={display(row.asset.change24h, "%")} /><Mini label="Data timestamp" value={time(data.header.marketTimestamp)} /></div></div><section className="rounded-2xl border border-white/[.07] bg-white/[.02] p-5"><span className="text-[10px] font-semibold uppercase tracking-[.17em] text-slate-500">Provenance</span><div className="mt-3 space-y-3 text-xs"><Info label="Market provider" value={data.header.marketProvider} /><Info label="Market state" value={data.header.marketState} /><Info label="OHLCV provider" value={data.header.ohlcvProvider ?? "UNAVAILABLE"} /><Info label="OHLCV timestamp" value={time(data.header.ohlcvTimestamp)} /><Info label="Timeframe" value={timeframe.toUpperCase()} /><Info label="Normalization" value={data.header.normalizationVersion ?? "UNAVAILABLE"} /></div></section></section><Tabs defaultValue="why" className="space-y-4"><TabsList className="h-auto w-full justify-start overflow-x-auto rounded-xl border border-white/[.07] bg-white/[.02] p-1"><TabsTrigger value="why">Why this score?</TabsTrigger><TabsTrigger value="technical">Technical</TabsTrigger><TabsTrigger value="chart">Chart</TabsTrigger><TabsTrigger value="risk">Risk & context</TabsTrigger><TabsTrigger value="data">Data status</TabsTrigger></TabsList><TabsContent value="why" className="space-y-4"><CurrentSetup setup={data.currentSetup as Setup} row={row} online={online} onPaperTrade={() => onPaperTrade(row, "SWING")} /><section className="grid gap-3 lg:grid-cols-3">{categories.map(category => <EvidenceGroup key={category.label} {...category} />)}</section><section className="rounded-xl border border-amber-300/15 bg-amber-300/[.035] p-4"><div className="flex gap-2"><FileWarning className="mt-0.5 h-4 w-4 shrink-0 text-amber-200" /><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-amber-200">Unavailable / remaining score conditions</span>{data.explainability.unavailable.length ? <ul className="mt-2 space-y-1.5 text-xs leading-5 text-amber-100/75">{data.explainability.unavailable.map(item => <li key={item}>{item}</li>)}</ul> : <p className="mt-2 text-xs text-amber-100/75">No current missing conditions were returned by the existing Opportunity Engine.</p>}</div></div></section></TabsContent><TabsContent value="technical" className="space-y-4"><TimeframePicker timeframe={timeframe} setTimeframe={setTimeframe} /><MultiTimeframeMatrix rows={data.technical.matrix} /><section className="grid gap-3 md:grid-cols-2"><MetricBlock label="RSI 14" value={display(data.technical.selectedAnalysis?.rsi)} detail="Existing completed-bar calculation" /><MetricBlock label="MACD histogram" value={display(data.technical.selectedAnalysis?.macdHistogram)} detail="Existing completed-bar calculation" /><MetricBlock label="EMA20 / EMA50 / EMA200" value={`${display(data.technical.selectedAnalysis?.ema20)} / ${display(data.technical.selectedAnalysis?.ema50)} / ${display(data.technical.selectedAnalysis?.ema200)}`} detail="Existing EMA calculations." /><MetricBlock label="ATR / volume" value={`${display(data.technical.selectedAnalysis?.atrPercent, "%")} / ${display(data.technical.selectedAnalysis?.volumeExpansion, "×")}`} detail="Existing ATR percent and volume expansion." /></section><SignalChecklist rows={data.technical.checklist} /></TabsContent><TabsContent value="chart" className="space-y-4"><TimeframePicker timeframe={timeframe} setTimeframe={setTimeframe} /><TechnicalChart points={data.technical.chart.candles} /><p className="text-[11px] leading-5 text-slate-500">Chart values use the selected validated candle series. Overlays use existing EMA20, EMA50, and EMA200 calculations; no alternative indicator is inferred.</p></TabsContent><TabsContent value="risk" className="grid gap-4 lg:grid-cols-2"><section className="rounded-xl border border-white/[.07] bg-white/[.02] p-4"><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-rose-200">Risk</span><div className="mt-3 grid grid-cols-2 gap-2"><Mini label="ATR" value={display(data.risk.atrPercent, "%")} /><Mini label="Volatility state" value={data.risk.volatilityState} /><Mini label="Support" value="UNAVAILABLE" /><Mini label="Resistance" value="UNAVAILABLE" /><Mini label="Stop / target" value="UNAVAILABLE" /><Mini label="R:R" value="UNAVAILABLE" /></div><p className="mt-3 text-[11px] leading-5 text-slate-500">The score engine does not create trade levels. Current Setup shows validated technical levels only when its separate existing Swing plan is Qualified.</p></section><section className="rounded-xl border border-white/[.07] bg-white/[.02] p-4"><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Market context</span><div className="mt-3 space-y-3"><Info label="Market regime" value={data.context.marketRegime ? `${data.context.marketRegime.classification} · ${data.context.marketRegime.score}/100` : "UNAVAILABLE"} /><Info label="BTC dominance" value={display(data.context.marketRegime?.btcDominance, "%")} /><Info label="Breadth" value={display(data.context.marketRegime?.breadth, "%")} /><Info label="Sector" value={data.context.sector} /><Info label="Sector relative strength" value={display(data.context.sectorRelativeStrength)} /><Info label="Market momentum" value={display(data.context.marketMomentum, "/20")} /></div></section><section className="lg:col-span-2 rounded-xl border border-amber-300/15 bg-amber-300/[.035] p-4"><div className="flex gap-2"><ShieldAlert className="mt-0.5 h-4 w-4 text-amber-200" /><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-amber-200">Catalyst / rumor</span><p className="mt-1 text-xs leading-5 text-amber-100/75">UNAVAILABLE — no verified catalyst, fact, signal, or rumor source is persisted in the current production data model. This has no influence on the displayed technical analysis.</p></div></div></section></TabsContent><TabsContent value="data"><section className="rounded-xl border border-white/[.07] bg-white/[.02] p-4"><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Source evidence</span><div className="mt-3 grid gap-2 md:grid-cols-2">{data.provenance.dataStatuses.map((status, index) => <div key={`${status.source}-${status.timeframe}-${index}`} className="rounded-lg border border-white/[.07] bg-black/10 p-3 text-xs"><div className="flex items-center justify-between gap-2"><strong className="text-slate-200">{status.provider ?? status.source}</strong><span className={status.status === "live" ? "text-emerald-200" : status.status === "stale" ? "text-amber-200" : "text-rose-200"}>{status.status.toUpperCase()}</span></div><p className="mt-1 text-[11px] text-slate-500">{status.capability ?? "OHLCV"} · {status.timeframe?.toUpperCase() ?? "—"} · {time(status.fetchedAt)}</p><p className="mt-2 text-[11px] leading-4 text-slate-400">{status.message ?? status.errorClass ?? "Validated current source"}</p></div>)}</div></section></TabsContent></Tabs></div> : null}</ScrollArea></DialogContent></Dialog>;
client/src/components/crypto/AssetIntelligencePanel.tsx:32:function CurrentSetup({ setup, row, onPaperTrade, online }: { setup: Setup; row: ScannerRow; onPaperTrade: () => void; online: boolean }) { const qualified = setup.status === "QUALIFIED" && setup.paperTradeEligible; const plan = setup.readinessPlan; const conditional = plan.availability !== "UNAVAILABLE"; const tone = qualified ? "border-emerald-300/20 bg-emerald-300/[.04]" : setup.status === "DATA UNAVAILABLE" ? "border-slate-400/20 bg-slate-400/[.04]" : setup.status === "NO TRADE" ? "border-rose-300/20 bg-rose-300/[.04]" : "border-amber-300/20 bg-amber-300/[.04]"; return <section className={`rounded-xl border p-4 ${tone}`}><div className="flex flex-wrap items-start justify-between gap-3"><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Setup plan · validated Swing evidence</span><div className="mt-2 flex flex-wrap items-center gap-2"><strong className="text-sm text-slate-100">{setup.status}</strong><span className="rounded-full border border-white/[.1] px-2 py-0.5 text-[9px] text-slate-400">{setup.maturity}</span><span className="rounded-full border border-violet-300/20 px-2 py-0.5 text-[9px] text-violet-100">{setup.setupReadiness.state} · {setup.setupReadiness.score ?? "UNAVAILABLE"}{setup.setupReadiness.score === null ? "" : "/100"}</span>{setup.regime.restricted ? <span className="rounded-full border border-rose-300/20 px-2 py-0.5 text-[9px] text-rose-200">RISK OFF RESTRICTED</span> : null}</div><p className="mt-2 max-w-3xl text-xs leading-5 text-slate-300">{setup.exactReason}</p></div>{qualified ? <Button onClick={onPaperTrade} disabled={!online || row.asset.price === null} className="bg-emerald-300 text-slate-950 hover:bg-emerald-200 disabled:bg-slate-600"><WalletCards className="mr-2 h-4 w-4" />Paper trade qualified Swing</Button> : null}</div><div className="mt-3 grid gap-2 sm:grid-cols-2 lg:grid-cols-4"><Mini label="Setup readiness" value={setup.setupReadiness.explanation} /><Mini label="Provider / freshness" value={`${setup.provider ?? "UNAVAILABLE"} · ${setup.freshness}`} /><Mini label="Timeframes" value={`${setup.timeframes.execution.toUpperCase()} / ${setup.timeframes.confirmation.toUpperCase()} / ${setup.timeframes.context.toUpperCase()}`} /><Mini label="Validation" value={setup.validationStatus ?? "UNAVAILABLE"} /></div>{qualified ? <div className="mt-3 grid gap-2 sm:grid-cols-3"><Mini label="Preferred entry" value={setup.sourcePlan.entryZone ? currency.format(setup.sourcePlan.entryZone.preferred) : "UNAVAILABLE"} /><Mini label="Stop" value={setup.sourcePlan.stop ? currency.format(setup.sourcePlan.stop.price) : "UNAVAILABLE"} /><Mini label="Target" value={setup.sourcePlan.targets[0] ? currency.format(setup.sourcePlan.targets[0].price) : "UNAVAILABLE"} /></div> : <div className="mt-3 rounded-lg border border-white/[.08] bg-black/10 p-3"><p className="text-[9px] font-semibold uppercase tracking-[.14em] text-amber-200">Not yet eligible for Paper Trading</p><p className="mt-1 text-xs leading-5 text-slate-400">Potential and Watch remain monitoring states; server-side qualification and explicit confirmation are still required.</p>{setup.whyInteresting.length ? <ul className="mt-3 space-y-1 text-xs leading-5 text-emerald-100/85">{setup.whyInteresting.map(reason => <li key={reason}>• Working: {reason}</li>)}</ul> : null}{setup.confirmationRequirements.length ? <ul className="mt-3 space-y-1 text-xs leading-5 text-amber-100/85">{setup.confirmationRequirements.map(reason => <li key={reason}>• Waiting for: {reason}</li>)}</ul> : null}{conditional ? <div className="mt-3 border-t border-white/[.07] pt-3"><p className="text-[9px] font-semibold uppercase tracking-[.14em] text-violet-200">Conditional technical plan — not a trade instruction</p><p className="mt-1 text-xs leading-5 text-slate-400">{plan.reason}</p><div className="mt-2 grid gap-2 sm:grid-cols-2 lg:grid-cols-4">{plan.entryZone ? <Mini label="Entry zone" value={`${currency.format(plan.entryZone.low)} – ${currency.format(plan.entryZone.high)}`} /> : null}{plan.invalidation ? <Mini label="Technical invalidation" value={currency.format(plan.invalidation.price)} /> : null}{plan.targets.map(target => <Mini key={target.label} label={`${target.label} · ${target.rMultiple === null ? "R unavailable" : `${target.rMultiple}R`}`} value={currency.format(target.price)} />)}{plan.rewardRisk !== null ? <Mini label="Current first R:R" value={`${plan.rewardRisk}:1`} /> : null}</div>{plan.confirmation ? <p className="mt-2 text-xs leading-5 text-cyan-100/85"><strong>Confirmation:</strong> {plan.confirmation}</p> : null}<p className="mt-2 text-xs leading-5 text-rose-100/85"><strong>Invalidation:</strong> {setup.invalidationExplanation}</p></div> : <p className="mt-3 text-xs leading-5 text-slate-500">Entry zone, targets, and invalidation remain unavailable because the existing validated setup engine did not derive all required evidence.</p>}<p className="mt-3 text-[10px] leading-4 text-slate-500">Trade Health: {plan.health.state}. {plan.health.reason} Provenance timestamp: {time(setup.dataTimestamp)}.</p></div>}</section>; }
client/src/components/crypto/AssetIntelligencePanel.tsx:35:function MultiTimeframeMatrix({ rows }: { rows: Array<{ timeframe: string; trend: string; rsi: number | null; macdHistogram: number | null; maStructure: string; volume: number | null; overall: string; score: number | null; maxScore: number | null }> }) { return <div className="overflow-x-auto rounded-xl border border-white/[.07] bg-white/[.02] p-4"><p className="mb-3 text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Multi-timeframe explanation matrix</p><table className="w-full min-w-[760px] text-left text-xs"><thead className="text-[9px] uppercase tracking-[.14em] text-slate-500"><tr><th className="pb-2">Metric</th>{rows.map(row => <th key={row.timeframe} className="pb-2 font-mono">{row.timeframe.toUpperCase()}</th>)}</tr></thead><tbody>{[{ label: "Trend", pick: (row: typeof rows[number]) => row.trend }, { label: "RSI", pick: (row: typeof rows[number]) => display(row.rsi) }, { label: "MACD", pick: (row: typeof rows[number]) => display(row.macdHistogram) }, { label: "MA structure", pick: (row: typeof rows[number]) => row.maStructure }, { label: "Volume", pick: (row: typeof rows[number]) => display(row.volume, "×") }, { label: "Overall", pick: (row: typeof rows[number]) => row.score === null ? "UNAVAILABLE" : `${row.overall} · ${row.score}/${row.maxScore}` }].map(metric => <tr key={metric.label} className="border-t border-white/[.055]"><td className="py-2.5 font-medium text-slate-300">{metric.label}</td>{rows.map(row => <td key={row.timeframe} className="py-2.5 pr-3 text-slate-400">{metric.pick(row)}</td>)}</tr>)}</tbody></table><p className="mt-3 text-[10px] text-slate-500">This matrix explains existing per-timeframe inputs; it is not a second production score. UNAVAILABLE is not bearish.</p></div>; }
client/src/components/crypto/AssetIntelligencePanel.tsx:36:function SignalChecklist({ rows }: { rows: Array<{ label: string; state: string; detail: string; contribution: number; maximum: number }> }) { return <section className="rounded-xl border border-white/[.07] bg-white/[.02] p-4"><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Signal checklist</span><div className="mt-3 grid gap-2 md:grid-cols-2">{rows.length ? rows.map(item => <div key={item.label} className="rounded-lg border border-white/[.06] bg-black/10 p-3"><div className="flex items-center justify-between gap-2"><span className="text-xs font-medium text-slate-200">{item.label}</span><span className={item.state === "CONFIRMED" ? "text-emerald-200" : item.state === "NOT CONFIRMED" ? "text-rose-200" : "text-slate-400"}>{item.state}</span></div><p className="mt-1 text-[11px] leading-4 text-slate-500">{item.detail}</p><p className="mt-2 font-mono text-[10px] text-slate-400">{item.contribution}/{item.maximum} existing contribution</p></div>) : <p className="text-xs text-slate-500">UNAVAILABLE — no current technical analysis exists for this timeframe.</p>}</div></section>; }
client/src/components/crypto/AssetIntelligencePanel.tsx:42:function Unavailable({ message }: { message: string }) { return <div className="grid min-h-[500px] place-items-center px-6 text-center"><div><CircleHelp className="mx-auto h-6 w-6 text-slate-600" /><p className="mt-3 text-sm font-medium text-slate-300">Asset Intelligence unavailable</p><p className="mx-auto mt-2 max-w-md text-xs leading-5 text-slate-500">{message}</p></div></div>; }
client/src/components/crypto/TechnicalChart.tsx:21:  return <div className={cn("overflow-hidden rounded-xl border border-white/[.07] bg-[#070d18]", className)}><div className="flex flex-wrap items-center justify-between gap-2 border-b border-white/[.06] px-4 py-3"><div><p className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Validated candle chart</p><p className="mt-1 text-xs text-slate-500">Candles with the same EMA and completed-bar inputs used by the technical engine.</p></div><div className="flex flex-wrap gap-2 text-[9px] font-semibold uppercase tracking-[.12em]"><span className="text-cyan-200">EMA20</span><span className="text-amber-200">EMA50</span><span className="text-fuchsia-200">EMA200</span></div></div><div className="overflow-x-auto"><svg viewBox={`0 0 ${width} ${height}`} className="min-w-[680px] w-full" role="img" aria-label="Validated candle chart with EMA overlays"><rect width={width} height={height} fill="#070d18" />{[0, 0.25, 0.5, 0.75, 1].map(ratio => <g key={ratio}><line x1={padding.left} x2={width - padding.right} y1={padding.top + ratio * (height - padding.top - padding.bottom)} y2={padding.top + ratio * (height - padding.top - padding.bottom)} stroke="rgba(148,163,184,.12)" /><text x={6} y={padding.top + ratio * (height - padding.top - padding.bottom) + 4} fill="#64748b" fontSize="10">{format(max - ratio * range, 4)}</text></g>)}{points.map((point, index) => { const bullish = point.close >= point.open; const candleWidth = Math.max(2, (width - padding.left - padding.right) / points.length * 0.56); return <g key={point.openTime}><line x1={x(index)} x2={x(index)} y1={y(point.high)} y2={y(point.low)} stroke={bullish ? "#34d399" : "#fb7185"} strokeWidth="1" /><rect x={x(index) - candleWidth / 2} y={y(Math.max(point.open, point.close))} width={candleWidth} height={Math.max(1, Math.abs(y(point.open) - y(point.close)))} fill={bullish ? "#34d399" : "#fb7185"} opacity=".82" /></g> })}<polyline points={line("ema20")} fill="none" stroke="#67e8f9" strokeWidth="1.6" /><polyline points={line("ema50")} fill="none" stroke="#fbbf24" strokeWidth="1.4" /><polyline points={line("ema200")} fill="none" stroke="#e879f9" strokeWidth="1.35" /></svg></div><div className="grid grid-cols-2 gap-px border-t border-white/[.06] bg-white/[.06] sm:grid-cols-4"><Stat label="Close" value={format(latest.close, 5)} /><Stat label="Volume" value={format(latest.volume, 2)} /><Stat label="RSI 14" value={format(latest.rsi)} /><Stat label="MACD histogram" value={format(latest.macdHistogram, 4)} /></div></div>;
client/src/components/crypto/ResearchRunWorkspace.tsx:12:  return <div className="space-y-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><span className="text-[10px] font-semibold uppercase tracking-[.18em] text-fuchsia-200">Persisted point-in-time research</span><h3 className="mt-1 text-lg font-semibold">{detail.name}</h3><p className="mt-1 text-[11px] text-slate-500">Run #{detail.id} · {detail.protocolVersion} · fingerprint {detail.configurationFingerprint.slice(0, 12)}…</p></div><div className="flex gap-2"><Button variant="outline" size="sm" onClick={() => onDownload("csv")} className="border-white/[.1] bg-white/[.03] text-xs text-slate-200"><Download className="mr-1.5 h-3.5 w-3.5" />CSV</Button><Button variant="outline" size="sm" onClick={() => onDownload("json")} className="border-white/[.1] bg-white/[.03] text-xs text-slate-200"><Download className="mr-1.5 h-3.5 w-3.5" />JSON</Button></div></div><Tabs defaultValue="overview" className="space-y-4"><TabsList className="h-auto w-full justify-start overflow-x-auto rounded-xl border border-white/[.07] bg-white/[.02] p-1"><TabsTrigger value="overview">Overview</TabsTrigger><TabsTrigger value="variables">Variables</TabsTrigger><TabsTrigger value="methodology">Methodology</TabsTrigger><TabsTrigger value="dataset">Dataset</TabsTrigger><TabsTrigger value="results">Results</TabsTrigger><TabsTrigger value="calibration">Calibration</TabsTrigger><TabsTrigger value="segments">Segments</TabsTrigger><TabsTrigger value="trades">Trades</TabsTrigger><TabsTrigger value="export">Export</TabsTrigger></TabsList><TabsContent value="overview" className="space-y-4"><div className="rounded-xl border border-amber-300/15 bg-amber-300/[.035] p-4"><div className="flex gap-2"><ShieldAlert className="mt-0.5 h-4 w-4 shrink-0 text-amber-200" /><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-amber-200">Research boundary</span><p className="mt-1 text-xs leading-5 text-amber-100/75">Technical-core OHLCV research is separate from live production scoring. Completed bars, next-open entry, and the time split are preserved by the stored protocol.</p></div></div></div><div className="grid grid-cols-2 gap-2 sm:grid-cols-4"><Metric label="Signals" value={String(aggregate?.metrics.signalCount ?? "UNAVAILABLE")} /><Metric label="Win rate" value={display(aggregate?.metrics.winRate, "%")} /><Metric label="Average return" value={display(aggregate?.metrics.averageReturn, "%")} /><Metric label="Max drawdown" value={display(aggregate?.metrics.maximumDrawdown, "%")} /><Metric label="Median return" value={display(aggregate?.metrics.medianReturn, "%")} /><Metric label="Profit factor" value={display(aggregate?.metrics.profitFactor)} /><Metric label="Average R" value={display(aggregate?.metrics.averageR)} /><Metric label="Expectancy" value={display(aggregate?.metrics.expectancy, "%")} /></div><Candidate status={candidate?.status} reason={candidate?.reason} /></TabsContent><TabsContent value="variables"><BoundedCard title="Declared variables" text="The immutable configuration is represented by the protocol version and configuration fingerprint for this saved run. Detailed per-field configuration is available in the JSON export; it is not reconstructed from current settings." value={`${detail.protocolVersion} · ${detail.configurationFingerprint}`} /></TabsContent><TabsContent value="methodology"><div className="grid gap-3 md:grid-cols-2"><BoundedCard title="Look-ahead protection" text="Completed bars are used and the declared entry convention is the next available open." /><BoundedCard title="Time split" text="The earlier period is in-sample and the later period is out-of-sample under the persisted protocol." /><BoundedCard title="Threshold policy" text="Score and confidence calibration buckets are measured as persisted; no threshold is tuned in this workspace." /><BoundedCard title="Costs and assumptions" text="Only values retained by the experiment record are available. Any missing treatment is shown as UNAVAILABLE rather than inferred." /></div></TabsContent><TabsContent value="dataset"><BoundedCard title="Dataset / provenance" text="Dataset identifiers and source coverage are retained with the persisted experiment payload and export. This summary does not substitute a current live scanner state for a historical run." value="Use JSON export for full immutable provenance payload" /></TabsContent><TabsContent value="results"><ResultOutcome aggregate={aggregate} /></TabsContent><TabsContent value="calibration"><ResultSection title="Score and confidence calibration" detail="Fixed buckets are measured as-is; no thresholds are tuned to force monotonicity." rows={calibration} /></TabsContent><TabsContent value="segments"><ResultSection title="Time split, regime, and sector segments" rows={segments} /></TabsContent><TabsContent value="trades"><BoundedCard title="Trade-level inspection" text="UNAVAILABLE — the existing experiment presentation persists aggregate and segment outcome evidence, not a row-level simulated-trade ledger. The workspace does not fabricate trades from aggregates." /></TabsContent><TabsContent value="export"><div className="rounded-xl border border-fuchsia-300/15 bg-fuchsia-300/[.035] p-5"><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-fuchsia-200">Exports</span><p className="mt-2 text-xs leading-5 text-slate-400">Download the exact persisted experiment data and metadata for external inspection. Export does not rerun the experiment.</p><div className="mt-4 flex gap-2"><Button onClick={() => onDownload("csv")} variant="outline" className="border-white/[.1] text-slate-200">CSV</Button><Button onClick={() => onDownload("json")} className="bg-fuchsia-300 text-slate-950 hover:bg-fuchsia-200">JSON with provenance</Button></div></div></TabsContent></Tabs></div>;
client/src/components/crypto/ResearchRunWorkspace.tsx:14:function ResultOutcome({ aggregate }: { aggregate: Result | undefined }) { return aggregate ? <section className="rounded-xl border border-white/[.07] bg-white/[.02] p-4"><div className="flex items-center justify-between gap-3"><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-slate-500">Selected experiment outcome</span><p className="mt-1 text-xs text-slate-400">{aggregate.reason}</p></div><Status status={aggregate.evidenceStatus} /></div><div className="mt-3 grid gap-2 text-[10px] text-slate-500 sm:grid-cols-4">{aggregate.metrics.outcomeHorizons.map(horizon => <div key={horizon.label} className="rounded-lg border border-white/[.06] px-3 py-2"><span>{horizon.label} · n={horizon.observationCount}</span><strong className="mt-1 block font-mono text-xs text-slate-200">{display(horizon.averageReturn, "%")}</strong></div>)}</div></section> : <BoundedCard title="Results" text="UNAVAILABLE — no aggregate result is stored for this experiment." />; }
client/src/components/crypto/ResearchRunWorkspace.tsx:19:function ResultSection({ title, detail, rows }: { title: string; detail?: string; rows: Result[] }) { return <section><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-fuchsia-200">{title}</span>{detail ? <p className="mt-1 text-[11px] text-slate-500">{detail}</p> : null}</div><div className="mt-3 grid gap-2 md:grid-cols-2">{rows.length ? rows.map(result => <div key={result.id} className="rounded-xl border border-white/[.07] bg-white/[.02] p-3"><div className="flex items-start justify-between gap-2"><div><span className="text-[9px] font-semibold uppercase tracking-[.14em] text-slate-500">{result.dimension.replaceAll("_", " ")}</span><p className="mt-1 text-xs font-semibold text-slate-200">{result.dimensionKey}</p></div><Status status={result.evidenceStatus} /></div><div className="mt-3 grid grid-cols-3 gap-2 font-mono text-[10px]"><span className="text-slate-500">n <strong className="text-slate-200">{result.signalCount}</strong></span><span className="text-slate-500">win <strong className="text-slate-200">{display(result.metrics.winRate, "%")}</strong></span><span className="text-slate-500">avg <strong className="text-slate-200">{display(result.metrics.averageReturn, "%")}</strong></span></div><p className="mt-2 line-clamp-2 text-[10px] leading-4 text-slate-500">{result.reason}</p></div>) : <BoundedCard title="Segment evidence" text="UNAVAILABLE — no persisted segment rows are available for this run." />}</div></section>; }
client/src/components/crypto/PaperTradingWorkspace.tsx:28:  const { user, loading } = useAuth();
client/src/components/crypto/PaperTradingWorkspace.tsx:42:  const openTrade = trpc.crypto.openPaperTrade.useMutation({ onSuccess: async result => { await Promise.all([utils.crypto.paperPortfolio.invalidate(), utils.crypto.scanner.invalidate()]); toast.success("Simulated trade recorded", { description: `Entry ${result.entryPrice}; immutable entry snapshot retained. No real funds were used.` }); }, onError: error => toast.error("Paper trade was not recorded", { description: error.message }) });
client/src/components/crypto/PaperTradingWorkspace.tsx:43:  const closeTrade = trpc.crypto.closePaperTrade.useMutation({ onSuccess: async result => { await utils.crypto.paperPortfolio.invalidate(); toast.success("Simulated position closed", { description: `Realized P/L ${money.format(result.realizedPnl)}.` }); }, onError: error => toast.error("Paper trade was not closed", { description: error.message }) });
client/src/components/crypto/PaperTradingWorkspace.tsx:44:  const monitorTrade = trpc.crypto.recordPaperTradeMonitoring.useMutation({ onSuccess: async result => { await utils.crypto.paperPortfolio.invalidate(); toast.message("Trade Health refreshed", { description: result.recordedEvents.length ? `${result.recordedEvents.length} material monitoring event(s) recorded. No position was modified.` : "No material state transition was recorded." }); }, onError: error => toast.error("Trade Health could not be refreshed", { description: error.message }) });
client/src/components/crypto/PaperTradingWorkspace.tsx:45:  const refreshLowTimeframeHealth = trpc.crypto.refreshLowTimeframePaperTradeHealth.useMutation({ onSuccess: result => { setLowHealthObservation({ tradeId: selectedTradeId ?? -1, health: result.health as LowTimeframeHealth }); toast.message("Low-timeframe health refreshed", { description: "Manual observation only. No paper position, target, alert, or monitoring event was modified." }); }, onError: error => toast.error("Low-timeframe health could not be refreshed", { description: error.message }) });
client/src/components/crypto/PaperTradingWorkspace.tsx:49:  if (loading) return <WorkspaceLoading />;
client/src/components/crypto/PaperTradingWorkspace.tsx:57:    <PortfolioDashboard metrics={data?.metrics} counts={healthCounts} loading={portfolio.isLoading} />
client/src/components/crypto/PaperTradingWorkspace.tsx:63:function EntryConfirmation({ asset, setupMode, side, setSide, riskPercent, setRiskPercent, online, pending, onConfirm }: { asset: ScannerRow | null; setupMode?: "SCALP" | "SWING" | "LOW_TIMEFRAME_SCALPING"; side: "long" | "short"; setSide: (value: "long" | "short") => void; riskPercent: number; setRiskPercent: (value: number) => void; online: boolean; pending: boolean; onConfirm: () => void }) { if (!asset?.score || !setupMode) return <section className="rounded-2xl border border-dashed border-white/[.12] bg-white/[.015] p-6"><p className="text-sm font-semibold text-slate-200">No qualified paper-trade setup selected</p><p className="mt-2 text-xs leading-5 text-slate-500">Open a current Qualified Scalping, Swing, or validated low-timeframe setup first. Potential, Watch, No Trade, and Data Unavailable states cannot initiate a paper position.</p></section>; return <section className="rounded-2xl border border-emerald-300/20 bg-emerald-300/[.035] p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-[10px] font-semibold uppercase tracking-[.17em] text-emerald-200">Qualified paper trade confirmation</p><h3 className="mt-1 text-lg font-semibold">{asset.asset.symbol} · {setupMode} setup</h3><p className="mt-1 text-xs text-slate-400">Current price {asset.asset.price === null ? "UNAVAILABLE" : money.format(asset.asset.price)} · Opportunity {asset.score.score}/100 · Confidence {asset.score.confidence}/100. The server revalidates this qualified setup before recording anything.</p></div><span className="rounded-full border border-amber-300/20 bg-amber-300/[.07] px-3 py-1 text-[10px] font-semibold text-amber-100">SIMULATED ONLY · NO REAL FUNDS</span></div><div className="mt-4 grid gap-3 lg:grid-cols-[220px_180px_1fr]"><div className="flex rounded-lg border border-white/[.08] bg-black/15 p-1"><button onClick={() => setSide("long")} disabled={!online} className={`flex-1 rounded-md py-2 text-xs font-semibold disabled:opacity-45 ${side === "long" ? "bg-emerald-300/[.15] text-emerald-100" : "text-slate-500"}`}><ArrowUpRight className="mr-1 inline h-3.5 w-3.5" />LONG</button><button onClick={() => setSide("short")} disabled={!online} className={`flex-1 rounded-md py-2 text-xs font-semibold disabled:opacity-45 ${side === "short" ? "bg-rose-300/[.15] text-rose-100" : "text-slate-500"}`}><ArrowDownRight className="mr-1 inline h-3.5 w-3.5" />SHORT</button></div><label className="flex items-center justify-between rounded-lg border border-white/[.08] bg-black/15 px-3 text-xs text-slate-400">Risk <input type="number" min="0.1" max="5" step="0.1" value={riskPercent} disabled={!online} onChange={event => setRiskPercent(Number(event.target.value))} className="w-16 bg-transparent text-right font-mono text-emerald-100 outline-none disabled:opacity-45" />%</label><Button onClick={onConfirm} disabled={pending || asset.asset.price === null || !online} className="bg-emerald-300 text-slate-950 hover:bg-emerald-200 disabled:bg-slate-500">{pending ? <Loader2 className="mr-2 h-4 w-4 animate-spin" /> : <BadgeDollarSign className="mr-2 h-4 w-4" />}{online ? "CONFIRM PAPER TRADE" : "LIVE CONNECTION REQUIRED"}</Button></div></section>; }
client/src/components/crypto/PaperTradingWorkspace.tsx:65:function PortfolioDashboard({ metrics, counts, loading }: { metrics: Metrics | undefined; counts: Record<TradeHealth["state"], number>; loading: boolean }) { if (loading) return <div className="grid gap-3 sm:grid-cols-3 xl:grid-cols-6">{Array.from({ length: 12 }, (_, index) => <div key={index} className="h-20 animate-pulse rounded-xl border border-white/[.06] bg-white/[.025]" />)}</div>; if (!metrics) return null; const sampleSufficient = (metrics.closedPositions ?? 0) >= 2; const cards: Array<[string, string, "profit" | "loss" | "attention" | "neutral"]> = [["Current equity", money.format(metrics.currentEquity), metrics.totalPnl >= 0 ? "profit" : "loss"], ["Total P&L", money.format(metrics.totalPnl), metrics.totalPnl >= 0 ? "profit" : "loss"], ["Unrealized P&L", money.format(metrics.unrealizedPnl), metrics.unrealizedPnl >= 0 ? "profit" : "loss"], ["Open positions", String(metrics.openPositions), "attention"], ["Healthy", String(counts.HEALTHY), "profit"], ["Caution", String(counts.CAUTION), "attention"], ["Reversal risk", String(counts["REVERSAL RISK"]), "loss"], ["Invalidated", String(counts.INVALIDATED), "loss"], ["Win rate", sampleSufficient ? asValue(metrics.winRate, "%") : "INSUFFICIENT SAMPLE", "neutral"], ["Profit factor", sampleSufficient && metrics.profitFactor !== null ? asValue(metrics.profitFactor) : "INSUFFICIENT SAMPLE", "neutral"]]; return <section><div className="mb-3"><p className="text-[10px] font-semibold uppercase tracking-[.17em] text-emerald-200">Portfolio health</p><h3 className="mt-1 text-base font-semibold">Current simulated account state</h3></div><div className="grid gap-2 sm:grid-cols-3 xl:grid-cols-5">{cards.map(([label, value, tone]) => <Metric key={label} label={label} value={value} tone={tone} />)}</div></section>; }
client/src/components/crypto/PaperTradingWorkspace.tsx:67:function OpenPositions({ trades, online, closing, onInspect, onClose }: { trades: Trade[]; online: boolean; closing: boolean; onInspect: (id: number) => void; onClose: (id: number) => void }) { return <section className="rounded-2xl border border-white/[.07] bg-[#09101c] p-5"><div className="flex items-end justify-between"><div><p className="text-[10px] font-semibold uppercase tracking-[.17em] text-amber-200">Open positions</p><h3 className="mt-1 text-base font-semibold">Live simulated exposure</h3></div><span className="rounded-full border border-amber-300/20 px-2.5 py-1 text-[10px] text-amber-100">{trades.length} OPEN</span></div>{trades.length ? <div className="mt-4 space-y-2">{trades.map(trade => <article key={trade.id} className="grid gap-3 rounded-xl border border-white/[.07] bg-white/[.02] p-3 sm:grid-cols-[1fr_auto_auto_auto]"><button onClick={() => onInspect(trade.id)} className="text-left"><strong className="text-slate-100">{trade.entrySnapshot.asset?.symbol ?? trade.assetId.toUpperCase()}</strong><span className="ml-2 text-[10px] text-slate-500">{trade.side.toUpperCase()} · {trade.entrySnapshot.setupPlan?.mode ?? "GENERAL"}</span><span className={`ml-2 inline-flex rounded border px-1.5 py-0.5 text-[9px] font-semibold ${healthTone(trade.tradeHealth.state)}`}>{trade.tradeHealth.state}</span><span className="mt-1 block text-[10px] text-slate-500">Entry {money.format(trade.entryPrice)} · current {trade.currentPrice === null ? "UNAVAILABLE" : money.format(trade.currentPrice)}</span></button><MetricInline label="Unrealized" value={trade.unrealizedPnl === null ? "UNAVAILABLE" : money.format(trade.unrealizedPnl)} tone={pnlTone(trade.unrealizedPnl)} /><MetricInline label="Target path" value={trade.tradeHealth.targetPath.state} /><div className="flex items-center justify-end gap-2"><Button size="sm" variant="outline" onClick={() => onInspect(trade.id)} className="border-white/[.1] text-slate-300">Inspect</Button><Button size="sm" variant="outline" onClick={() => onClose(trade.id)} disabled={!online || closing} className="border-rose-300/25 text-rose-100">{online ? "Close" : "Offline"}</Button></div></article>)}</div> : <Empty title="No open positions" detail="Select a qualified asset to open a simulated Long or Short position." />}</section>; }
client/src/components/crypto/PaperTradingWorkspace.tsx:69:function TradeHistory({ trades, total, filters, setters, onInspect }: { trades: Trade[]; total: number; filters: { status: "all" | "open" | "closed"; side: "all" | "long" | "short"; result: "all" | "winning" | "losing" | "breakeven"; health: "all" | "healthy" | "caution" | "reversal-risk" | "invalidated" | "health-unknown"; strategy: "all" | "scalp" | "swing"; assetQuery: string }; setters: { setStatus: (value: "all" | "open" | "closed") => void; setSide: (value: "all" | "long" | "short") => void; setResult: (value: "all" | "winning" | "losing" | "breakeven") => void; setHealth: (value: "all" | "healthy" | "caution" | "reversal-risk" | "invalidated" | "health-unknown") => void; setStrategy: (value: "all" | "scalp" | "swing") => void; setAssetQuery: (value: string) => void }; onInspect: (id: number) => void }) { return <section className="rounded-2xl border border-white/[.07] bg-[#09101c] p-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-[10px] font-semibold uppercase tracking-[.17em] text-cyan-200">Trade history</p><h3 className="mt-1 text-base font-semibold">{trades.length} of {total} persistent records</h3></div><div className="flex flex-wrap gap-2"><Select value={filters.status} onChange={setters.setStatus} options={[["all", "All status"], ["open", "Open"], ["closed", "Closed"]]} /><Select value={filters.health} onChange={setters.setHealth} options={[["all", "All health"], ["healthy", "Healthy"], ["caution", "Caution"], ["reversal-risk", "Reversal risk"], ["invalidated", "Invalidated"], ["health-unknown", "Health unknown"]]} /><Select value={filters.strategy} onChange={setters.setStrategy} options={[["all", "All strategy"], ["scalp", "Scalping"], ["swing", "Swing"]]} /><Select value={filters.side} onChange={setters.setSide} options={[["all", "All direction"], ["long", "Long"], ["short", "Short"]]} /><Select value={filters.result} onChange={setters.setResult} options={[["all", "All results"], ["winning", "Winning"], ["losing", "Losing"], ["breakeven", "Breakeven"]]} /><input value={filters.assetQuery} onChange={event => setters.setAssetQuery(event.target.value)} placeholder="Search asset" className="rounded-md border border-white/[.08] bg-white/[.025] px-2 py-1.5 text-xs text-slate-200 outline-none placeholder:text-slate-600" /></div></div>{trades.length ? <><div className="mt-4 hidden overflow-x-auto md:block"><table className="w-full min-w-[1050px] text-left text-xs"><thead className="border-y border-white/[.06] text-[9px] uppercase tracking-[.13em] text-slate-500"><tr><th className="py-3">Asset / strategy</th><th>Entry / current</th><th>Health</th><th>Target path</th><th>P&L</th><th>Status</th><th /></tr></thead><tbody>{trades.map(trade => <tr key={trade.id} className="cursor-pointer border-b border-white/[.055] hover:bg-white/[.025]" onClick={() => onInspect(trade.id)}><td className="py-3"><strong>{trade.entrySnapshot.asset?.symbol ?? trade.assetId.toUpperCase()}</strong><span className="ml-2 text-[10px] text-slate-500">{trade.side.toUpperCase()} · {trade.entrySnapshot.setupPlan?.mode ?? "GENERAL"}</span></td><td className="font-mono text-slate-400">{money.format(trade.entryPrice)} / {trade.currentPrice === null ? "UNAVAILABLE" : money.format(trade.currentPrice)}</td><td><HealthBadge state={trade.tradeHealth.state} /></td><td>{trade.tradeHealth.targetPath.state}</td><td className={pnlTone(trade.status === "open" ? trade.unrealizedPnl : trade.realizedPnl)}>{trade.status === "open" ? asValue(trade.unrealizedPnl) : asValue(trade.realizedPnl)}</td><td>{trade.status.toUpperCase()}</td><td>›</td></tr>)}</tbody></table></div><div className="mt-4 space-y-2 md:hidden">{trades.map(trade => <button key={trade.id} onClick={() => onInspect(trade.id)} className="w-full rounded-xl border border-white/[.07] bg-white/[.02] p-3 text-left"><div className="flex justify-between gap-3"><span className="font-semibold">{trade.entrySnapshot.asset?.symbol ?? trade.assetId.toUpperCase()} · {trade.side.toUpperCase()}</span><span className={pnlTone(trade.status === "open" ? trade.unrealizedPnl : trade.realizedPnl)}>{trade.status === "open" ? asValue(trade.unrealizedPnl) : asValue(trade.realizedPnl)}</span></div><div className="mt-2 flex flex-wrap gap-2"><HealthBadge state={trade.tradeHealth.state} /><span className="text-[10px] text-slate-500">{trade.entrySnapshot.setupPlan?.mode ?? "GENERAL"} · {trade.tradeHealth.targetPath.state}</span></div></button>)}</div></> : <Empty title="No paper trades match these filters" detail="Filters are presentation-only. Clear a filter or select a qualified asset to record a new simulated position." />}</section>; }
client/src/components/crypto/PaperTradingWorkspace.tsx:73:    <section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Entry" value={money.format(trade.entryPrice)} tone="neutral" /><Metric label="Current price" value={trade.currentPrice === null ? "UNAVAILABLE" : money.format(trade.currentPrice)} tone="attention" /><Metric label="Unrealized P&L" value={trade.unrealizedPnl === null ? "UNAVAILABLE" : money.format(trade.unrealizedPnl)} tone={trade.unrealizedPnl === null ? "neutral" : trade.unrealizedPnl >= 0 ? "profit" : "loss"} /><Metric label="Trade Health" value={trade.tradeHealth.state} tone={trade.tradeHealth.state === "HEALTHY" ? "profit" : trade.tradeHealth.state === "CAUTION" ? "attention" : trade.tradeHealth.state === "HEALTH UNKNOWN" ? "neutral" : "loss"} /></section>
client/src/components/crypto/PaperTradingWorkspace.tsx:74:    <section className="grid gap-5 xl:grid-cols-2"><EntryState snapshot={snapshot} /><CurrentState trade={trade} /></section>
client/src/components/crypto/PaperTradingWorkspace.tsx:77:    <section className="grid gap-4 xl:grid-cols-2"><Levels plan={plan} /><section className="rounded-2xl border border-emerald-300/15 bg-emerald-300/[.035] p-5"><p className="text-[10px] font-semibold uppercase tracking-[.17em] text-emerald-200">Why this trade?</p><h3 className="mt-1 text-base font-semibold">Immutable reasons captured at entry</h3><p className="mt-1 text-xs leading-5 text-slate-500">Current state never rewrites entry evidence. Legacy records without a setup snapshot remain explicitly unavailable.</p><div className="mt-4 grid gap-2 md:grid-cols-2">{reasons.length ? reasons.map(reason => <div key={reason.label} className="rounded-xl border border-white/[.07] bg-black/10 p-3"><div className="flex justify-between gap-3"><strong className="text-xs text-slate-200">{reason.label}</strong><span className="font-mono text-xs text-emerald-200">{reason.score}/{reason.maxScore}</span></div><p className="mt-1 text-[11px] leading-4 text-slate-500">{reason.detail}</p></div>) : <Empty title="Entry reason unavailable" detail="This legacy position has no persisted score-reason list; current data will not be used to recreate it." />}</div></section></section>
client/src/components/crypto/PaperTradingWorkspace.tsx:80:function TradeHealthPanel({ health }: { health: TradeHealth }) { return <section className={`rounded-2xl border p-5 ${healthTone(health.state)}`}><p className="text-[10px] font-semibold uppercase tracking-[.17em]">Trade Health · current validated state</p><div className="mt-2 flex flex-wrap items-center gap-3"><h3 className="text-base font-semibold">{health.state}</h3><span className="rounded-full border border-current/20 px-2 py-1 text-[10px] font-semibold">TARGET PATH · {health.targetPath.state}</span></div><p className="mt-2 text-xs leading-5 opacity-80">{health.targetPath.explanation}</p><p className="mt-1 text-[10px] opacity-70">Provider {health.data.provider ?? "UNAVAILABLE"} · {health.data.availability} · {asTime(health.data.generatedAt)}</p><div className="mt-4 grid gap-4 lg:grid-cols-2"><div><h4 className="text-[10px] font-semibold uppercase tracking-[.14em] opacity-70">Why this state</h4><ul className="mt-2 space-y-1.5 text-xs leading-5">{health.reasons.map(reason => <li key={reason}>• {reason}</li>)}</ul>{health.reversalWarning ? <p className="mt-3 rounded-lg border border-current/20 bg-black/10 p-2 text-xs">{health.reversalWarning}</p> : null}</div><div><h4 className="text-[10px] font-semibold uppercase tracking-[.14em] opacity-70">Target progress</h4>{health.targetProgress.length ? <div className="mt-2 grid gap-2 sm:grid-cols-3">{health.targetProgress.map(target => <div key={target.label} className="rounded-lg border border-current/15 bg-black/10 p-2"><strong className="text-xs">{target.label} · {target.status}</strong><p className="mt-1 font-mono text-[10px]">{money.format(target.price)}</p><p className="mt-1 text-[10px] opacity-75">{target.distancePercent === null ? "Distance unavailable" : `${target.distancePercent}% to target`}</p></div>)}</div> : <p className="mt-2 text-xs opacity-75">UNAVAILABLE — current data is not sufficient to infer target progress.</p>}</div></div></section>; }
client/src/components/crypto/PaperTradingWorkspace.tsx:81:function LowTimeframeHealthPanel({ plan, health }: { plan: LowTimeframeSnapshot; health: LowTimeframeHealth | null }) { const current = health ?? { state: "HEALTH UNKNOWN" as const, reasons: ["No manual low-timeframe health observation has been requested. The immutable entry snapshot is shown below without inferring a current state."], targetPath: plan.targetPath ?? [], data: { provider: null, generatedAt: null, availability: "UNAVAILABLE" as const } }; const tone = current.state === "HEALTHY" ? "border-emerald-300/20 bg-emerald-300/[.06] text-emerald-100" : current.state === "WARNING" ? "border-amber-300/20 bg-amber-300/[.06] text-amber-100" : current.state === "DANGER" || current.state === "INVALIDATED" ? "border-rose-300/20 bg-rose-300/[.06] text-rose-100" : "border-slate-400/20 bg-slate-400/[.06] text-slate-200"; return <section className={`rounded-2xl border p-5 ${tone}`}><p className="text-[10px] font-semibold uppercase tracking-[.17em]">Low-timeframe Scalping Health · manual only</p><div className="mt-2 flex flex-wrap items-center gap-3"><h3 className="text-base font-semibold">{current.state}</h3><span className="rounded-full border border-current/20 px-2 py-1 text-[10px] font-semibold">{plan.dataBundle?.state ?? "UNAVAILABLE"} SNAPSHOT</span></div><p className="mt-2 text-xs leading-5 opacity-80">This separate 1m / 3m / 5m observation never changes the paper position, entry economics, stop, targets, alerts, or monitoring events. It requires a user-triggered online refresh.</p><p className="mt-1 text-[10px] opacity-70">Current provider {current.data.provider ?? "UNAVAILABLE"} · {current.data.availability} · {asTime(current.data.generatedAt)}</p><div className="mt-4 grid gap-4 lg:grid-cols-2"><div><h4 className="text-[10px] font-semibold uppercase tracking-[.14em] opacity-70">Immutable entry evidence</h4><div className="mt-2 grid gap-2 sm:grid-cols-3">{plan.timeframeStates?.map(state => <div key={state.timeframe} className="rounded-lg border border-current/15 bg-black/10 p-2"><strong className="text-xs">{state.timeframe.toUpperCase()} · {state.bias}</strong><p className="mt-1 text-[10px] opacity-75">RSI {state.rsi ?? "—"} · volume {state.volumeExpansion ?? "—"}×</p><p className="mt-1 text-[10px] opacity-75">Structure {state.structure}</p></div>) ?? <p className="text-xs opacity-75">UNAVAILABLE — no immutable timeframe state exists.</p>}</div><p className="mt-3 text-[10px] opacity-75">Provider {plan.provider ?? "UNAVAILABLE"} · captured {asTime(plan.dataTimestamp)} · alignment {plan.alignment ?? "UNAVAILABLE"}</p></div><div><h4 className="text-[10px] font-semibold uppercase tracking-[.14em] opacity-70">Manual health reasons</h4><ul className="mt-2 space-y-1.5 text-xs leading-5">{current.reasons.map(reason => <li key={reason}>• {reason}</li>)}</ul><div className="mt-3 flex flex-wrap gap-2">{current.targetPath.map(step => <span key={step.label} className="rounded border border-current/20 bg-black/10 px-2 py-1 text-[10px]"><strong>{step.label}</strong> · {money.format(step.price)} · {step.status}</span>)}</div></div></div></section>; }
client/src/components/crypto/PaperTradingWorkspace.tsx:82:function EntryState({ snapshot }: { snapshot: Snapshot }) { const plan = snapshot.setupPlan; const provider = plan?.provider ?? snapshot.dataStatus?.find(item => item.capability === "OHLCV")?.provider ?? "UNAVAILABLE"; return <section className="rounded-2xl border border-cyan-300/15 bg-cyan-300/[.035] p-5"><p className="text-[10px] font-semibold uppercase tracking-[.17em] text-cyan-200">Entry State · immutable</p><h3 className="mt-1 text-base font-semibold">Original recorded observation</h3><div className="mt-4 grid grid-cols-2 gap-2"><Metric label="Opportunity" value={snapshot.opportunity?.score === undefined ? "UNAVAILABLE" : `${snapshot.opportunity.score}/100`} tone="neutral" /><Metric label="Confidence" value={snapshot.opportunity?.confidence === undefined ? "UNAVAILABLE" : `${snapshot.opportunity.confidence}/100`} tone="neutral" /><Metric label="Setup" value={plan?.setupType ?? snapshot.opportunity?.setupType ?? "UNAVAILABLE"} tone="neutral" /><Metric label="Regime" value={snapshot.marketRegime?.classification ?? "UNAVAILABLE"} tone="neutral" /><Metric label="Provider" value={provider} tone="neutral" /><Metric label="Timestamp" value={asTime(plan?.dataTimestamp ?? snapshot.observation?.timestamp)} tone="neutral" /></div></section>; }
client/src/components/crypto/PaperTradingWorkspace.tsx:83:function CurrentState({ trade }: { trade: Trade }) { const current = trade.currentState; return <section className="rounded-2xl border border-amber-300/15 bg-amber-300/[.035] p-5"><p className="text-[10px] font-semibold uppercase tracking-[.17em] text-amber-200">Current State · separate reference</p><h3 className="mt-1 text-base font-semibold">Live mark never replaces entry evidence</h3><div className="mt-4 grid grid-cols-2 gap-2"><Metric label="Current score" value={current?.score ? `${current.score.score}/100` : "UNAVAILABLE"} tone="attention" /><Metric label="Current regime" value={current?.marketRegime?.classification ?? "UNAVAILABLE"} tone="attention" /><Metric label="Current setup" value={current?.score?.setupType ?? "UNAVAILABLE"} tone="attention" /><Metric label="Live timestamp" value={asTime(current?.generatedAt)} tone="attention" /><Metric label="Current price" value={trade.currentPrice === null ? "UNAVAILABLE" : money.format(trade.currentPrice)} tone="attention" /><Metric label="Data state" value={trade.tradeHealth.data.availability} tone="attention" /></div></section>; }
client/src/components/crypto/PaperTradingWorkspace.tsx:84:function Levels({ plan }: { plan: SetupPlanSnapshot | undefined }) { if (!plan) return <section className="rounded-2xl border border-dashed border-white/[.12] p-5"><p className="text-sm font-semibold text-slate-200">Setup levels unavailable</p><p className="mt-2 text-xs leading-5 text-slate-500">This legacy position has no immutable Scalping or Swing snapshot. Current levels will not be invented.</p></section>; return <section className="rounded-2xl border border-white/[.08] bg-white/[.015] p-5"><p className="text-[10px] font-semibold uppercase tracking-[.17em] text-slate-300">Immutable setup levels</p><h3 className="mt-1 text-base font-semibold">{plan.setupType ?? "Setup"} · {plan.mode ?? "UNAVAILABLE"}</h3><div className="mt-4 grid gap-2 sm:grid-cols-2">{plan.entryZone?.preferred !== undefined ? <Metric label="Preferred entry" value={money.format(plan.entryZone.preferred)} tone="neutral" /> : null}{plan.stop?.price !== undefined ? <Metric label="Stop" value={money.format(plan.stop.price)} tone="loss" /> : null}{plan.targets?.map(target => <Metric key={target.label} label={`${target.label} · ${target.reason}`} value={money.format(target.price)} tone="profit" />)}</div><p className="mt-4 text-xs text-slate-500">Technical chart evidence remains available through Asset Intelligence; this detail preserves the exact entry snapshot rather than drawing from current data.</p></section>; }
client/src/components/crypto/PaperTradingWorkspace.tsx:91:function WorkspaceLoading() { return <section className="grid min-h-[65vh] place-items-center px-6"><div className="max-w-md rounded-2xl border border-white/[.08] bg-[#09101c] p-7 text-center"><Loader2 className="mx-auto h-7 w-7 animate-spin text-emerald-300" /><p className="mt-4 text-[10px] font-semibold uppercase tracking-[.18em] text-emerald-200">Simulated trading terminal</p><h2 className="mt-2 text-lg font-semibold">Loading private Paper Trading workspace</h2><p className="mt-2 text-xs leading-5 text-slate-500">No simulated position or live-market value is created while this state is loading.</p></div></section>; }
client/src/components/crypto/TradeSetupWorkspace.tsx:13:type DataBundle = { provider: string | null; state: string | null; coherent: boolean; eligibleForScoring: boolean; statusMessage: string; timeframes: Array<{ timeframe: string; provider: string | null; state: string; status: "live" | "stale" | "unavailable"; fetchedAt: number | null; candleCount: number; eligibleForScoring: boolean; message: string | null; errorClass: string | null }> };
client/src/components/crypto/TradeSetupWorkspace.tsx:37:    <section className="grid gap-3 sm:grid-cols-3"><StatusCard label="Validated timeframes" value={mode === "SCALP" ? "15M · 1H · 4H" : "1H · 4H · 1D"} tone="cyan" /><div className="rounded-xl border border-amber-300/15 bg-amber-300/[.035] p-4 text-amber-100"><span className="text-[9px] font-semibold uppercase tracking-[.14em] opacity-70">Low-timeframe layer</span><p className="mt-2 text-xs font-medium">1M / 3M / 5M is isolated from this established view.</p>{mode === "SCALP" ? <Button size="sm" variant="outline" onClick={() => setScalpingView("LOW_TIMEFRAME")} className="mt-3 h-7 border-amber-300/25 px-2 text-[10px] text-amber-100">Open Scalping Intelligence</Button> : null}</div><StatusCard label="Market context" value={data?.marketRegime ? `${data.marketRegime.classification} · ${data.marketRegime.score}/100` : "UNAVAILABLE"} tone="violet" /></section>
client/src/components/crypto/TradeSetupWorkspace.tsx:40:    {setups.isLoading ? <Loading /> : setups.error ? <ErrorPanel detail={setups.error.message} /> : data ? <section className="space-y-4"><SetupOverview summary={data.diagnostics} plans={data.setups} />{data.setups.map((plan, index) => <SetupCard key={`${plan.mode}-${plan.assetId}`} plan={plan} ordinal={plan.rank ?? index + 1} online={online} onInspect={() => onInspectAsset(plan.assetId)} onPaperTrade={() => onPaperTrade(plan.assetId, mode)} />)}</section> : <Empty />}
client/src/components/crypto/TradeSetupWorkspace.tsx:51:    {qualified ? <div className="grid gap-4 p-5 xl:grid-cols-[1.1fr_.9fr]"><div className="grid gap-2 sm:grid-cols-2 lg:grid-cols-4"><Level label="Entry zone" value={`${money.format(plan.entryZone!.low)} – ${money.format(plan.entryZone!.high)}`} note={plan.entryZone!.reason} /><Level label="Preferred entry" value={money.format(plan.entryZone!.preferred)} note="Execution-timeframe technical level" /><Level label="Stop / invalidation" value={money.format(plan.stop!.price)} note={plan.stop!.reason} tone="risk" />{plan.targets.map(target => <Level key={target.label} label={`${target.label} · ${targetRr(target) === null ? "R:R unavailable" : `R:R ${targetRr(target)!.toFixed(2)}`}`} value={money.format(target.price)} note={target.reason} tone="positive" />)}</div><Evidence plan={plan} /></div> : plan.presentationStatus === "WATCH" ? <WatchEvidence plan={plan} /> : <NoTradeEvidence plan={plan} />}
client/src/components/crypto/TradeSetupWorkspace.tsx:55:function BundleStatus({ bundle }: { bundle: DataBundle }) { const live = bundle.eligibleForScoring && bundle.coherent; const stateTone = live ? "border-emerald-300/15 bg-emerald-300/[.035]" : bundle.state === "STALE" ? "border-amber-300/15 bg-amber-300/[.035]" : "border-rose-300/15 bg-rose-300/[.035]"; const label = live ? "LIVE & COHERENT" : bundle.state ?? "NO DATA"; return <section className={`border-b border-white/[.06] px-5 py-3 ${stateTone}`} aria-label="Provider data status"><div className="flex flex-wrap items-center justify-between gap-2"><div><p className="text-[9px] font-semibold uppercase tracking-[.14em] text-slate-400">Data status</p><p className="mt-1 text-xs font-semibold text-slate-100">{label} · {bundle.provider ?? "No eligible provider"}</p></div><p className="max-w-xl text-[11px] leading-4 text-slate-400">{bundle.statusMessage}</p></div><div className="mt-3 grid gap-2 sm:grid-cols-3">{bundle.timeframes.map(item => <div key={item.timeframe} className="rounded-lg border border-white/[.08] bg-black/10 p-2"><div className="flex items-center justify-between gap-2"><span className="font-mono text-[11px] text-slate-200">{item.timeframe.toUpperCase()}</span><span className={`text-[9px] font-semibold ${item.eligibleForScoring ? "text-emerald-200" : item.status === "stale" ? "text-amber-200" : "text-rose-200"}`}>{item.state}</span></div><p className="mt-1 truncate text-[10px] text-slate-400">{item.provider ?? "UNAVAILABLE"} · {item.candleCount} closed</p><p className="mt-1 truncate text-[10px] text-slate-500">{item.fetchedAt ? timestamp(item.fetchedAt) : item.errorClass ?? item.message ?? "No response"}</p></div>)}</div></section>; }
client/src/components/crypto/TradeSetupWorkspace.tsx:57:function WatchEvidence({ plan }: { plan: Plan }) { return <div className="space-y-4 p-5"><div className="flex gap-3"><Clock3 className="mt-0.5 h-5 w-5 shrink-0 text-amber-200" /><div><p className="font-semibold text-amber-100">WATCH — NOT A TRADE</p><p className="mt-1 text-sm leading-6 text-slate-300">Current data is coherent, but an existing strategy requirement has not passed. No Paper Trading action is created.</p></div></div><div className="grid gap-3 md:grid-cols-2"><TextList title="Missing confirmation" items={plan.watch?.missing ?? []} tone="amber" /><TextList title="Would become eligible when" items={plan.watch?.wouldQualifyWhen ?? []} tone="cyan" /></div>{plan.readinessCandidate ? <ConditionalCandidate plan={plan} /> : null}<Diagnostics conditions={plan.diagnostics} /></div>; }
client/src/components/crypto/TradeSetupWorkspace.tsx:58:function ConditionalCandidate({ plan }: { plan: Plan }) { const candidate = plan.readinessCandidate; if (!candidate) return null; const targetR = (price: number) => !candidate.entryZone || !candidate.invalidation ? null : Math.abs(price - candidate.entryZone.preferred) / Math.max(Math.abs(candidate.entryZone.preferred - candidate.invalidation.price), Number.EPSILON); return <section className="rounded-xl border border-violet-300/15 bg-violet-300/[.035] p-4"><p className="text-[10px] font-semibold uppercase tracking-[.15em] text-violet-200">Conditional technical plan — not eligible for Paper Trading</p><p className="mt-2 text-xs leading-5 text-slate-300">{candidate.reason}</p><div className="mt-3 grid gap-2 sm:grid-cols-2 lg:grid-cols-4">{candidate.entryZone ? <Level label="Conditional entry zone" value={`${money.format(candidate.entryZone.low)} – ${money.format(candidate.entryZone.high)}`} note={candidate.entryZone.reason} /> : null}{candidate.invalidation ? <Level label="Technical invalidation" value={money.format(candidate.invalidation.price)} note={candidate.invalidation.reason} tone="risk" /> : null}{candidate.targets.map(target => <Level key={target.label} label={`${target.label} · ${targetR(target.price) === null ? "R unavailable" : `${targetR(target.price)!.toFixed(2)}R`}`} value={money.format(target.price)} note={target.reason} tone="positive" />)}{candidate.rewardRisk !== null ? <Level label="Current first R:R" value={`${candidate.rewardRisk}:1`} note="Existing minimum remains 1:1" tone={candidate.rewardRisk >= 1 ? "positive" : "risk"} /> : null}</div><p className="mt-3 text-[11px] leading-5 text-slate-500">The conditional level is shown because existing validated structure derived it. It becomes unavailable if the underlying live input is unavailable, stale, invalid, or incoherent.</p></section>; }
client/src/components/crypto/TradeSetupWorkspace.tsx:60:function Diagnostics({ conditions }: { conditions: Condition[] }) { return <div className="grid gap-2 lg:grid-cols-2">{conditions.map(condition => <ConditionCard key={condition.key} condition={condition} />)}</div>; }
client/src/components/crypto/TradeSetupWorkspace.tsx:61:function ConditionCard({ condition }: { condition: Condition }) { const tone = condition.status === "PASSED" ? "border-emerald-300/18 bg-emerald-300/[.035]" : condition.status === "FAILED" ? "border-rose-300/18 bg-rose-300/[.035]" : condition.status === "STALE" ? "border-amber-300/18 bg-amber-300/[.035]" : "border-slate-400/15 bg-slate-400/[.035]"; const Icon = condition.status === "PASSED" ? CircleCheck : condition.status === "FAILED" ? CircleX : condition.status === "STALE" ? Clock3 : CircleAlert; const label = condition.status === "PASSED" ? "Passed" : condition.status === "FAILED" ? "Failed" : condition.status === "STALE" ? "Stale" : "Unavailable"; return <section className={`rounded-xl border p-3 ${tone}`}><div className="flex items-start justify-between gap-3"><h4 className="text-xs font-semibold text-slate-100">{condition.label}</h4><span className="flex shrink-0 items-center gap-1 text-[10px] font-semibold uppercase tracking-wide text-slate-300"><Icon className="h-3.5 w-3.5" />{label}</span></div><dl className="mt-3 space-y-2 text-[11px] leading-4"><div><dt className="text-slate-500">Current</dt><dd className="mt-0.5 break-words text-slate-200">{condition.actual}</dd></div><div><dt className="text-slate-500">Required</dt><dd className="mt-0.5 break-words text-slate-300">{condition.required}</dd></div><div><dt className="sr-only">Explanation</dt><dd className="mt-1 border-t border-white/[.07] pt-2 text-slate-400">{condition.detail}</dd></div></dl></section>; }
client/src/components/crypto/TradeSetupWorkspace.tsx:63:function SetupOverview({ summary, plans }: { summary: DiagnosticSummary; plans: Plan[] }) { const qualified = plans.filter(plan => plan.presentationStatus === "QUALIFIED").length; const watch = plans.filter(plan => plan.presentationStatus === "WATCH").length; return <section className="rounded-2xl border border-cyan-300/15 bg-cyan-300/[.025] p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Current setup overview</p><h3 className="mt-1 text-sm font-semibold text-slate-100">{summary.evaluatedAssets} assets scanned · {qualified} qualified · {watch} watch · {Math.max(0, summary.noTradeAssets - watch)} no trade</h3></div><p className="max-w-md text-xs leading-5 text-slate-400">Qualified ranking uses the existing Opportunity Score only. Watch and No Trade remain non-actionable until every existing condition passes.</p></div><div className="mt-4 grid gap-2 sm:grid-cols-2 xl:grid-cols-4"><Level label="Lack of market setup" value={String(summary.classification.lackOfMarketSetups)} note="Existing direction/structure did not produce a plan" /><Level label="Missing data" value={String(summary.classification.missingData)} note="At least one required input was unavailable" tone="risk" /><Level label="Stale data" value={String(summary.classification.staleData)} note="Existing provider state marked an input stale" /><Level label="Existing setup requirement" value={String(summary.classification.existingSetupRequirement)} note="Valid levels failed the existing R:R minimum" tone="risk" /></div>{summary.topNoTradeReasons.length ? <div className="mt-4 border-t border-white/[.08] pt-3"><p className="text-[10px] font-semibold uppercase tracking-[.14em] text-slate-500">Top current NO TRADE reasons</p><div className="mt-2 flex flex-wrap gap-2">{summary.topNoTradeReasons.map(reason => <span key={`${reason.key}-${reason.status}`} className="rounded-full border border-white/[.1] bg-black/10 px-2.5 py-1 text-[11px] text-slate-300">{reason.label}: {reason.count} {reason.status.toLowerCase()}</span>)}</div></div> : null}</section>; }
client/src/components/crypto/TradeSetupWorkspace.tsx:64:function Evidence({ plan }: { plan: Plan }) { return <div className="grid gap-3 sm:grid-cols-2"><section className="rounded-xl border border-emerald-300/15 bg-emerald-300/[.035] p-4"><div className="flex items-center gap-2"><CircleCheck className="h-4 w-4 text-emerald-300" /><h4 className="text-[10px] font-semibold uppercase tracking-[.15em] text-emerald-200">Why this setup</h4></div><p className="mt-2 text-sm font-semibold text-slate-100">Technical evidence from validated inputs</p><ul className="mt-3 space-y-1.5 text-xs leading-5 text-slate-300">{plan.evidence.slice(0, 5).map(item => <li key={item}>• {item}</li>)}</ul></section><section className="rounded-xl border border-amber-300/15 bg-amber-300/[.035] p-4"><div className="flex items-center gap-2"><CircleX className="h-4 w-4 text-amber-300" /><h4 className="text-[10px] font-semibold uppercase tracking-[.15em] text-amber-200">Risk factors</h4></div><p className="mt-2 text-sm font-semibold text-slate-100">Opportunity {plan.opportunityScore ?? "UNAVAILABLE"} · Regime {plan.regimeScore ?? "UNAVAILABLE"}</p><ul className="mt-3 space-y-1.5 text-xs leading-5 text-slate-300">{plan.risks.length ? plan.risks.slice(0, 5).map(item => <li key={item}>• {item}</li>) : <li>• No negative technical component is currently recorded.</li>}</ul></section></div>; }
client/src/components/crypto/TradeSetupWorkspace.tsx:65:function ProviderHealthPanel({ providers }: { providers: ProviderHealth[] }) { return <section className="rounded-2xl border border-white/[.08] bg-white/[.015] p-5" aria-label="Provider health"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Provider health</p><h3 className="mt-1 text-sm font-semibold text-slate-100">Current scanner evidence — read only</h3></div><p className="max-w-xl text-xs leading-5 text-slate-400">HTTP 451 remains ineligible. A fallback is used only when all required timeframes form one validated provider bundle.</p></div><div className="mt-4 grid gap-3 xl:grid-cols-3">{providers.map(provider => <article key={provider.provider} className="rounded-xl border border-white/[.08] bg-slate-950/35 p-3"><div className="flex items-start justify-between gap-2"><div><h4 className="text-xs font-semibold text-slate-100">{provider.provider}</h4><p className={`mt-1 text-[10px] font-semibold ${provider.status === "LIVE" ? "text-emerald-200" : provider.status === "PARTIAL" ? "text-amber-200" : "text-rose-200"}`}>{provider.status}</p></div><span className="text-[10px] text-slate-500">{provider.supportedTimeframes.length ? provider.supportedTimeframes.map(value => value.toUpperCase()).join(" / ") : "No technical timeframe"}</span></div><dl className="mt-3 space-y-1 text-[10px] leading-4 text-slate-400"><div><dt className="inline text-slate-500">Last success: </dt><dd className="inline">{timestamp(provider.lastSuccessfulAt)}</dd></div><div><dt className="inline text-slate-500">Last failure: </dt><dd className="inline">{timestamp(provider.lastFailureAt)}</dd></div><div><dt className="inline text-slate-500">Classification: </dt><dd className="inline">{provider.failureClassification ?? "NONE"}</dd></div><div><dt className="inline text-slate-500">Validated symbols: </dt><dd className="inline">{provider.validatedSymbols.length || "None"}</dd></div></dl><p className="mt-3 border-t border-white/[.06] pt-2 text-[10px] leading-4 text-slate-500">{provider.note}</p></article>)}</div></section>; }
client/src/components/crypto/LowTimeframeScalpingWorkspace.tsx:8:  dataBundle: { state: "VALID" | "PARTIAL" | "STALE" | "MISSING" | "INCOHERENT"; coherent: boolean; eligibleForScalping: boolean; capturedAt: number; statusMessage: string; timeframes: Array<{ timeframe: "1m" | "3m" | "5m"; provider: string | null; symbol: string | null; state: "VALID" | "STALE" | "MISSING" | "INVALID"; status: "live" | "stale" | "unavailable"; fetchedAt: number | null; candleCount: number; oldestCandleAt: number | null; newestCandleAt: number | null; freshnessMs: number | null; eligibleForScalping: boolean; message: string | null; errorClass: string | null }> };
client/src/components/crypto/LowTimeframeScalpingWorkspace.tsx:17:type LowResponse = { generatedAt: number; provider: string; requiredTimeframes: string[]; minimumCandles: number; providerDocumentation: { kline: string; instruments: string; rateLimits: string; terms: string }; summary: { evaluatedAssets: number; qualified: number; watch: number; noTrade: number; dataUnavailable: number; byBundleState: Array<{ state: string; count: number }> }; note: string; setups: LowPlan[] };
client/src/components/crypto/LowTimeframeScalpingWorkspace.tsx:29:    <section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Status label="Required evidence" value="1M · 3M · 5M" tone="cyan" /><Status label="Provider" value={data?.provider ?? "Bybit Spot"} tone="violet" /><Status label="Validated depth" value={data ? `${data.minimumCandles} closed / timeframe` : "Validating"} tone="cyan" /><Status label="Capture" value={data ? ago(data.generatedAt) : "UNAVAILABLE"} tone="amber" /></section>
client/src/components/crypto/LowTimeframeScalpingWorkspace.tsx:31:    {query.isLoading ? <Loading /> : query.error ? <ErrorPanel detail={query.error.message} /> : data ? <><Overview data={data} />{data.setups.map((plan, index) => <LowSetupCard key={plan.assetId} plan={plan} ordinal={plan.rank ?? index + 1} online={online} onInspect={() => onInspectAsset(plan.assetId)} onPaperTrade={() => onPaperTrade(plan.assetId)} />)}</> : <Empty />}
client/src/components/crypto/LowTimeframeScalpingWorkspace.tsx:35:function Overview({ data }: { data: LowResponse }) { return <section className="rounded-2xl border border-cyan-300/15 bg-cyan-300/[.025] p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Current low-timeframe overview</p><h3 className="mt-1 text-sm font-semibold text-slate-100">{data.summary.evaluatedAssets} assets evaluated · {data.summary.qualified} qualified · {data.summary.watch} watch · {data.summary.noTrade} no trade</h3></div><p className="max-w-xl text-xs leading-5 text-slate-400">{data.note}</p></div><div className="mt-4 grid gap-2 sm:grid-cols-2 xl:grid-cols-5"><Mini label="Valid bundles" value={String(data.summary.byBundleState.find(item => item.state === "VALID")?.count ?? 0)} note="Only these may be analyzed" /><Mini label="Partial" value={String(data.summary.byBundleState.find(item => item.state === "PARTIAL")?.count ?? 0)} note="Not used" tone="risk" /><Mini label="Stale" value={String(data.summary.byBundleState.find(item => item.state === "STALE")?.count ?? 0)} note="No new entry" tone="amber" /><Mini label="Missing" value={String(data.summary.byBundleState.find(item => item.state === "MISSING")?.count ?? 0)} note="No provider fallback" tone="risk" /><Mini label="Incoherent" value={String(data.summary.byBundleState.find(item => item.state === "INCOHERENT")?.count ?? 0)} note="No mixing permitted" tone="risk" /></div></section>; }
client/src/components/crypto/LowTimeframeScalpingWorkspace.tsx:44:    {qualified ? <div className="space-y-4 p-5"><div className="grid gap-2 sm:grid-cols-2 lg:grid-cols-4"><Mini label="Current price" value={plan.currentPrice === null ? "UNAVAILABLE" : money.format(plan.currentPrice)} note="Latest completed 1m close" /><Mini label="Preferred entry" value={money.format(plan.entryZone!.preferred)} note={plan.entryZone!.reason} tone={plan.entryZone!.state === "READY" ? "positive" : "risk"} /><Mini label="Stop / invalidation" value={money.format(plan.stop!.price)} note={plan.stop!.reason} tone="risk" /><Mini label="Expected duration" value={plan.expectedDuration ?? "UNAVAILABLE"} note={`First target R:R ${plan.rewardRisk ?? "UNAVAILABLE"}:1`} /></div><div className="grid gap-2 sm:grid-cols-3">{plan.targets.map(target => <Mini key={target.label} label={`${target.label} · R:R ${target.rewardRisk ?? "—"}`} value={money.format(target.price)} note={`${target.distancePercent ?? "—"}% · ${target.reason}`} tone="positive" />)}</div><TargetPath path={plan.targetPath} /><Evidence title="Why this setup" items={plan.evidence} tone="positive" /><div className="rounded-xl border border-amber-300/15 bg-amber-300/[.035] p-3 text-[11px] leading-5 text-amber-100"><strong>Paper Trading boundary.</strong> Opening a user-confirmed simulation retains this immutable low-timeframe snapshot as evidence. Existing server-authoritative 4h-ATR / 2R accounting, authorization, manual close, and no-real-trading rules remain unchanged.</div></div> : <NoTrade plan={plan} />}
client/src/components/crypto/LowTimeframeScalpingWorkspace.tsx:48:function Provenance({ plan }: { plan: LowPlan }) { const valid = plan.dataBundle.state === "VALID" && plan.dataBundle.coherent; return <section className={`border-b border-white/[.06] px-5 py-3 ${valid ? "bg-emerald-300/[.025]" : plan.dataBundle.state === "STALE" ? "bg-amber-300/[.025]" : "bg-rose-300/[.025]"}`}><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-[9px] font-semibold uppercase tracking-[.14em] text-slate-400">Provider provenance and data quality</p><p className="mt-1 text-xs font-semibold text-slate-100">{plan.dataBundle.state} · {plan.dataBundle.coherent ? "ONE PROVIDER / COHERENT" : "NO PLAN"} · {plan.provider ?? "UNAVAILABLE"}</p></div><p className="max-w-2xl text-[11px] leading-4 text-slate-400">{plan.dataBundle.statusMessage}</p></div><div className="mt-3 grid gap-2 sm:grid-cols-3">{plan.dataBundle.timeframes.map(item => <div key={item.timeframe} className="rounded-lg border border-white/[.08] bg-black/10 p-2"><div className="flex items-center justify-between gap-2"><strong className="font-mono text-[11px] text-slate-100">{item.timeframe.toUpperCase()}</strong><span className={`text-[9px] font-semibold ${item.eligibleForScalping ? "text-emerald-200" : item.status === "stale" ? "text-amber-200" : "text-rose-200"}`}>{item.state}</span></div><p className="mt-1 truncate text-[10px] text-slate-400">{item.provider ?? "UNAVAILABLE"} · {item.symbol ?? "NO SYMBOL"}</p><p className="mt-1 text-[10px] text-slate-500">{item.candleCount} completed · freshness {item.freshnessMs === null ? "UNAVAILABLE" : `${Math.round(item.freshnessMs / 1_000)}s`}</p><p className="mt-1 truncate text-[10px] text-slate-600">{item.fetchedAt ? asTime(item.fetchedAt) : item.errorClass ?? item.message ?? "NO RESPONSE"}</p></div>)}</div></section>; }
client/src/components/crypto/LowTimeframeScalpingWorkspace.tsx:51:function TimeframeStates({ states }: { states: LowPlan["timeframeStates"] }) { return states.length ? <div className="grid gap-2 sm:grid-cols-3">{states.map(state => <div key={state.timeframe} className="rounded-xl border border-white/[.07] bg-white/[.02] p-3"><div className="flex items-center justify-between"><strong className="font-mono text-xs text-slate-100">{state.timeframe.toUpperCase()}</strong><span className={state.bias === "bullish" ? "text-emerald-200" : state.bias === "bearish" ? "text-rose-200" : "text-slate-400"}>{state.bias.toUpperCase()}</span></div><p className="mt-2 text-[10px] text-slate-400">RSI {state.rsi ?? "—"} · volume {state.volumeExpansion ?? "—"}×</p><p className="mt-1 text-[10px] text-slate-500">Momentum {state.momentum} · structure {state.structure}</p></div>)}</div> : null; }
client/src/components/crypto/OpportunityDiscoveryWorkspace.tsx:17:type Response = { generatedAt: number; marketRegime: { classification: string; score: number } | null; discovery: { items: DiscoveryItem[]; summary: { evaluated: number; qualified: number; potential: number; watch: number; noTrade: number; dataUnavailable: number; restrictedByRiskOff: number } } };
client/src/components/crypto/OpportunityDiscoveryWorkspace.tsx:33:  const createMonitor = trpc.crypto.createSetupMonitor.useMutation({ onSuccess: () => { toast.success("Setup saved for monitoring", { description: "The original snapshot is now preserved server-side." }); }, onError: error => toast.error("Setup was not saved", { description: error.message }) });
client/src/components/crypto/OpportunityDiscoveryWorkspace.tsx:41:    {query.isLoading ? <Loading /> : query.error ? <ErrorPanel detail={query.error.message} /> : data && summary ? <><Summary summary={summary} /><section className="space-y-2"><div className="flex flex-wrap gap-2" aria-label="Opportunity status filters">{(["ALL", "QUALIFIED", "POTENTIAL", "WATCH", "NO TRADE", "DATA UNAVAILABLE"] as Filter[]).map(value => <Button key={value} size="sm" variant="outline" onClick={() => setFilter(value)} className={`border-white/[.1] text-[11px] ${filter === value ? "bg-cyan-300/[.12] text-cyan-100" : "bg-white/[.02] text-slate-400"}`}>{value === "ALL" ? "All" : value}</Button>)}</div><div className="flex flex-wrap items-center gap-2" aria-label="Opportunity intelligence filters"><span className="text-[10px] font-semibold uppercase tracking-[.14em] text-slate-500">Health</span>{(["ALL", "HEALTHY", "CAUTION", "REVERSAL RISK", "INVALIDATED", "HEALTH UNKNOWN"] as HealthFilter[]).map(value => <Button key={value} size="sm" variant="outline" onClick={() => setHealthFilter(value)} className={`h-7 border-white/[.1] text-[10px] ${healthFilter === value ? "bg-amber-300/[.12] text-amber-100" : "bg-white/[.02] text-slate-400"}`}>{value === "ALL" ? "All health" : value}</Button>)}<span className="text-[10px] font-semibold uppercase tracking-[.14em] text-slate-500">Direction</span>{(["ALL", "LONG", "SHORT"] as DirectionFilter[]).map(value => <Button key={value} size="sm" variant="outline" onClick={() => setDirectionFilter(value)} className={`h-7 border-white/[.1] text-[10px] ${directionFilter === value ? "bg-cyan-300/[.12] text-cyan-100" : "bg-white/[.02] text-slate-400"}`}>{value === "ALL" ? "All directions" : value}</Button>)}<span className="text-[10px] font-semibold uppercase tracking-[.14em] text-slate-500">Plan</span>{(["ALL", "ENTRY READY", "WAITING CONFIRMATION", "INCOMPLETE"] as PlanFilter[]).map(value => <Button key={value} size="sm" variant="outline" onClick={() => setPlanFilter(value)} className={`h-7 border-white/[.1] text-[10px] ${planFilter === value ? "bg-violet-300/[.12] text-violet-100" : "bg-white/[.02] text-slate-400"}`}>{value === "ALL" ? "All plans" : value}</Button>)}</div><div className="flex flex-wrap items-center gap-2 text-[10px] text-slate-500"><span className="font-semibold uppercase tracking-[.14em]">Sort</span>{(["DEFAULT", "CLOSEST", "OPPORTUNITY"] as Sort[]).map(value => <Button key={value} size="sm" variant="outline" onClick={() => setSort(value)} className={`h-7 border-white/[.1] text-[10px] ${sort === value ? "bg-violet-300/[.12] text-violet-100" : "bg-white/[.02] text-slate-400"}`}>{value === "DEFAULT" ? "Status + Opportunity" : value === "CLOSEST" ? "Closest to qualification" : "Highest Opportunity"}</Button>)}<span>Readiness is a separate deterministic explanation, never an Opportunity Score replacement.</span></div></section><section className="space-y-4">{items.length ? items.map(item => <DiscoveryCard key={item.assetId} item={item} online={online} onInspect={() => onInspectAsset(item.assetId)} onPaper={() => onPaperTrade(item.assetId)} onMonitor={() => createMonitor.mutate({ assetId: item.assetId, mode: "SWING" })} monitorPending={createMonitor.isPending && createMonitor.variables?.assetId === item.assetId} />) : <Empty />}</section></> : <Empty />}
client/src/components/crypto/OpportunityDiscoveryWorkspace.tsx:45:function Summary({ summary }: { summary: Response["discovery"]["summary"] }) { return <section className="rounded-2xl border border-cyan-300/15 bg-cyan-300/[.025] p-5"><p className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Current Swing discovery</p><h3 className="mt-1 text-sm font-semibold text-slate-100">{summary.evaluated} assets evaluated from one validated 1H / 4H / 1D interpretation pass</h3><div className="mt-4 grid gap-2 sm:grid-cols-2 xl:grid-cols-5"><Metric label="Qualified" value={summary.qualified} tone="emerald" /><Metric label="Potential" value={summary.potential} tone="amber" /><Metric label="Watch" value={summary.watch} tone="cyan" /><Metric label="No trade" value={summary.noTrade} tone="rose" /><Metric label="Data unavailable" value={summary.dataUnavailable} tone="slate" /></div></section>; }
client/src/components/crypto/OpportunityDiscoveryWorkspace.tsx:52:    <div className="grid gap-4 p-5 lg:grid-cols-[1.15fr_.85fr]"><section className="rounded-xl border border-white/[.07] bg-white/[.02] p-4"><div className="flex items-center gap-2"><Icon className="h-4 w-4 text-cyan-200" /><p className="text-[10px] font-semibold uppercase tracking-[.15em] text-cyan-200">Why this status</p></div><p className="mt-2 text-sm leading-6 text-slate-200">{item.exactReason}</p>{item.whyInteresting.length ? <TextList title="What is working" items={item.whyInteresting} tone="cyan" /> : null}{item.dataReason ? <p className="mt-3 rounded-lg border border-slate-400/15 bg-black/10 p-3 text-xs leading-5 text-slate-400">{item.dataReason}</p> : null}</section><div className="space-y-4"><ReadinessPanel item={item} />{qualified ? <QualifiedLevels item={item} /> : <UpgradeConditions item={item} />}</div></div>
client/src/components/crypto/OpportunityDiscoveryWorkspace.tsx:56:function QualifiedLevels({ item }: { item: DiscoveryItem }) { return <section className="rounded-xl border border-emerald-300/15 bg-emerald-300/[.035] p-4"><p className="text-[10px] font-semibold uppercase tracking-[.15em] text-emerald-200">Validated trade plan</p><p className="mt-2 text-xs leading-5 text-slate-400">These existing technical levels are shown only for a qualified plan. Paper Trading remains simulated and requires explicit confirmation.</p><div className="mt-3 grid gap-2 sm:grid-cols-2">{item.sourcePlan.entryZone ? <Metric label="Preferred entry" value={money.format(item.sourcePlan.entryZone.preferred)} tone="emerald" /> : null}{item.sourcePlan.stop ? <Metric label="Stop" value={money.format(item.sourcePlan.stop.price)} tone="rose" /> : null}{item.sourcePlan.targets.map(target => <Metric key={target.label} label={target.label} value={money.format(target.price)} tone="emerald" />)}{item.sourcePlan.rewardRisk !== null ? <Metric label="First R:R" value={`${item.sourcePlan.rewardRisk}:1`} tone="cyan" /> : null}</div></section>; }
client/src/components/crypto/OpportunityDiscoveryWorkspace.tsx:61:  return <section className="rounded-xl border border-violet-300/15 bg-violet-300/[.035] p-4"><div className="flex flex-wrap items-start justify-between gap-2"><div><p className="text-[10px] font-semibold uppercase tracking-[.15em] text-violet-200">Setup readiness · separate interpretation</p><p className="mt-1 text-xs leading-5 text-slate-300">{item.setupReadiness.explanation}</p></div><span className="rounded-full border border-violet-300/20 px-2 py-1 font-mono text-[11px] text-violet-100">{item.setupReadiness.state} · {item.setupReadiness.score ?? "UNAVAILABLE"}{item.setupReadiness.score === null ? "" : "/100"}</span></div><div className="mt-3 grid gap-2 sm:grid-cols-2"><Metric label="Trend / confirmation" value={`${item.setupReadiness.components.timeframeAlignment}/35`} tone="cyan" /><Metric label="Evidence / plan" value={`${item.setupReadiness.components.positiveEvidence}/20 · ${item.setupReadiness.components.technicalPlan}/20`} tone="cyan" /><Metric label="R:R / regime" value={`${item.setupReadiness.components.rewardRisk}/15 · ${item.setupReadiness.components.regime}/10`} tone="amber" /><Metric label="Trade Health" value={plan.health.state} tone="slate" /></div>{hasPlan ? <div className="mt-3 rounded-lg border border-white/[.08] bg-black/10 p-3"><p className="text-[9px] font-semibold uppercase tracking-[.14em] text-amber-200">Conditional technical plan — not a trade instruction</p><p className="mt-1 text-[11px] leading-5 text-slate-400">{plan.reason}</p><div className="mt-3 grid gap-2 sm:grid-cols-2">{plan.entryZone ? <Metric label="Entry zone" value={`${money.format(plan.entryZone.low)} – ${money.format(plan.entryZone.high)}`} tone="amber" /> : null}{plan.invalidation ? <Metric label="Invalidation" value={money.format(plan.invalidation.price)} tone="rose" /> : null}{plan.targets.map(target => <Metric key={target.label} label={`${target.label} · ${target.rMultiple === null ? "R unavailable" : `${target.rMultiple}R`}`} value={`${money.format(target.price)} · ${target.distancePercent ?? "UNAVAILABLE"}%`} tone="emerald" />)}{plan.rewardRisk !== null ? <Metric label="Current first R:R" value={`${plan.rewardRisk}:1`} tone={plan.rewardRisk >= 1 ? "cyan" : "rose"} /> : null}</div>{plan.confirmation ? <p className="mt-3 border-t border-white/[.07] pt-3 text-xs leading-5 text-cyan-100/85"><strong>Confirmation:</strong> {plan.confirmation}</p> : null}<p className="mt-2 text-xs leading-5 text-rose-100/85"><strong>What would invalidate it:</strong> {item.invalidationExplanation}</p></div> : <p className="mt-3 rounded-lg border border-slate-400/15 bg-black/10 p-3 text-xs leading-5 text-slate-400">Entry zone, targets, and invalidation are unavailable because the existing validated setup engine did not derive every required technical input.</p>}<p className="mt-3 text-[10px] leading-4 text-slate-500">Provenance: {item.provider ?? "UNAVAILABLE"} · {item.timeframes.execution.toUpperCase()} / {item.timeframes.confirmation.toUpperCase()} / {item.timeframes.context.toUpperCase()} · {time(item.dataTimestamp)} · {item.freshness} · {item.validationStatus ?? "UNAVAILABLE"}. Potential alerts are disabled.</p></section>;
client/src/components/crypto/SetupMonitorWorkspace.tsx:13:const date = (value: string | Date | number | null | undefined) => value ? new Date(value).toLocaleString() : "Unavailable";
client/src/components/crypto/SetupMonitorWorkspace.tsx:44:      <div className="mt-4 flex flex-wrap gap-2"><Filter label="Health" value={healthFilter} onChange={setHealthFilter} options={[["ALL", "All health"], ["HEALTHY", "Healthy"], ["CAUTION", "Caution"], ["REVERSAL_RISK", "Reversal risk"], ["INVALIDATED", "Invalidated"], ["DATA_UNAVAILABLE", "Unavailable"]]} /><Filter label="Direction" value={directionFilter} onChange={setDirectionFilter} options={[["ALL", "All directions"], ["LONG", "Long"], ["SHORT", "Short"]]} /><Filter label="Trade plan" value={planFilter} onChange={setPlanFilter} options={[["ALL", "All plans"], ["ENTRY_READY", "Entry ready"], ["WAITING_CONFIRMATION", "Waiting confirmation"], ["INCOMPLETE", "Incomplete"]]} /></div>
client/src/components/crypto/SetupMonitorWorkspace.tsx:47:    <div className="grid gap-5 xl:grid-cols-[minmax(0,1.15fr)_minmax(360px,.85fr)]">
client/src/components/crypto/SetupMonitorWorkspace.tsx:51:        <div className="grid grid-cols-2 gap-2"><Snapshot title="Original setup" value={label(String(original.status ?? "UNAVAILABLE"))} detail={`Captured ${date(original.capturedAt as number | null)}`} /><Snapshot title="Current state" value={label(String(selected.current.status))} detail={`Validated ${date(selected.current.lastValidatedAt as string | Date | null)}`} /></div>
client/src/components/crypto/SetupMonitorWorkspace.tsx:55:        <div className="rounded-xl border border-white/[.07] bg-white/[.025] p-4"><p className="text-[10px] font-semibold uppercase tracking-[.17em] text-slate-500">Current evidence & provenance</p><p className="mt-2 text-sm leading-6 text-slate-300">{String(current.reason ?? selected.current.stateReason ?? "No current explanation available.")}</p><p className="mt-3 text-xs text-slate-500">Provider: {String(json(selected.current.providerProvenance).provider ?? "Unavailable")} · Timeframe: {selected.timeframe} · Freshness: {String(json(selected.current.providerProvenance).freshness ?? "Unavailable")} · Last validated: {date(selected.current.lastValidatedAt as string | Date | null)}</p></div>
client/src/components/crypto/SetupMonitorWorkspace.tsx:67:function PlanSection({ title, plan, progress, muted = false }: { title: string; plan: Record<string, any>; progress: any[]; muted?: boolean }) { const entry = json(plan.entryZone); const stop = json(plan.stop); const invalidation = json(plan.invalidation); const targets = Array.isArray(plan.targets) ? plan.targets : []; return <section className={cn("rounded-xl border p-4", muted ? "border-white/[.06] bg-white/[.015]" : "border-cyan-300/15 bg-cyan-300/[.025]")}><div className="flex items-center justify-between gap-3"><p className="text-[10px] font-semibold uppercase tracking-[.15em] text-cyan-200">{title}</p>{plan.opportunityScore != null ? <span className="text-[10px] text-slate-500">Opportunity {String(plan.opportunityScore)}/100</span> : null}</div><div className="mt-3 grid gap-2 sm:grid-cols-2"><Fact label="Direction" value={String(plan.direction ?? "UNAVAILABLE")} /><Fact label="Entry zone" value={entry.low != null && entry.high != null ? `${price(entry.low)} – ${price(entry.high)}` : "UNAVAILABLE"} /><Fact label="Preferred entry" value={price(entry.preferred)} /><Fact label="Current price" value={price(plan.currentPrice)} /><Fact label="Stop / invalidation" value={stop.price != null ? price(stop.price) : invalidation.price != null ? price(invalidation.price) : "UNAVAILABLE"} /><Fact label="Distance to invalidation" value={progress[0]?.distanceToInvalidationPercent != null ? `${progress[0].distanceToInvalidationPercent}%` : "UNAVAILABLE"} /><Fact label="R:R to first target" value={plan.rewardRisk != null ? `${plan.rewardRisk}:1` : targets[0]?.rMultiple != null ? `${targets[0].rMultiple}:1` : "UNAVAILABLE"} /><Fact label="Data quality" value={String(plan.validationStatus ?? plan.freshness ?? "UNAVAILABLE")} /></div>{plan.confirmationRequirements?.length || plan.confirmation ? <p className="mt-3 text-xs leading-5 text-amber-100"><strong>Confirmation:</strong> {String(plan.confirmation ?? plan.confirmationRequirements?.[0])}</p> : null}<div className="mt-3 space-y-2">{progress.length ? progress.map((target: any, index: number) => <div key={`${target.label}-${index}`}><div className="flex items-center justify-between gap-2 text-[10px]"><span className="text-slate-300">{target.label ?? `TP${index + 1}`} · {price(target.price)}</span><span className={target.reached ? "text-emerald-200" : "text-slate-500"}>{target.reached ? "REACHED" : `${target.progressPercent ?? 0}% toward target`}</span></div><div className="mt-1 h-1.5 overflow-hidden rounded-full bg-white/[.08]"><div className={cn("h-full rounded-full", target.reached ? "bg-emerald-300" : "bg-cyan-300")} style={{ width: `${Math.min(100, Math.max(0, Number(target.progressPercent ?? 0)))}%` }} /></div></div>) : <p className="text-xs text-slate-500">No technically justified target path is available.</p>}</div><p className="mt-3 text-[10px] leading-4 text-slate-500">Server-derived evidence · Provider: {String(plan.provider ?? "UNAVAILABLE")} · Timeframe: {Array.isArray(plan.timeframe) ? plan.timeframe.join(" / ") : String(plan.timeframe ?? "UNAVAILABLE")} · Captured: {date(plan.capturedAt as number | null)}</p></section>; }
client/src/pages/ComponentShowcase.tsx:58:  DialogContent,
client/src/pages/ComponentShowcase.tsx:251:                <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
client/src/pages/ComponentShowcase.tsx:330:                <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
client/src/pages/ComponentShowcase.tsx:620:                  <div className="grid grid-cols-2 gap-4">
client/src/pages/ComponentShowcase.tsx:894:              <TabsList className="grid w-full grid-cols-3">
client/src/pages/ComponentShowcase.tsx:1027:                    <DialogContent>
client/src/pages/ComponentShowcase.tsx:1056:                    </DialogContent>
client/src/pages/ComponentShowcase.tsx:1334:                        sonnerToast.error("Operation failed", {
client/src/pages/ComponentShowcase.tsx:1366:                        sonnerToast.loading("Loading", {
client/src/pages/ComponentShowcase.tsx:1380:                          loading: "Processing...",
client/src/pages/ComponentShowcase.tsx:1382:                          error: "Processing failed",
client/src/pages/ComponentShowcase.tsx:1403:                      Features markdown rendering, auto-scrolling, and loading states.
client/src/pages/ComponentShowcase.tsx:1415:                    emptyStateMessage="How can I help you today?"
client/src/pages/Home.tsx:57:  if (value === null) return "Unavailable";
client/src/pages/Home.tsx:62:  if (value === null) return "Unavailable";
client/src/pages/Home.tsx:67:  if (value === null) return "Unavailable";
client/src/pages/Home.tsx:72:  if (!timestamp) return "Unavailable";
client/src/pages/Home.tsx:100:  return <div className="grid min-h-48 place-items-center px-6 text-center text-sm text-slate-400"><div><Database className="mx-auto mb-3 h-5 w-5 text-slate-500" /><p className="font-medium text-slate-300">No live score is available</p><p className="mt-1 max-w-sm text-xs leading-5">The system will show “Unavailable” rather than construct an opportunity when the required source data is missing.</p></div></div>;
client/src/pages/Home.tsx:105:  return <button onClick={onSelect} className={cn("grid w-full grid-cols-[36px_minmax(150px,1.4fr)_minmax(88px,.8fr)_minmax(86px,.7fr)_minmax(116px,.9fr)_minmax(106px,.8fr)_32px] items-center gap-3 border-b border-white/[.055] px-5 py-3.5 text-left transition-colors hover:bg-cyan-300/[.045] focus:outline-none focus-visible:bg-cyan-300/[.08]", selected && "bg-cyan-300/[.07] shadow-[inset_2px_0_0_#22d3ee]", !score && "opacity-70")}>
client/src/pages/Home.tsx:123:      <div className="grid grid-cols-3 gap-2"><MetricSmall label="Opportunity" value={score.score} tone="cyan" /><MetricSmall label="Confidence" value={score.confidence} tone="emerald" /><MetricSmall label="Risk safety" value={score.riskScore} tone={score.riskScore >= 65 ? "emerald" : "amber"} /></div>
client/src/pages/Home.tsx:126:      <section><h3 className="mb-2 text-[10px] font-semibold uppercase tracking-[.17em] text-slate-500">Multi-timeframe contribution</h3><div className="grid grid-cols-2 gap-2">{score.technicalByTimeframe.map(analysis => <div key={analysis.timeframe} className="rounded-lg border border-white/[.06] bg-white/[.02] px-3 py-2.5"><div className="flex items-center justify-between"><span className="font-mono text-xs text-slate-300">{analysis.timeframe.toUpperCase()}</span><span className={cn("text-[10px] font-semibold uppercase", analysis.bias === "bullish" ? "text-emerald-300" : analysis.bias === "bearish" ? "text-rose-300" : "text-slate-400")}>{analysis.bias}</span></div><div className="mt-2 flex items-end justify-between"><span className="font-mono text-lg text-slate-100">{analysis.score}<span className="text-[10px] text-slate-500">/10</span></span><span className="text-[10px] text-slate-500">RSI {analysis.rsi ?? "—"}</span></div></div>)}</div></section>
client/src/pages/Home.tsx:142:    const grouped = new Map<string, { provider: string; status: "live" | "stale" | "unavailable"; lastSuccess: number | null; lastError: string | null; errorClass: string | null; affected: Set<string> }>();
client/src/pages/Home.tsx:145:      const item = grouped.get(provider) ?? { provider, status: "live" as const, lastSuccess: null, lastError: null, errorClass: null, affected: new Set<string>() };
client/src/pages/Home.tsx:148:      if (status.status === "unavailable") { item.status = "unavailable"; item.lastError = status.message ?? "Provider unavailable."; item.errorClass = status.errorClass ?? "PROVIDER_REQUEST_FAILED"; }
client/src/pages/Home.tsx:157:  const lastFailure = monitorChecks.find(check => check.status === "unavailable" && (check.details as Record<string, unknown>)?.expectedUnavailable !== true) ?? null;
client/src/pages/Home.tsx:160:    <div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-lg bg-black/15"><Activity className="h-4 w-4" /></div><div><div className="flex items-center gap-2"><span className="text-[10px] font-semibold uppercase tracking-[.18em] opacity-65">Market regime</span><span className="h-1 w-1 rounded-full bg-current opacity-80" /><span className="font-mono text-sm font-bold">{regime?.classification ?? "UNAVAILABLE"}</span></div><p className="mt-0.5 text-xs opacity-75">{regime ? `Regime score ${regime.score}/100 · BTC dominance ${regime.btcDominance === null ? "Unavailable" : `${decimal.format(regime.btcDominance)}%`} · Breadth ${regime.breadth === null ? "Unavailable" : `${decimal.format(regime.breadth)}%`}` : "The source data needed to classify the market environment is unavailable."}</p></div></div>
client/src/pages/Home.tsx:161:    {providerHealth.length ? <div className="mt-3 grid gap-2 border-t border-current/10 pt-3 sm:grid-cols-2"><span className="col-span-full text-[9px] font-semibold uppercase tracking-[.14em] opacity-65">Live OHLCV provider health</span>{providerHealth.map(item => <div key={item.provider} className="rounded-md bg-black/10 px-2.5 py-2 text-[10px]"><div className="flex items-center justify-between gap-2"><span className="font-medium">SOURCE: {item.provider}</span><span className={item.status === "live" ? "text-emerald-200" : "text-rose-200"}>DATA QUALITY: {item.status === "live" ? "VALID" : "UNAVAILABLE"}</span></div><p className="mt-1 opacity-75">OHLCV · {Array.from(item.affected).join(", ")} · {item.lastSuccess ? `last success ${timeAgo(item.lastSuccess)}` : `last error ${item.errorClass ?? "UNAVAILABLE"}`}</p>{item.lastError ? <p className="mt-1 truncate text-rose-100/80">{item.lastError}</p> : null}</div>)}</div> : null}
client/src/pages/Home.tsx:162:    <div className="mt-3 rounded-md border border-current/10 bg-black/10 px-2.5 py-2 text-[10px]"><div className="flex items-center justify-between"><span className="font-medium">Scheduled Provider Monitor</span><span>{monitorSummary?.monitor?.lastStatus ?? "PENDING"}</span></div><p className="mt-1 opacity-75">Primary Provider: {primaryMonitor?.provider ?? "Pending"} · Fallback Provider: Kraken Spot · Fallback Usage: {fallbackUsedCheck ? "USED IN CONTROLLED 451 CHECK" : "AVAILABLE / NOT USED"}</p><p className="mt-1 opacity-75">Last Successful Request: {primaryMonitor?.status === "live" ? timeAgo(primaryMonitor.createdAt.getTime()) : "Unavailable"} · Last Failure: {lastFailure?.classification ?? "None recorded"} · Last Monitoring Run: {monitorSummary?.execution?.completedAt ? timeAgo(monitorSummary.execution.completedAt.getTime()) : "Pending"}</p></div>
client/src/pages/Home.tsx:167:  const { online, connectionState, liveDataAvailable, updateReady, markReadSnapshot, markLiveDataUnavailable } = usePwaStatus();
client/src/pages/Home.tsx:188:  const { data: scan, isLoading, isFetching, error } = trpc.crypto.scanner.useQuery({ forceRefresh: refreshNonce > 0 }, { refetchOnWindowFocus: false, staleTime: 45_000, retry: 1 });
client/src/pages/Home.tsx:196:  useEffect(() => { if (online && error) markLiveDataUnavailable(); }, [online, error, markLiveDataUnavailable]);
client/src/pages/Home.tsx:231:        <div className="p-4 sm:p-6"><div className="mb-5 grid gap-4 xl:grid-cols-[1.45fr_.9fr]"><RegimeBanner scan={scan} /><div className="flex items-center justify-between rounded-xl border border-white/[.07] bg-white/[.025] px-4 py-3"><div><p className="text-[10px] font-semibold uppercase tracking-[.16em] text-slate-500">Data quality</p><p className="mt-1 text-xs text-slate-300">{scan?.dataStatus.filter(status => status.status === "live").length ?? 0} live provider feeds · {scan?.dataStatus.filter(status => status.status !== "live").length ?? 0} unavailable</p></div><ShieldCheck className="h-5 w-5 text-cyan-300" /></div></div>
client/src/pages/Home.tsx:232:          {error ? <div className="mb-5 rounded-xl border border-rose-300/20 bg-rose-300/[.05] p-4 text-sm text-rose-100"><div className="flex gap-2"><AlertTriangle className="h-4 w-4 shrink-0" /><div><p className="font-semibold">Live scanner unavailable</p><p className="mt-1 text-xs text-rose-100/70">{error.message}. No market or score values have been generated.</p></div></div></div> : null}
client/src/pages/Home.tsx:233:          <section className="mb-5 flex flex-wrap items-center justify-between gap-4 rounded-xl border border-cyan-300/15 bg-cyan-300/[.03] p-4"><div><p className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Opportunity Discovery</p><p className="mt-1 max-w-2xl text-xs leading-5 text-slate-400">Inspect validated Swing evidence as Qualified, Potential, Watch, No Trade, or Data Unavailable without changing Opportunity Score or creating an automatic trade.</p></div><Button size="sm" variant="outline" onClick={openDiscoveryWorkspace} className="border-cyan-300/20 bg-cyan-300/[.05] text-cyan-100"><Sparkles className="mr-1.5 h-3.5 w-3.5" />Open Discovery</Button></section>
client/src/pages/Home.tsx:234:          <section className="mb-5"><div className="mb-3 flex items-end justify-between"><div><span className="text-[10px] font-semibold uppercase tracking-[.18em] text-cyan-300">Live overview</span><h3 className="mt-1 text-lg font-semibold tracking-tight">Highest current research signals</h3></div><span className="hidden text-[11px] text-slate-500 md:block">Ranked from active configuration, timestamped market inputs</span></div><div className="grid gap-3 md:grid-cols-3">{isLoading ? [0, 1, 2].map(key => <Skeleton key={key} className="h-[146px] bg-white/[.04]" />) : topRows.length ? topRows.map((row, index) => <button key={row.asset.id} onClick={() => setSelectedId(row.asset.id)} className={cn("group rounded-xl border p-4 text-left transition-all hover:-translate-y-0.5", index === 0 ? "border-cyan-300/25 bg-cyan-300/[.055] shadow-[0_10px_40px_rgba(8,145,178,.09)]" : "border-white/[.07] bg-white/[.025] hover:border-white/[.14]")}><div className="flex items-start justify-between"><span className="flex items-center gap-2"><span className="grid h-8 w-8 place-items-center rounded-lg bg-slate-800 font-mono text-xs font-bold text-cyan-200">{row.asset.symbol.slice(0, 2)}</span><span><span className="block text-sm font-semibold">{row.asset.symbol}</span><span className="block text-[10px] uppercase tracking-[.15em] text-slate-500">Rank {String(index + 1).padStart(2, "0")}</span></span></span><span className={cn("font-mono text-3xl font-semibold", scoreClass(row.score?.score))}>{row.score?.score ?? "—"}</span></div><div className="mt-4 flex items-end justify-between"><div><span className="font-mono text-sm text-slate-200">{formatPrice(row.asset.price)}</span><span className={cn("mt-1 block text-[11px]", changeClass(row.asset.change24h))}>{formatPct(row.asset.change24h)} <span className="text-slate-600">24H</span></span></div><span className="rounded-md border border-white/[.07] bg-black/15 px-2 py-1 text-[10px] text-slate-400">{row.score?.setupType}</span></div></button>) : <div className="col-span-3 rounded-xl border border-white/[.07] bg-white/[.025]"><EmptyRow /></div>}</div></section>
client/src/pages/Home.tsx:235:          <div className="grid gap-5 2xl:grid-cols-[minmax(0,1fr)_290px]"><section id="market-scanner" className="scroll-mt-24 overflow-hidden rounded-xl border border-white/[.07] bg-[#090f1b]/70"><div className="flex flex-col gap-3 border-b border-white/[.07] p-4 xl:flex-row xl:items-center xl:justify-between"><div><span className="text-[10px] font-semibold uppercase tracking-[.17em] text-cyan-300">Scanner</span><h3 className="mt-0.5 text-base font-semibold">Ranked opportunity universe</h3></div><div className="flex flex-wrap items-center gap-2"><label className="flex h-8 items-center gap-2 rounded-md border border-white/[.08] bg-white/[.025] px-2.5 text-[11px] text-slate-400">Min score <select value={minScore} onChange={event => setMinScore(Number(event.target.value))} className="bg-transparent text-slate-200 outline-none"><option value={0}>All</option><option value={50}>50+</option><option value={65}>65+</option><option value={75}>75+</option></select><ChevronDown className="h-3 w-3" /></label><label className="flex h-8 items-center gap-2 rounded-md border border-white/[.08] bg-white/[.025] px-2.5 text-[11px] text-slate-400">Sector <select value={sector} onChange={event => setSector(event.target.value)} className="max-w-24 bg-transparent text-slate-200 outline-none"><option>All sectors</option>{sectors.slice(1).map(item => <option key={item}>{item}</option>)}</select><ChevronDown className="h-3 w-3" /></label><Button variant="ghost" size="sm" onClick={() => toast.message("All active scanner inputs are currently visible in the detail card.")} className="h-8 px-2.5 text-xs text-cyan-200 hover:bg-cyan-300/[.08] hover:text-cyan-100"><SlidersHorizontal className="mr-1.5 h-3.5 w-3.5" />Filters</Button></div></div><div className="scanner-head grid grid-cols-[36px_minmax(150px,1.4fr)_minmax(88px,.8fr)_minmax(86px,.7fr)_minmax(116px,.9fr)_minmax(106px,.8fr)_32px] gap-3 border-b border-white/[.055] px-5 py-2 text-[9px] font-semibold uppercase tracking-[.14em] text-slate-600"><span>#</span><span>Asset / sector</span><span>Price / 24h</span><span>Score</span><span>Setup</span><span>Confidence</span><span /></div>{isLoading ? <div className="space-y-0 p-5">{[0, 1, 2, 3, 4].map(key => <Skeleton key={key} className="mb-3 h-12 bg-white/[.035]" />)}</div> : filteredRows.length ? filteredRows.map((row, index) => <OpportunityRow key={row.asset.id} row={row} rank={index + 1} selected={selected?.asset.id === row.asset.id} onSelect={() => setSelectedId(row.asset.id)} />) : <EmptyRow />}<div className="flex items-center justify-between bg-white/[.015] px-5 py-3 text-[10px] text-slate-500"><span>{filteredRows.length} of {rows.length} tracked assets shown</span><span>CoinGecko + Binance public data</span></div></section>
client/src/pages/Home.tsx:236:            <section className="space-y-5"><div className="rounded-xl border border-white/[.07] bg-[#090f1b]/70 p-4"><div className="flex items-center justify-between"><div><span className="text-[10px] font-semibold uppercase tracking-[.17em] text-cyan-300">Rotation</span><h3 className="mt-0.5 text-sm font-semibold">Sector relative strength</h3></div><Layers3 className="h-4 w-4 text-slate-500" /></div><div className="mt-4 space-y-3">{sectorRotation.length ? sectorRotation.map((item, index) => <div key={item.name}><div className="mb-1.5 flex justify-between text-xs"><span className="text-slate-300">{item.name}</span><span className={scoreClass(item.score)}>{decimal.format(item.score)}</span></div><Progress value={item.score} className="h-1.5 bg-slate-800 [&>div]:bg-cyan-300" /></div>) : <p className="text-xs text-slate-500">Unavailable until live asset scores are calculated.</p>}</div></div><div className="rounded-xl border border-white/[.07] bg-[#090f1b]/70 p-4"><div className="flex items-center justify-between"><div><span className="text-[10px] font-semibold uppercase tracking-[.17em] text-cyan-300">Configuration</span><h3 className="mt-0.5 text-sm font-semibold">Scoring controls</h3></div><Settings2 className="h-4 w-4 text-slate-500" /></div><div className="mt-4 space-y-3 text-xs"><ControlRow label="Technical weight" value="40%" /><ControlRow label="Market momentum" value="20%" /><ControlRow label="Liquidity / risk" value="15%" /><ControlRow label="Enabled timeframes" value="4" /></div><button onClick={() => toast.message("Editable scoring settings are part of the next authenticated milestone.", { description: "The current default configuration is documented and persisted with every scan." })} className="mt-4 flex w-full items-center justify-center gap-2 rounded-md border border-cyan-300/20 bg-cyan-300/[.06] py-2 text-[11px] font-medium text-cyan-100 transition-colors hover:bg-cyan-300/[.1]"><Settings2 className="h-3.5 w-3.5" />View configuration</button></div><div className="rounded-xl border border-white/[.07] bg-white/[.018] p-4"><div className="flex gap-3"><BookOpen className="mt-0.5 h-4 w-4 shrink-0 text-slate-500" /><p className="text-[11px] leading-5 text-slate-500">The scanner reports inputs, data freshness, scoring components, and missing conditions. It does not claim a score predicts price movement.</p></div></div></section></div>
client/src/pages/Home.tsx:238:      </main></div><DetailPanel row={selected} onClose={() => setSelectedId(null)} onPaperTrade={openPaperWorkspace} /><SettingsPanel open={settingsOpen} onOpenChange={setSettingsOpen} />{setupMonitorOpen ? <section className="fixed inset-0 z-[69] overflow-y-auto bg-[#060a12] text-slate-100"><div className="terminal-grid fixed inset-0 pointer-events-none opacity-35" /><div className="relative mx-auto min-h-screen max-w-[1800px] px-4 py-5 sm:px-6 lg:px-10"><SetupMonitorWorkspace /></div></section> : null}{discoveryOpen ? <section className="fixed inset-0 z-[69] overflow-y-auto bg-[#060a12] text-slate-100"><div className="terminal-grid fixed inset-0 pointer-events-none opacity-35" /><div className="relative mx-auto min-h-screen max-w-[1800px] px-4 py-5 sm:px-6 lg:px-10"><OpportunityDiscoveryWorkspace onBack={() => { setDiscoveryOpen(false); setActiveNav("Dashboard"); if (typeof window !== "undefined") window.history.replaceState(null, "", window.location.pathname); }} onInspectAsset={assetId => { setSelectedId(assetId); setAssetIntelligenceOpen(true); }} onPaperTrade={assetId => openPaperWorkspace(rows.find(row => row.asset.id === assetId) ?? null, "SWING")} /></div></section> : null}{setupMode ? <section className="fixed inset-0 z-[69] overflow-y-auto bg-[#060a12] text-slate-100"><div className="terminal-grid fixed inset-0 pointer-events-none opacity-35" /><div className="relative mx-auto min-h-screen max-w-[1800px] px-4 py-5 sm:px-6 lg:px-10"><TradeSetupWorkspace mode={setupMode} onBack={() => { setSetupMode(null); setActiveNav("Dashboard"); }} onInspectAsset={assetId => { setSelectedId(assetId); setAssetIntelligenceOpen(true); }} onPaperTrade={(assetId, mode) => openPaperWorkspace(rows.find(row => row.asset.id === assetId) ?? null, mode)} /></div></section> : null}{paperOpen ? <section className="fixed inset-0 z-[70] overflow-y-auto bg-[#060a12] text-slate-100"><div className="terminal-grid fixed inset-0 pointer-events-none opacity-35" /><div className="relative mx-auto min-h-screen max-w-[1800px] px-4 py-5 sm:px-6 lg:px-10"><PaperTradingWorkspace asset={paperAsset} setupMode={paperSetupMode} onBack={closePaperWorkspace} /></div></section> : null}<BacktestingPanel open={backtestingOpen} onOpenChange={setBacktestingOpen} selectedAsset={selected} /><AlertsPanel open={alertsOpen} onOpenChange={setAlertsOpen} onInspectAsset={assetId => { setSelectedId(assetId); setAlertsOpen(false); setAssetIntelligenceOpen(true); }} /><ResearchSummaryPanel open={researchSummaryOpen} onOpenChange={setResearchSummaryOpen} /><OpportunityResearchLab open={researchLabOpen} onOpenChange={setResearchLabOpen} /><HistoricalDataFoundation open={historicalDataOpen} onOpenChange={setHistoricalDataOpen} /><ExecutionCostLab open={executionCostLabOpen} onOpenChange={setExecutionCostLabOpen} /><BackupRecoveryPanel open={backupRecoveryOpen} onOpenChange={setBackupRecoveryOpen} />
client/src/pages/Home.tsx:253:  return <nav aria-label="Mobile workspace navigation" className="pwa-mobile-nav fixed inset-x-0 bottom-0 z-[60] flex border-t border-white/[.08] bg-[#080d17]/95 px-1 pt-1 backdrop-blur-xl lg:hidden">{items.map(item => { const Icon = item.icon; return <button key={item.label} onClick={item.onClick} aria-current={item.active ? "page" : undefined} className={cn("flex min-w-0 flex-1 flex-col items-center gap-1 rounded-lg px-1 py-2 text-[10px] font-medium", item.active ? "text-cyan-200" : "text-slate-500")}><Icon className="h-4 w-4" /><span className="truncate">{item.label}</span></button>; })}</nav>;
client/src/components/DashboardLayout.tsx:49:  const { loading, user } = useAuth();
client/src/components/DashboardLayout.tsx:55:  if (loading) {
client/src/pwa/PwaStatus.tsx:15:  markLiveDataUnavailable: () => void;
client/src/pwa/PwaStatus.tsx:86:  const markLiveDataUnavailable = useCallback(() => {
client/src/pwa/PwaStatus.tsx:92:  const value = useMemo(() => ({ online, connectionState, liveDataAvailable, lastOnlineAt, updateReady, activateUpdate, markReadSnapshot, markLiveDataUnavailable }), [online, connectionState, liveDataAvailable, lastOnlineAt, updateReady, activateUpdate, markReadSnapshot, markLiveDataUnavailable]);
client/src/pwa/PwaMobileNavigation.tsx:26:  return <nav aria-label="Mobile workspace navigation" className="pwa-mobile-nav fixed inset-x-0 bottom-0 z-[80] flex border-t border-white/[.08] bg-[#080d17]/95 px-1 pt-1 backdrop-blur-xl lg:hidden">{moreOpen ? <div id="pwa-mobile-more" className="pwa-mobile-more absolute inset-x-2 bottom-full mb-2 rounded-xl border border-white/[.1] bg-[#0b1321]/98 p-2 shadow-2xl"><p className="px-2 py-1 text-[9px] font-semibold uppercase tracking-[.16em] text-slate-500">More workspaces</p>{moreItems.map(item => { const Icon = item.icon; return <button key={item.target} onClick={() => choose(item.target)} className="flex w-full items-center gap-3 rounded-lg px-3 py-3 text-left text-xs text-slate-200 hover:bg-white/[.06]"><Icon className="h-4 w-4 text-cyan-200" />{item.label}</button>; })}</div> : null}{primaryItems.map(item => { const Icon = item.icon; return <button key={item.target} onClick={() => choose(item.target)} className="flex min-w-0 flex-1 flex-col items-center gap-1 rounded-lg px-1 py-2 text-[10px] font-medium text-slate-400 hover:bg-white/[.05] hover:text-cyan-100"><Icon className="h-4 w-4" /><span className="truncate">{item.label}</span></button>; })}<button aria-expanded={moreOpen} aria-controls="pwa-mobile-more" onClick={() => setMoreOpen(value => !value)} className="flex min-w-0 flex-1 flex-col items-center gap-1 rounded-lg px-1 py-2 text-[10px] font-medium text-slate-400 hover:bg-white/[.05] hover:text-cyan-100"><Menu className="h-4 w-4" /><span className="truncate">More</span></button></nav>;
== CURRENT STATUS TOKEN SIGNALS ==
client/src/components/crypto/HistoricalDataFoundation.tsx:54:  return <section className="overflow-hidden rounded-xl border border-cyan-300/15 bg-cyan-300/[.025]"><div className="flex flex-wrap items-start justify-between gap-3 border-b border-cyan-300/10 px-4 py-3"><div className="flex gap-2"><span className="mt-0.5 grid h-7 w-7 place-items-center rounded-md bg-cyan-300/[.1] text-cyan-200"><Layers3 className="h-3.5 w-3.5" /></span><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Market Coverage Matrix</span><p className="mt-1 text-xs text-slate-500">Registry-backed availability and quality evidence by selected immutable dataset.</p></div></div><span className="rounded-full border border-cyan-300/20 bg-cyan-300/[.06] px-2 py-1 text-[9px] font-semibold text-cyan-100">{rows.length} registry assets</span></div><div className="grid gap-2 border-b border-cyan-300/10 bg-amber-300/[.035] p-3 lg:grid-cols-2"><Warning title="CURRENT SURVIVOR UNIVERSE" detail={snapshot?.survivorshipWarning ?? "This registry is an intentional source-supported candidate set, not a historical market-membership reconstruction. Delisted and inactive assets may be absent."} /><Warning title="HISTORICAL SECTOR DATA UNAVAILABLE" detail="Registry sector labels organize coverage only. They are not substituted for point-in-time historical sector membership, rank, or sector momentum." /></div>{loading ? <Loading /> : <div className="max-h-[440px] overflow-auto"><table className="w-full min-w-[1540px] text-left text-xs"><thead className="sticky top-0 z-10 bg-[#0d1421] text-[9px] uppercase tracking-[.14em] text-slate-500"><tr><th className="px-4 py-3">Asset / inclusion</th><th className="px-3 py-3">15M</th><th className="px-3 py-3">1H</th><th className="px-3 py-3">4H</th><th className="px-3 py-3">1D</th><th className="px-3 py-3">Market cap</th><th className="px-3 py-3">Regime</th><th className="px-3 py-3">Historical sector</th><th className="px-3 py-3">Aggregate coverage</th><th className="px-3 py-3">Longest gap</th><th className="px-3 py-3">Quality</th><th className="px-3 py-3">Latest observed</th></tr></thead><tbody>{rows.map(row => { const aggregate = aggregateRow(row); return <tr key={row.assetId} className="border-t border-white/[.055] align-top text-slate-300"><td className="px-4 py-3"><div className="flex items-center gap-2"><strong>{row.symbol}</strong><span className="rounded border border-white/[.1] px-1.5 py-0.5 font-mono text-[8px] text-slate-400">{row.priorityTier.replace("_", " ")}</span></div><p className="mt-1 max-w-64 text-[10px] leading-4 text-slate-500">{row.inclusionReason}</p><span className="mt-1 inline-block text-[9px] text-slate-400">Registry taxonomy: {row.registrySector ?? "Unassigned"}</span></td>{(["15m", "1h", "4h", "1d"] as const).map(frame => <td key={frame} className="px-3 py-3"><FrameCell scope={row.timeframes[frame]} /></td>)}<td className="px-3 py-3"><Pill value={row.marketCapStatus} /></td><td className="px-3 py-3"><Pill value={row.regimeStatus} /></td><td className="px-3 py-3"><Pill value={row.sectorStatus} /></td><td className="px-3 py-3 font-mono text-slate-200">{aggregate.coverage.toFixed(2)}%<span className="mt-1 block text-[9px] text-slate-500">{aggregate.observed}/{aggregate.expected}</span></td><td className="px-3 py-3 font-mono">{gapLabel(aggregate.longestGap)}</td><td className="px-3 py-3"><Pill value={row.dataQualityStatus} label={aggregate.score === null ? row.dataQualityStatus : `${aggregate.score.toFixed(2)} · ${row.dataQualityStatus}`} /></td><td className="px-3 py-3 text-[10px] text-slate-500">{local(row.lastObservedAt)}</td></tr>; })}{!rows.length ? <tr><td colSpan={12} className="px-4 py-8 text-center text-slate-500">No registry or coverage snapshot is available for this dataset.</td></tr> : null}</tbody></table></div>}</section>;
client/src/components/crypto/ExecutionCostLab.tsx:71:function PreviewView({ preview }: { preview: any }) { const outcome = preview.outcome; const availability = preview.state?.funding?.status; const sample = preview.sensitivity?.filter((row: any) => row.tradeSizeUsd === 10_000 && row.feePercent === 0.1) ?? []; return <div className="space-y-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><span className="text-[10px] font-semibold uppercase tracking-[.18em] text-violet-200">Transparent cost preview</span><h3 className="mt-1 text-lg font-semibold">Gross result → declared execution costs → net result</h3><p className="mt-1 text-[11px] text-slate-500">Sealed dataset {preview.provenance.dataset.version} · {preview.provenance.dataset.fingerprint?.slice(0, 12) ?? "no fingerprint"}…</p></div><Pill value={outcome.availability} /></div><section className="rounded-xl border border-amber-300/15 bg-amber-300/[.035] p-4"><div className="flex gap-2"><ShieldAlert className="mt-0.5 h-4 w-4 shrink-0 text-amber-200" /><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-amber-200">Research-only and point-in-time</span><p className="mt-1 text-xs leading-5 text-amber-100/75">Historical order-book data is unavailable and is not fabricated. Liquidity impact is labeled as an estimate from the stored OHLCV window. This preview does not create a Research Lab experiment or change any live configuration.</p></div></div></section><div className="grid gap-2 sm:grid-cols-4"><Metric label="Gross return" value={fmt(outcome.gross.returnPercent, "%")} /><Metric label="Gross P/L" value={money(outcome.gross.pnlUsd)} /><Metric label="Total costs" value={money(outcome.totalTradingCostsUsd)} /><Metric label="Net return" value={fmt(outcome.netReturnPercent, "%")} /></div><section className="overflow-hidden rounded-xl border border-white/[.07] bg-white/[.02]"><div className="border-b border-white/[.07] px-4 py-3"><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-violet-200">Cost breakdown</span><p className="mt-1 text-xs text-slate-500">Every charge is shown separately; an unavailable required component leaves net output unavailable.</p></div><div className="overflow-auto"><table className="w-full min-w-[760px] text-left text-xs"><thead className="bg-black/10 text-[9px] uppercase tracking-[.14em] text-slate-500"><tr><th className="px-4 py-3">Component</th><th className="px-3 py-3">Entry</th><th className="px-3 py-3">Exit</th><th className="px-3 py-3">Funding</th><th className="px-3 py-3">Status</th></tr></thead><tbody><Breakdown label="Fee" entry={outcome.entry.fee} exit={outcome.exit.fee} /><Breakdown label="Fixed slippage" entry={outcome.entry.slippage} exit={outcome.exit.slippage} /><Breakdown label="Estimated liquidity impact" entry={outcome.entry.liquidityImpact} exit={outcome.exit.liquidityImpact} /><tr className="border-t border-white/[.06]"><td className="px-4 py-3 font-medium text-slate-200">Funding</td><td className="px-3 py-3 text-slate-600">—</td><td className="px-3 py-3 text-slate-600">—</td><td className="px-3 py-3 font-mono text-slate-200">{money(outcome.funding.amountUsd)}<span className="ml-2 text-[9px] text-slate-500">{outcome.funding.mode}</span></td><td className="px-3 py-3"><Pill value={outcome.funding.status} /></td></tr></tbody></table></div></section><div className="grid gap-5 xl:grid-cols-2"><section className="rounded-xl border border-white/[.07] bg-white/[.02] p-4"><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-violet-200">Liquidity evidence</span><div className="mt-3 grid grid-cols-2 gap-2"><Metric label="Entry tier" value={`${outcome.liquidity.entry.tier} · ${outcome.liquidity.entry.status}`} /><Metric label="Exit tier" value={`${outcome.liquidity.exit.tier} · ${outcome.liquidity.exit.status}`} /><Metric label="Entry quote volume" value={money(outcome.liquidity.entry.quoteVolume)} /><Metric label="Exit quote volume" value={money(outcome.liquidity.exit.quoteVolume)} /></div><p className="mt-3 text-[10px] leading-4 text-slate-500">Funding availability: {availability}. Order book: HISTORICAL ORDER-BOOK DATA UNAVAILABLE.</p></section><section className="rounded-xl border border-white/[.07] bg-white/[.02] p-4"><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-violet-200">Liquidity stress test</span><div className="mt-3 space-y-2">{(preview.stress ?? []).map((item: any) => <div key={item.scenario} className="flex items-center justify-between rounded-lg border border-white/[.06] bg-black/10 px-3 py-2"><span className="text-[10px] text-slate-300">{item.scenario.replaceAll("_", " ")}</span><span className="font-mono text-xs text-slate-100">{fmt(item.outcome.netReturnPercent, "%")}</span></div>)}</div></section></div><section className="rounded-xl border border-white/[.07] bg-white/[.02] p-4"><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-violet-200">Cost sensitivity — fee / slippage / size</span><p className="mt-1 text-xs text-slate-500">All configurations remain visible; the lab never chooses the most favorable one.</p><div className="mt-3 grid gap-2 sm:grid-cols-2 lg:grid-cols-4">{sample.slice(0, 8).map((row: any) => <div key={`${row.tradeSizeUsd}-${row.feePercent}-${row.slippagePercent}`} className="rounded-lg border border-white/[.06] bg-black/10 px-3 py-2"><span className="text-[9px] text-slate-500">{money(row.tradeSizeUsd)} · fee {row.feePercent}% · slip {row.slippagePercent}%</span><strong className="mt-1 block font-mono text-xs text-slate-100">{fmt(row.outcome.netReturnPercent, "%")}</strong></div>)}</div></section>{outcome.limitations.length ? <section className="rounded-xl border border-amber-300/15 bg-amber-300/[.035] p-4"><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-amber-200">Limitations retained</span><ul className="mt-2 space-y-1 text-[10px] leading-4 text-amber-100/75">{outcome.limitations.map((item: string) => <li key={item}>• {item}</li>)}</ul></section> : null}</div>; }
client/src/components/crypto/AssetIntelligencePanel.tsx:14:type Setup = { status: "QUALIFIED" | "POTENTIAL" | "WATCH" | "NO TRADE" | "DATA UNAVAILABLE"; maturity: string; setupReadiness: { state: string; score: number | null; explanation: string }; paperTradeEligible: boolean; exactReason: string; whyInteresting: string[]; missingEvidence: string[]; confirmationRequirements: string[]; invalidationExplanation: string; conditionalEntry: string | null; provider: string | null; dataTimestamp: number | null; freshness: string; validationStatus: string | null; timeframes: { execution: string; confirmation: string; context: string }; regime: { restricted: boolean }; readinessPlan: { availability: "SUPPORTED" | "PARTIAL" | "UNAVAILABLE"; currentPrice: number | null; entryZone: { low: number; high: number; preferred: number; reason: string } | null; confirmation: string | null; invalidation: { price: number; reason: string } | null; targets: Array<{ label: string; price: number; distancePercent: number | null; rMultiple: number | null; reason: string }>; rewardRisk: number | null; reason: string; health: { state: string; reason: string } }; sourcePlan: { entryZone: { preferred: number } | null; stop: { price: number } | null; targets: Array<{ label: string; price: number }> } };
client/src/components/crypto/AssetIntelligencePanel.tsx:32:function CurrentSetup({ setup, row, onPaperTrade, online }: { setup: Setup; row: ScannerRow; onPaperTrade: () => void; online: boolean }) { const qualified = setup.status === "QUALIFIED" && setup.paperTradeEligible; const plan = setup.readinessPlan; const conditional = plan.availability !== "UNAVAILABLE"; const tone = qualified ? "border-emerald-300/20 bg-emerald-300/[.04]" : setup.status === "DATA UNAVAILABLE" ? "border-slate-400/20 bg-slate-400/[.04]" : setup.status === "NO TRADE" ? "border-rose-300/20 bg-rose-300/[.04]" : "border-amber-300/20 bg-amber-300/[.04]"; return <section className={`rounded-xl border p-4 ${tone}`}><div className="flex flex-wrap items-start justify-between gap-3"><div><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Setup plan · validated Swing evidence</span><div className="mt-2 flex flex-wrap items-center gap-2"><strong className="text-sm text-slate-100">{setup.status}</strong><span className="rounded-full border border-white/[.1] px-2 py-0.5 text-[9px] text-slate-400">{setup.maturity}</span><span className="rounded-full border border-violet-300/20 px-2 py-0.5 text-[9px] text-violet-100">{setup.setupReadiness.state} · {setup.setupReadiness.score ?? "UNAVAILABLE"}{setup.setupReadiness.score === null ? "" : "/100"}</span>{setup.regime.restricted ? <span className="rounded-full border border-rose-300/20 px-2 py-0.5 text-[9px] text-rose-200">RISK OFF RESTRICTED</span> : null}</div><p className="mt-2 max-w-3xl text-xs leading-5 text-slate-300">{setup.exactReason}</p></div>{qualified ? <Button onClick={onPaperTrade} disabled={!online || row.asset.price === null} className="bg-emerald-300 text-slate-950 hover:bg-emerald-200 disabled:bg-slate-600"><WalletCards className="mr-2 h-4 w-4" />Paper trade qualified Swing</Button> : null}</div><div className="mt-3 grid gap-2 sm:grid-cols-2 lg:grid-cols-4"><Mini label="Setup readiness" value={setup.setupReadiness.explanation} /><Mini label="Provider / freshness" value={`${setup.provider ?? "UNAVAILABLE"} · ${setup.freshness}`} /><Mini label="Timeframes" value={`${setup.timeframes.execution.toUpperCase()} / ${setup.timeframes.confirmation.toUpperCase()} / ${setup.timeframes.context.toUpperCase()}`} /><Mini label="Validation" value={setup.validationStatus ?? "UNAVAILABLE"} /></div>{qualified ? <div className="mt-3 grid gap-2 sm:grid-cols-3"><Mini label="Preferred entry" value={setup.sourcePlan.entryZone ? currency.format(setup.sourcePlan.entryZone.preferred) : "UNAVAILABLE"} /><Mini label="Stop" value={setup.sourcePlan.stop ? currency.format(setup.sourcePlan.stop.price) : "UNAVAILABLE"} /><Mini label="Target" value={setup.sourcePlan.targets[0] ? currency.format(setup.sourcePlan.targets[0].price) : "UNAVAILABLE"} /></div> : <div className="mt-3 rounded-lg border border-white/[.08] bg-black/10 p-3"><p className="text-[9px] font-semibold uppercase tracking-[.14em] text-amber-200">Not yet eligible for Paper Trading</p><p className="mt-1 text-xs leading-5 text-slate-400">Potential and Watch remain monitoring states; server-side qualification and explicit confirmation are still required.</p>{setup.whyInteresting.length ? <ul className="mt-3 space-y-1 text-xs leading-5 text-emerald-100/85">{setup.whyInteresting.map(reason => <li key={reason}>• Working: {reason}</li>)}</ul> : null}{setup.confirmationRequirements.length ? <ul className="mt-3 space-y-1 text-xs leading-5 text-amber-100/85">{setup.confirmationRequirements.map(reason => <li key={reason}>• Waiting for: {reason}</li>)}</ul> : null}{conditional ? <div className="mt-3 border-t border-white/[.07] pt-3"><p className="text-[9px] font-semibold uppercase tracking-[.14em] text-violet-200">Conditional technical plan — not a trade instruction</p><p className="mt-1 text-xs leading-5 text-slate-400">{plan.reason}</p><div className="mt-2 grid gap-2 sm:grid-cols-2 lg:grid-cols-4">{plan.entryZone ? <Mini label="Entry zone" value={`${currency.format(plan.entryZone.low)} – ${currency.format(plan.entryZone.high)}`} /> : null}{plan.invalidation ? <Mini label="Technical invalidation" value={currency.format(plan.invalidation.price)} /> : null}{plan.targets.map(target => <Mini key={target.label} label={`${target.label} · ${target.rMultiple === null ? "R unavailable" : `${target.rMultiple}R`}`} value={currency.format(target.price)} />)}{plan.rewardRisk !== null ? <Mini label="Current first R:R" value={`${plan.rewardRisk}:1`} /> : null}</div>{plan.confirmation ? <p className="mt-2 text-xs leading-5 text-cyan-100/85"><strong>Confirmation:</strong> {plan.confirmation}</p> : null}<p className="mt-2 text-xs leading-5 text-rose-100/85"><strong>Invalidation:</strong> {setup.invalidationExplanation}</p></div> : <p className="mt-3 text-xs leading-5 text-slate-500">Entry zone, targets, and invalidation remain unavailable because the existing validated setup engine did not derive all required evidence.</p>}<p className="mt-3 text-[10px] leading-4 text-slate-500">Trade Health: {plan.health.state}. {plan.health.reason} Provenance timestamp: {time(setup.dataTimestamp)}.</p></div>}</section>; }
client/src/components/crypto/PaperTradingWorkspace.tsx:13:type LowTimeframeSnapshot = { direction?: "LONG" | "SHORT" | "NO TRADE"; provider?: string | null; dataTimestamp?: number | null; currentPrice?: number | null; alignment?: string; entryZone?: { low?: number; high?: number; preferred?: number; reason?: string; state?: string }; stop?: { price?: number; reason?: string }; invalidation?: { price?: number; reason?: string }; targets?: Array<{ label: string; price: number; reason: string; rewardRisk?: number | null }>; rewardRisk?: number | null; targetPath?: Array<{ label: string; price: number; status: string }>; timeframeStates?: Array<{ timeframe: string; bias: string; rsi: number | null; volumeExpansion: number | null; structure: string }>; dataBundle?: { state?: string; coherent?: boolean; statusMessage?: string } };
client/src/components/crypto/PaperTradingWorkspace.tsx:15:type TradeHealth = { state: "HEALTHY" | "CAUTION" | "REVERSAL RISK" | "INVALIDATED" | "HEALTH UNKNOWN"; reasons: string[]; targetProgress: Array<{ label: string; price: number; reached: boolean; status: "REACHED" | "PENDING"; progressPercent: number | null; distancePercent: number | null; reason: string }>; targetPath: { state: "HEALTHY" | "WEAKENING" | "AT RISK" | "INVALIDATED" | "UNAVAILABLE"; explanation: string }; reversalWarning: string | null; data: { provider: string | null; generatedAt: number | null; availability: "LIVE" | "STALE" | "UNAVAILABLE" } };
client/src/components/crypto/PaperTradingWorkspace.tsx:16:type LowTimeframeHealth = { state: "HEALTHY" | "WARNING" | "DANGER" | "INVALIDATED" | "HEALTH UNKNOWN"; reasons: string[]; targetPath: Array<{ label: string; price: number; status: "CURRENT" | "REACHED" | "PENDING" }>; data: { provider: string | null; generatedAt: number | null; availability: "LIVE" | "STALE" | "UNAVAILABLE" } };
client/src/components/crypto/PaperTradingWorkspace.tsx:25:const healthTone = (state: TradeHealth["state"]) => state === "HEALTHY" ? "border-emerald-300/20 bg-emerald-300/[.06] text-emerald-100" : state === "CAUTION" ? "border-amber-300/20 bg-amber-300/[.06] text-amber-100" : state === "REVERSAL RISK" || state === "INVALIDATED" ? "border-rose-300/20 bg-rose-300/[.06] text-rose-100" : "border-slate-400/20 bg-slate-400/[.06] text-slate-200";
client/src/components/crypto/PaperTradingWorkspace.tsx:65:function PortfolioDashboard({ metrics, counts, loading }: { metrics: Metrics | undefined; counts: Record<TradeHealth["state"], number>; loading: boolean }) { if (loading) return <div className="grid gap-3 sm:grid-cols-3 xl:grid-cols-6">{Array.from({ length: 12 }, (_, index) => <div key={index} className="h-20 animate-pulse rounded-xl border border-white/[.06] bg-white/[.025]" />)}</div>; if (!metrics) return null; const sampleSufficient = (metrics.closedPositions ?? 0) >= 2; const cards: Array<[string, string, "profit" | "loss" | "attention" | "neutral"]> = [["Current equity", money.format(metrics.currentEquity), metrics.totalPnl >= 0 ? "profit" : "loss"], ["Total P&L", money.format(metrics.totalPnl), metrics.totalPnl >= 0 ? "profit" : "loss"], ["Unrealized P&L", money.format(metrics.unrealizedPnl), metrics.unrealizedPnl >= 0 ? "profit" : "loss"], ["Open positions", String(metrics.openPositions), "attention"], ["Healthy", String(counts.HEALTHY), "profit"], ["Caution", String(counts.CAUTION), "attention"], ["Reversal risk", String(counts["REVERSAL RISK"]), "loss"], ["Invalidated", String(counts.INVALIDATED), "loss"], ["Win rate", sampleSufficient ? asValue(metrics.winRate, "%") : "INSUFFICIENT SAMPLE", "neutral"], ["Profit factor", sampleSufficient && metrics.profitFactor !== null ? asValue(metrics.profitFactor) : "INSUFFICIENT SAMPLE", "neutral"]]; return <section><div className="mb-3"><p className="text-[10px] font-semibold uppercase tracking-[.17em] text-emerald-200">Portfolio health</p><h3 className="mt-1 text-base font-semibold">Current simulated account state</h3></div><div className="grid gap-2 sm:grid-cols-3 xl:grid-cols-5">{cards.map(([label, value, tone]) => <Metric key={label} label={label} value={value} tone={tone} />)}</div></section>; }
client/src/components/crypto/PaperTradingWorkspace.tsx:73:    <section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Entry" value={money.format(trade.entryPrice)} tone="neutral" /><Metric label="Current price" value={trade.currentPrice === null ? "UNAVAILABLE" : money.format(trade.currentPrice)} tone="attention" /><Metric label="Unrealized P&L" value={trade.unrealizedPnl === null ? "UNAVAILABLE" : money.format(trade.unrealizedPnl)} tone={trade.unrealizedPnl === null ? "neutral" : trade.unrealizedPnl >= 0 ? "profit" : "loss"} /><Metric label="Trade Health" value={trade.tradeHealth.state} tone={trade.tradeHealth.state === "HEALTHY" ? "profit" : trade.tradeHealth.state === "CAUTION" ? "attention" : trade.tradeHealth.state === "HEALTH UNKNOWN" ? "neutral" : "loss"} /></section>
client/src/components/crypto/PaperTradingWorkspace.tsx:81:function LowTimeframeHealthPanel({ plan, health }: { plan: LowTimeframeSnapshot; health: LowTimeframeHealth | null }) { const current = health ?? { state: "HEALTH UNKNOWN" as const, reasons: ["No manual low-timeframe health observation has been requested. The immutable entry snapshot is shown below without inferring a current state."], targetPath: plan.targetPath ?? [], data: { provider: null, generatedAt: null, availability: "UNAVAILABLE" as const } }; const tone = current.state === "HEALTHY" ? "border-emerald-300/20 bg-emerald-300/[.06] text-emerald-100" : current.state === "WARNING" ? "border-amber-300/20 bg-amber-300/[.06] text-amber-100" : current.state === "DANGER" || current.state === "INVALIDATED" ? "border-rose-300/20 bg-rose-300/[.06] text-rose-100" : "border-slate-400/20 bg-slate-400/[.06] text-slate-200"; return <section className={`rounded-2xl border p-5 ${tone}`}><p className="text-[10px] font-semibold uppercase tracking-[.17em]">Low-timeframe Scalping Health · manual only</p><div className="mt-2 flex flex-wrap items-center gap-3"><h3 className="text-base font-semibold">{current.state}</h3><span className="rounded-full border border-current/20 px-2 py-1 text-[10px] font-semibold">{plan.dataBundle?.state ?? "UNAVAILABLE"} SNAPSHOT</span></div><p className="mt-2 text-xs leading-5 opacity-80">This separate 1m / 3m / 5m observation never changes the paper position, entry economics, stop, targets, alerts, or monitoring events. It requires a user-triggered online refresh.</p><p className="mt-1 text-[10px] opacity-70">Current provider {current.data.provider ?? "UNAVAILABLE"} · {current.data.availability} · {asTime(current.data.generatedAt)}</p><div className="mt-4 grid gap-4 lg:grid-cols-2"><div><h4 className="text-[10px] font-semibold uppercase tracking-[.14em] opacity-70">Immutable entry evidence</h4><div className="mt-2 grid gap-2 sm:grid-cols-3">{plan.timeframeStates?.map(state => <div key={state.timeframe} className="rounded-lg border border-current/15 bg-black/10 p-2"><strong className="text-xs">{state.timeframe.toUpperCase()} · {state.bias}</strong><p className="mt-1 text-[10px] opacity-75">RSI {state.rsi ?? "—"} · volume {state.volumeExpansion ?? "—"}×</p><p className="mt-1 text-[10px] opacity-75">Structure {state.structure}</p></div>) ?? <p className="text-xs opacity-75">UNAVAILABLE — no immutable timeframe state exists.</p>}</div><p className="mt-3 text-[10px] opacity-75">Provider {plan.provider ?? "UNAVAILABLE"} · captured {asTime(plan.dataTimestamp)} · alignment {plan.alignment ?? "UNAVAILABLE"}</p></div><div><h4 className="text-[10px] font-semibold uppercase tracking-[.14em] opacity-70">Manual health reasons</h4><ul className="mt-2 space-y-1.5 text-xs leading-5">{current.reasons.map(reason => <li key={reason}>• {reason}</li>)}</ul><div className="mt-3 flex flex-wrap gap-2">{current.targetPath.map(step => <span key={step.label} className="rounded border border-current/20 bg-black/10 px-2 py-1 text-[10px]"><strong>{step.label}</strong> · {money.format(step.price)} · {step.status}</span>)}</div></div></div></section>; }
client/src/components/crypto/PaperTradingWorkspace.tsx:92:function countHealth(trades: Trade[]) { return trades.reduce<Record<TradeHealth["state"], number>>((counts, trade) => ({ ...counts, [trade.tradeHealth.state]: counts[trade.tradeHealth.state] + 1 }), { HEALTHY: 0, CAUTION: 0, "REVERSAL RISK": 0, INVALIDATED: 0, "HEALTH UNKNOWN": 0 }); }
client/src/components/crypto/TradeSetupWorkspace.tsx:15:  assetId: string; symbol: string; mode: Mode; actionable: boolean; presentationStatus: "QUALIFIED" | "WATCH" | "NO TRADE"; rank: number | null; watch: { missing: string[]; wouldQualifyWhen: string[] } | null; availability: "LIVE" | "STALE" | "UNAVAILABLE"; direction: "LONG" | "SHORT" | "NO TRADE"; setupType: string; expectedHorizon: string; dataTimestamp: number | null; provider: string | null; dataBundle: DataBundle; opportunityScore: number | null; regimeScore: number | null; marketContext: string; currentPrice: number | null; entryZone: { low: number; high: number; preferred: number; reason: string } | null; stop: { price: number; reason: string } | null; invalidation: { price: number; reason: string } | null; targets: Array<{ label: string; price: number; reason: string; priority: string }>; rewardRisk: number | null; readinessCandidate: { availability: "SUPPORTED" | "PARTIAL" | "UNAVAILABLE"; reason: string; entryZone: { low: number; high: number; preferred: number; reason: string } | null; stop: { price: number; reason: string } | null; invalidation: { price: number; reason: string } | null; targets: Array<{ label: string; price: number; reason: string; priority: string }>; rewardRisk: number | null } | null; evidence: string[]; risks: string[]; unavailable: string[]; diagnostics: Condition[];
client/src/components/crypto/TradeSetupWorkspace.tsx:22:const presentationHealth = (plan: Plan) => plan.presentationStatus === "QUALIFIED" ? "HEALTHY" : plan.presentationStatus === "WATCH" ? "CAUTION · WAITING CONFIRMATION" : plan.availability === "STALE" ? "HEALTH UNKNOWN · STALE" : "HEALTH UNKNOWN";
client/src/components/crypto/TradeSetupWorkspace.tsx:45:  const qualified = plan.presentationStatus === "QUALIFIED" && plan.actionable && plan.entryZone && plan.stop && plan.invalidation;
client/src/components/crypto/TradeSetupWorkspace.tsx:46:  const statusTone = plan.presentationStatus === "QUALIFIED" ? "border-emerald-300/25 bg-emerald-300/[.07] text-emerald-100" : plan.presentationStatus === "WATCH" ? "border-amber-300/25 bg-amber-300/[.07] text-amber-100" : "border-slate-500/25 bg-slate-500/[.07] text-slate-300";
client/src/components/crypto/TradeSetupWorkspace.tsx:48:  return <article className={`overflow-hidden rounded-2xl border ${qualified ? "border-cyan-300/18 bg-[#09111f]" : plan.presentationStatus === "WATCH" ? "border-amber-300/20 bg-amber-300/[.025]" : "border-slate-500/20 bg-slate-950/55"}`}>
client/src/components/crypto/TradeSetupWorkspace.tsx:51:    {qualified ? <div className="grid gap-4 p-5 xl:grid-cols-[1.1fr_.9fr]"><div className="grid gap-2 sm:grid-cols-2 lg:grid-cols-4"><Level label="Entry zone" value={`${money.format(plan.entryZone!.low)} – ${money.format(plan.entryZone!.high)}`} note={plan.entryZone!.reason} /><Level label="Preferred entry" value={money.format(plan.entryZone!.preferred)} note="Execution-timeframe technical level" /><Level label="Stop / invalidation" value={money.format(plan.stop!.price)} note={plan.stop!.reason} tone="risk" />{plan.targets.map(target => <Level key={target.label} label={`${target.label} · ${targetRr(target) === null ? "R:R unavailable" : `R:R ${targetRr(target)!.toFixed(2)}`}`} value={money.format(target.price)} note={target.reason} tone="positive" />)}</div><Evidence plan={plan} /></div> : plan.presentationStatus === "WATCH" ? <WatchEvidence plan={plan} /> : <NoTradeEvidence plan={plan} />}
client/src/components/crypto/TradeSetupWorkspace.tsx:57:function WatchEvidence({ plan }: { plan: Plan }) { return <div className="space-y-4 p-5"><div className="flex gap-3"><Clock3 className="mt-0.5 h-5 w-5 shrink-0 text-amber-200" /><div><p className="font-semibold text-amber-100">WATCH — NOT A TRADE</p><p className="mt-1 text-sm leading-6 text-slate-300">Current data is coherent, but an existing strategy requirement has not passed. No Paper Trading action is created.</p></div></div><div className="grid gap-3 md:grid-cols-2"><TextList title="Missing confirmation" items={plan.watch?.missing ?? []} tone="amber" /><TextList title="Would become eligible when" items={plan.watch?.wouldQualifyWhen ?? []} tone="cyan" /></div>{plan.readinessCandidate ? <ConditionalCandidate plan={plan} /> : null}<Diagnostics conditions={plan.diagnostics} /></div>; }
client/src/components/crypto/TradeSetupWorkspace.tsx:59:function NoTradeEvidence({ plan }: { plan: Plan }) { return <div className="space-y-4 p-5"><div className="flex gap-3"><CircleAlert className="mt-0.5 h-5 w-5 shrink-0 text-slate-400" /><div><p className="font-semibold text-slate-100">NO TRADE — WHY?</p><p className="mt-1 text-sm leading-6 text-slate-400">{plan.unavailable.join(" ") || "The existing setup engine did not confirm a plan from current validated inputs."}</p><p className="mt-2 text-xs text-slate-500">These cards report existing requirements only. No condition was loosened and no level was fabricated.</p></div></div><Diagnostics conditions={plan.diagnostics} /></div>; }
client/src/components/crypto/TradeSetupWorkspace.tsx:63:function SetupOverview({ summary, plans }: { summary: DiagnosticSummary; plans: Plan[] }) { const qualified = plans.filter(plan => plan.presentationStatus === "QUALIFIED").length; const watch = plans.filter(plan => plan.presentationStatus === "WATCH").length; return <section className="rounded-2xl border border-cyan-300/15 bg-cyan-300/[.025] p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-[10px] font-semibold uppercase tracking-[.16em] text-cyan-200">Current setup overview</p><h3 className="mt-1 text-sm font-semibold text-slate-100">{summary.evaluatedAssets} assets scanned · {qualified} qualified · {watch} watch · {Math.max(0, summary.noTradeAssets - watch)} no trade</h3></div><p className="max-w-md text-xs leading-5 text-slate-400">Qualified ranking uses the existing Opportunity Score only. Watch and No Trade remain non-actionable until every existing condition passes.</p></div><div className="mt-4 grid gap-2 sm:grid-cols-2 xl:grid-cols-4"><Level label="Lack of market setup" value={String(summary.classification.lackOfMarketSetups)} note="Existing direction/structure did not produce a plan" /><Level label="Missing data" value={String(summary.classification.missingData)} note="At least one required input was unavailable" tone="risk" /><Level label="Stale data" value={String(summary.classification.staleData)} note="Existing provider state marked an input stale" /><Level label="Existing setup requirement" value={String(summary.classification.existingSetupRequirement)} note="Valid levels failed the existing R:R minimum" tone="risk" /></div>{summary.topNoTradeReasons.length ? <div className="mt-4 border-t border-white/[.08] pt-3"><p className="text-[10px] font-semibold uppercase tracking-[.14em] text-slate-500">Top current NO TRADE reasons</p><div className="mt-2 flex flex-wrap gap-2">{summary.topNoTradeReasons.map(reason => <span key={`${reason.key}-${reason.status}`} className="rounded-full border border-white/[.1] bg-black/10 px-2.5 py-1 text-[11px] text-slate-300">{reason.label}: {reason.count} {reason.status.toLowerCase()}</span>)}</div></div> : null}</section>; }
client/src/components/crypto/LowTimeframeScalpingWorkspace.tsx:7:  assetId: string; symbol: string; displaySymbol: string; actionable: boolean; presentationStatus: "QUALIFIED" | "WATCH" | "NO TRADE"; rank: number | null; direction: "LONG" | "SHORT" | "NO TRADE"; expectedDuration: string | null; provider: string | null; providerSymbol: string | null; dataTimestamp: number | null;
client/src/components/crypto/LowTimeframeScalpingWorkspace.tsx:38:  const qualified = plan.presentationStatus === "QUALIFIED" && plan.actionable && plan.entryZone && plan.stop;
client/src/components/crypto/LowTimeframeScalpingWorkspace.tsx:39:  const tone = qualified ? "border-emerald-300/20 bg-[#09111f]" : plan.presentationStatus === "WATCH" ? "border-amber-300/20 bg-amber-300/[.025]" : "border-slate-500/20 bg-slate-950/55";
client/src/components/crypto/LowTimeframeScalpingWorkspace.tsx:40:  const badge = qualified ? "border-emerald-300/25 bg-emerald-300/[.07] text-emerald-100" : plan.presentationStatus === "WATCH" ? "border-amber-300/25 bg-amber-300/[.07] text-amber-100" : "border-slate-500/25 bg-slate-500/[.07] text-slate-300";
client/src/components/crypto/LowTimeframeScalpingWorkspace.tsx:50:function NoTrade({ plan }: { plan: LowPlan }) { return <div className="space-y-4 p-5"><div className="flex gap-3"><CircleAlert className="mt-0.5 h-5 w-5 shrink-0 text-slate-400" /><div><p className="font-semibold text-slate-100">{plan.presentationStatus === "WATCH" ? "WATCH — NOT A TRADE" : "NO TRADE — WHY?"}</p><p className="mt-1 text-sm leading-6 text-slate-400">{plan.noTradeReasons.join(" ") || "Current low-timeframe evidence did not support a valid plan."}</p><p className="mt-2 text-xs text-slate-500">No 1m/3m/5m series was synthesized, resampled, mixed, or used after becoming stale.</p></div></div><TimeframeStates states={plan.timeframeStates} /><Evidence title="Observed evidence" items={plan.evidence} tone="neutral" /><Evidence title="Risk / missing confirmation" items={plan.risks.length ? plan.risks : [plan.setupQuality.detail]} tone="risk" /></div>; }
client/src/components/crypto/OpportunityDiscoveryWorkspace.tsx:8:type Status = "QUALIFIED" | "POTENTIAL" | "WATCH" | "NO TRADE" | "DATA UNAVAILABLE";
client/src/components/crypto/OpportunityDiscoveryWorkspace.tsx:10:type HealthFilter = "ALL" | "HEALTHY" | "CAUTION" | "REVERSAL RISK" | "INVALIDATED" | "HEALTH UNKNOWN";
client/src/components/crypto/OpportunityDiscoveryWorkspace.tsx:15:  assetId: string; symbol: string; status: Status; maturity: "EARLY" | "DEVELOPING" | "CONFIRMING" | "QUALIFIED" | "INVALIDATED" | "UNAVAILABLE"; setupReadiness: { state: "READY" | "NEAR_READY" | "EARLY" | "WATCH" | "INVALID" | "DATA_UNAVAILABLE"; score: number | null; components: { timeframeAlignment: number; positiveEvidence: number; technicalPlan: number; rewardRisk: number; regime: number }; explanation: string }; tradeReadiness: "READY" | "WAITING" | "RESTRICTED" | "NOT ELIGIBLE" | "UNAVAILABLE"; paperTradeEligible: boolean; potentialAlertEligible: false; rank: number | null; opportunityScore: number | null; direction: "LONG" | "SHORT" | "NO TRADE"; provider: string | null; dataTimestamp: number | null; freshness: "LIVE" | "STALE" | "UNAVAILABLE"; validationStatus: string | null; timeframes: { execution: string; confirmation: string; context: string }; regime: { classification: string | null; restricted: boolean }; timeframeAgreement: { aligned: number; required: number; direction: string; label: string }; whyInteresting: string[]; missingEvidence: string[]; confirmationRequirements: string[]; invalidationExplanation: string; whatWouldChange: string[]; conditionalEntry: string | null; readinessPlan: { availability: "SUPPORTED" | "PARTIAL" | "UNAVAILABLE"; currentPrice: number | null; entryZone: { low: number; high: number; preferred: number; reason: string } | null; confirmation: string | null; invalidation: { price: number; reason: string } | null; targets: Array<{ label: string; price: number; distancePercent: number | null; rMultiple: number | null; reason: string }>; rewardRisk: number | null; reason: string; health: { state: "WAITING" | "UNAVAILABLE"; reason: string } }; exactReason: string; dataReason: string | null; sourcePlan: { entryZone: { preferred: number; reason: string } | null; stop: { price: number; reason: string } | null; targets: Array<{ label: string; price: number; reason: string }>; rewardRisk: number | null };
client/src/components/crypto/OpportunityDiscoveryWorkspace.tsx:20:const tone: Record<Status, string> = { QUALIFIED: "border-emerald-300/25 bg-emerald-300/[.08] text-emerald-100", POTENTIAL: "border-amber-300/25 bg-amber-300/[.08] text-amber-100", WATCH: "border-cyan-300/25 bg-cyan-300/[.08] text-cyan-100", "NO TRADE": "border-rose-300/25 bg-rose-300/[.07] text-rose-100", "DATA UNAVAILABLE": "border-slate-400/20 bg-slate-400/[.06] text-slate-200" };
client/src/components/crypto/OpportunityDiscoveryWorkspace.tsx:22:const healthFor = (item: DiscoveryItem): Exclude<HealthFilter, "ALL"> => item.status === "QUALIFIED" ? "HEALTHY" : item.status === "POTENTIAL" || item.status === "WATCH" ? "CAUTION" : item.status === "NO TRADE" && item.maturity === "INVALIDATED" ? "INVALIDATED" : "HEALTH UNKNOWN";
client/src/components/crypto/OpportunityDiscoveryWorkspace.tsx:41:    {query.isLoading ? <Loading /> : query.error ? <ErrorPanel detail={query.error.message} /> : data && summary ? <><Summary summary={summary} /><section className="space-y-2"><div className="flex flex-wrap gap-2" aria-label="Opportunity status filters">{(["ALL", "QUALIFIED", "POTENTIAL", "WATCH", "NO TRADE", "DATA UNAVAILABLE"] as Filter[]).map(value => <Button key={value} size="sm" variant="outline" onClick={() => setFilter(value)} className={`border-white/[.1] text-[11px] ${filter === value ? "bg-cyan-300/[.12] text-cyan-100" : "bg-white/[.02] text-slate-400"}`}>{value === "ALL" ? "All" : value}</Button>)}</div><div className="flex flex-wrap items-center gap-2" aria-label="Opportunity intelligence filters"><span className="text-[10px] font-semibold uppercase tracking-[.14em] text-slate-500">Health</span>{(["ALL", "HEALTHY", "CAUTION", "REVERSAL RISK", "INVALIDATED", "HEALTH UNKNOWN"] as HealthFilter[]).map(value => <Button key={value} size="sm" variant="outline" onClick={() => setHealthFilter(value)} className={`h-7 border-white/[.1] text-[10px] ${healthFilter === value ? "bg-amber-300/[.12] text-amber-100" : "bg-white/[.02] text-slate-400"}`}>{value === "ALL" ? "All health" : value}</Button>)}<span className="text-[10px] font-semibold uppercase tracking-[.14em] text-slate-500">Direction</span>{(["ALL", "LONG", "SHORT"] as DirectionFilter[]).map(value => <Button key={value} size="sm" variant="outline" onClick={() => setDirectionFilter(value)} className={`h-7 border-white/[.1] text-[10px] ${directionFilter === value ? "bg-cyan-300/[.12] text-cyan-100" : "bg-white/[.02] text-slate-400"}`}>{value === "ALL" ? "All directions" : value}</Button>)}<span className="text-[10px] font-semibold uppercase tracking-[.14em] text-slate-500">Plan</span>{(["ALL", "ENTRY READY", "WAITING CONFIRMATION", "INCOMPLETE"] as PlanFilter[]).map(value => <Button key={value} size="sm" variant="outline" onClick={() => setPlanFilter(value)} className={`h-7 border-white/[.1] text-[10px] ${planFilter === value ? "bg-violet-300/[.12] text-violet-100" : "bg-white/[.02] text-slate-400"}`}>{value === "ALL" ? "All plans" : value}</Button>)}</div><div className="flex flex-wrap items-center gap-2 text-[10px] text-slate-500"><span className="font-semibold uppercase tracking-[.14em]">Sort</span>{(["DEFAULT", "CLOSEST", "OPPORTUNITY"] as Sort[]).map(value => <Button key={value} size="sm" variant="outline" onClick={() => setSort(value)} className={`h-7 border-white/[.1] text-[10px] ${sort === value ? "bg-violet-300/[.12] text-violet-100" : "bg-white/[.02] text-slate-400"}`}>{value === "DEFAULT" ? "Status + Opportunity" : value === "CLOSEST" ? "Closest to qualification" : "Highest Opportunity"}</Button>)}<span>Readiness is a separate deterministic explanation, never an Opportunity Score replacement.</span></div></section><section className="space-y-4">{items.length ? items.map(item => <DiscoveryCard key={item.assetId} item={item} online={online} onInspect={() => onInspectAsset(item.assetId)} onPaper={() => onPaperTrade(item.assetId)} onMonitor={() => createMonitor.mutate({ assetId: item.assetId, mode: "SWING" })} monitorPending={createMonitor.isPending && createMonitor.variables?.assetId === item.assetId} />) : <Empty />}</section></> : <Empty />}
client/src/components/crypto/OpportunityDiscoveryWorkspace.tsx:48:  const qualified = item.status === "QUALIFIED" && item.paperTradeEligible;
client/src/components/crypto/OpportunityDiscoveryWorkspace.tsx:49:  const Icon = item.status === "QUALIFIED" ? CircleCheck : item.status === "DATA UNAVAILABLE" || item.status === "NO TRADE" ? CircleAlert : Clock3;
client/src/components/crypto/OpportunityDiscoveryWorkspace.tsx:50:  return <article className={`overflow-hidden rounded-2xl border ${item.status === "QUALIFIED" ? "border-emerald-300/20 bg-[#09111f]" : "border-white/[.08] bg-slate-950/55"}`}>
client/src/components/crypto/OpportunityDiscoveryWorkspace.tsx:51:    <div className="flex flex-wrap items-start justify-between gap-3 border-b border-white/[.06] px-5 py-4"><div className="flex items-start gap-3"><span className="mt-0.5 font-mono text-xs text-slate-500">{item.rank ? String(item.rank).padStart(2, "0") : "—"}</span><div><div className="flex flex-wrap items-center gap-2"><h3 className="text-lg font-semibold">{item.symbol}</h3><span className={`rounded-full border px-2 py-1 text-[10px] font-semibold tracking-wide ${tone[item.status]}`}>{item.status}</span><span className="rounded-full border border-white/[.08] px-2 py-1 text-[10px] text-slate-400">{item.maturity}</span><span className="rounded-full border border-white/[.08] px-2 py-1 text-[10px] text-slate-400">{item.direction}</span><span className="rounded-full border border-white/[.08] px-2 py-1 text-[10px] text-slate-400">Health · {healthFor(item)}</span><span className="rounded-full border border-white/[.08] px-2 py-1 text-[10px] text-slate-400">Plan · {planFor(item)}</span></div><p className="mt-1 text-[11px] text-slate-500">Opportunity {item.opportunityScore ?? "UNAVAILABLE"}/100 · {item.provider ?? "UNAVAILABLE"} · {time(item.dataTimestamp)} · {item.timeframeAgreement.label}</p></div></div><div className="flex flex-wrap gap-2"><Button size="sm" variant="outline" onClick={onInspect} className="border-white/[.1] text-slate-300"><Eye className="mr-1.5 h-3.5 w-3.5" />Asset Intelligence</Button>{["POTENTIAL", "QUALIFIED", "WATCH"].includes(item.status) ? <Button size="sm" variant="outline" onClick={onMonitor} disabled={!online || monitorPending} className="border-violet-300/20 text-violet-100"><BookmarkPlus className="mr-1.5 h-3.5 w-3.5" />{monitorPending ? "Saving…" : "Monitor setup"}</Button> : null}{qualified ? <Button size="sm" onClick={onPaper} disabled={!online} className="bg-emerald-300 text-slate-950 hover:bg-emerald-200 disabled:bg-slate-600"><ArrowUpRight className="mr-1.5 h-3.5 w-3.5" />Paper {item.direction}</Button> : null}</div></div>
client/src/components/crypto/OpportunityDiscoveryWorkspace.tsx:64:function UpgradeConditions({ item }: { item: DiscoveryItem }) { return <section className="rounded-xl border border-amber-300/15 bg-amber-300/[.035] p-4"><p className="text-[10px] font-semibold uppercase tracking-[.15em] text-amber-200">What would change this setup?</p><p className="mt-2 text-xs leading-5 text-slate-300">{item.status === "POTENTIAL" || item.status === "WATCH" ? "Monitoring evidence only — not a trade instruction." : "This state is not eligible for an entry."}</p>{item.whatWouldChange.length ? <ul className="mt-3 space-y-1.5 text-xs leading-5 text-slate-300">{item.whatWouldChange.map(reason => <li key={reason}>• {reason}</li>)}</ul> : null}{item.conditionalEntry ? <div className="mt-3 rounded-lg border border-cyan-300/15 bg-cyan-300/[.035] p-3"><p className="text-[9px] font-semibold uppercase tracking-[.14em] text-cyan-200">Conditional entry</p><p className="mt-1 text-xs leading-5 text-cyan-100/85">{item.conditionalEntry}</p></div> : null}</section>; }
client/src/components/crypto/SetupMonitorWorkspace.tsx:9:const statusTone = (status: string) => status === "QUALIFIED" || status.includes("TARGET") ? "border-emerald-300/25 bg-emerald-300/[.07] text-emerald-200" : status === "INVALIDATED" || status === "ARCHIVED" ? "border-rose-300/25 bg-rose-300/[.07] text-rose-200" : status === "DATA_UNAVAILABLE" ? "border-slate-300/20 bg-slate-300/[.06] text-slate-300" : "border-amber-300/25 bg-amber-300/[.07] text-amber-200";
client/src/components/crypto/SetupMonitorWorkspace.tsx:10:const healthTone = (health: string) => health === "HEALTHY" ? "text-emerald-200" : health === "INVALIDATED" || health === "REVERSAL_RISK" ? "text-rose-200" : health === "DATA_UNAVAILABLE" ? "text-slate-300" : "text-amber-200";
client/src/components/crypto/SetupMonitorWorkspace.tsx:18:const tradePlan = (row: any) => { const snapshot = currentSnapshot(row); return snapshot.status === "QUALIFIED" ? "ENTRY_READY" : snapshot.entryZone && Array.isArray(snapshot.targets) && snapshot.targets.length ? "WAITING_CONFIRMATION" : "INCOMPLETE"; };
client/src/components/crypto/SetupMonitorWorkspace.tsx:44:      <div className="mt-4 flex flex-wrap gap-2"><Filter label="Health" value={healthFilter} onChange={setHealthFilter} options={[["ALL", "All health"], ["HEALTHY", "Healthy"], ["CAUTION", "Caution"], ["REVERSAL_RISK", "Reversal risk"], ["INVALIDATED", "Invalidated"], ["DATA_UNAVAILABLE", "Unavailable"]]} /><Filter label="Direction" value={directionFilter} onChange={setDirectionFilter} options={[["ALL", "All directions"], ["LONG", "Long"], ["SHORT", "Short"]]} /><Filter label="Trade plan" value={planFilter} onChange={setPlanFilter} options={[["ALL", "All plans"], ["ENTRY_READY", "Entry ready"], ["WAITING_CONFIRMATION", "Waiting confirmation"], ["INCOMPLETE", "Incomplete"]]} /></div>
client/src/components/crypto/SetupMonitorWorkspace.tsx:66:function HealthCard({ health: value, reason }: { health: string; reason: string }) { const tone = healthTone(value); const Icon = value === "HEALTHY" ? CircleCheck : value === "DATA_UNAVAILABLE" ? CircleAlert : Gauge; return <div className="rounded-xl border border-white/[.07] bg-white/[.025] p-4"><div className="flex items-center gap-2"><Icon className={cn("h-4 w-4", tone)} /><p className={cn("text-[10px] font-semibold uppercase tracking-[.17em]", tone)}>Trade health · {label(value)}</p></div><p className="mt-2 text-sm leading-6 text-slate-300">{reason}</p></div>; }
client/src/pwa/PwaStatus.tsx:9:  connectionState: "ONLINE" | "RECONNECTING" | "DATA UNAVAILABLE" | "OFFLINE · READ ONLY";
client/src/pwa/PwaStatus.tsx:32:  const [connectionState, setConnectionState] = useState<"ONLINE" | "RECONNECTING" | "DATA UNAVAILABLE" | "OFFLINE · READ ONLY">(() => typeof navigator === "undefined" || navigator.onLine ? "DATA UNAVAILABLE" : "OFFLINE · READ ONLY");
client/src/pwa/PwaStatus.tsx:82:    setConnectionState(hasLiveData ? "ONLINE" : "DATA UNAVAILABLE");
client/src/pwa/PwaStatus.tsx:89:    setConnectionState("DATA UNAVAILABLE");
client/src/pwa/PwaStatus.tsx:110:      {offline ? <span className="flex items-center gap-2"><WifiOff className="h-3.5 w-3.5" /><strong>OFFLINE · READ ONLY</strong><span className="text-amber-100/75">LAST UPDATED: {lastUpdated} · LIVE DATA UNAVAILABLE · Paper Trading requires a live connection to Crypto Hub.</span></span> : reconnecting ? <span className="flex items-center gap-2"><RefreshCw className="h-3.5 w-3.5 animate-spin" /><strong>RECONNECTING</strong><span className="text-cyan-100/75">Network access returned; waiting for a server-validated live-data response.</span></span> : connectionState === "DATA UNAVAILABLE" ? <span className="flex items-center gap-2"><Wifi className="h-3.5 w-3.5" /><strong>DATA UNAVAILABLE</strong><span className="text-cyan-100/75">Network is available, but current server-validated live market data is unavailable.</span></span> : <span className="flex items-center gap-2"><RefreshCw className="h-3.5 w-3.5" /><strong>UPDATE READY</strong><span className="text-cyan-100/75">A newer app shell is available. Reload only when you are not confirming a Paper Trade or Research action.</span></span>}
== UI AUDIT FILES ==
client/src/App.tsx
client/src/components/AIChatBox.tsx
client/src/components/DashboardLayout.tsx
client/src/components/DashboardLayoutSkeleton.tsx
client/src/components/ErrorBoundary.tsx
client/src/components/ManusDialog.tsx
client/src/components/Map.tsx
client/src/components/crypto/AlertExecutionHistory.tsx
client/src/components/crypto/AlertsPanel.tsx
client/src/components/crypto/AssetIntelligencePanel.tsx
client/src/components/crypto/BacktestingPanel.tsx
client/src/components/crypto/BackupRecoveryPanel.tsx
client/src/components/crypto/ExecutionCostLab.tsx
client/src/components/crypto/HistoricalDataFoundation.tsx
client/src/components/crypto/LowTimeframeScalpingWorkspace.tsx
client/src/components/crypto/OpportunityDiscoveryWorkspace.tsx
client/src/components/crypto/OpportunityResearchLab.tsx
client/src/components/crypto/PaperTradingPanel.tsx
client/src/components/crypto/PaperTradingWorkspace.tsx
client/src/components/crypto/ResearchRunWorkspace.tsx
client/src/components/crypto/ResearchSummaryPanel.tsx
client/src/components/crypto/SettingsPanel.tsx
client/src/components/crypto/SetupMonitorWorkspace.tsx
client/src/components/crypto/TechnicalChart.tsx
client/src/components/crypto/TradeSetupWorkspace.tsx
client/src/components/ui/accordion.tsx
client/src/components/ui/alert-dialog.tsx
client/src/components/ui/alert.tsx
client/src/components/ui/aspect-ratio.tsx
client/src/components/ui/avatar.tsx
client/src/components/ui/badge.tsx
client/src/components/ui/breadcrumb.tsx
client/src/components/ui/button-group.tsx
client/src/components/ui/button.tsx
client/src/components/ui/calendar.tsx
client/src/components/ui/card.tsx
client/src/components/ui/carousel.tsx
client/src/components/ui/chart.tsx
client/src/components/ui/checkbox.tsx
client/src/components/ui/collapsible.tsx
client/src/components/ui/command.tsx
client/src/components/ui/context-menu.tsx
client/src/components/ui/dialog.tsx
client/src/components/ui/drawer.tsx
client/src/components/ui/dropdown-menu.tsx
client/src/components/ui/empty.tsx
client/src/components/ui/field.tsx
client/src/components/ui/form.tsx
client/src/components/ui/hover-card.tsx
client/src/components/ui/input-group.tsx
client/src/components/ui/input-otp.tsx
client/src/components/ui/input.tsx
client/src/components/ui/item.tsx
client/src/components/ui/kbd.tsx
client/src/components/ui/label.tsx
client/src/components/ui/menubar.tsx
client/src/components/ui/navigation-menu.tsx
client/src/components/ui/pagination.tsx
client/src/components/ui/popover.tsx
client/src/components/ui/progress.tsx
client/src/components/ui/radio-group.tsx
client/src/components/ui/resizable.tsx
client/src/components/ui/scroll-area.tsx
client/src/components/ui/select.tsx
client/src/components/ui/separator.tsx
client/src/components/ui/sheet.tsx
client/src/components/ui/sidebar.tsx
client/src/components/ui/skeleton.tsx
client/src/components/ui/slider.tsx
client/src/components/ui/sonner.tsx
client/src/components/ui/spinner.tsx
client/src/components/ui/switch.tsx
client/src/components/ui/table.tsx
client/src/components/ui/tabs.tsx
client/src/components/ui/textarea.tsx
client/src/components/ui/toggle-group.tsx
client/src/components/ui/toggle.tsx
client/src/components/ui/tooltip.tsx
client/src/contexts/ThemeContext.tsx
client/src/hooks/useMobile.tsx
client/src/index.css
client/src/main.tsx
client/src/pages/ComponentShowcase.tsx
client/src/pages/Home.tsx
client/src/pages/NotFound.tsx
client/src/pwa/PwaMobileNavigation.tsx
client/src/pwa/PwaStatus.tsx
