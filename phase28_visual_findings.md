# Phase 28 Visual Findings

The final desktop capture at 1440x900 confirms that `/decision?status=potential&strategy=scalp` opens Decision Center directly. The page presents Market State, Decision Summary, URL-preserved filters, the honest unavailable/data-limited state, and the Auto Paper owner gate in the intended order.

The final iPhone capture at 390x844 confirms that the heading wraps cleanly, Refresh and Back remain reachable, a server-returned Potential card can render within the viewport, and the mobile primary navigation remains available with compact labels and touch-sized controls. No horizontal overflow was observed. The preview data status was server-derived and changed from UNAVAILABLE to FRESH between captures; no values were fabricated.
